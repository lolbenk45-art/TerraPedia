# Batch35 数据库物理外键整改（2026-03-23）

## 目标
- 按新规则整改：不使用物理外键，只保留逻辑外键（`*_id` 字段 + 索引 + 应用层校验）。

## 已完成整改
1. 新增 Flyway 迁移，统一移除物理外键
- 文件：`back/src/main/resources/db/migration/V10__remove_physical_foreign_keys.sql`
- 动作：
  - 删除 `fk_user_refresh_tokens_user_id`（若存在）
  - 删除 `fk_article_review_log_article_id`（若存在）
- 迁移写法为幂等：通过 `information_schema.TABLE_CONSTRAINTS` 动态判断后执行。

2. 同步更新本地基线脚本为“纯逻辑外键”
- 文件：`back/src/main/resources/schema.sql`
- 调整：
  - `articles` 增加审核相关字段（`review_status` 等）
  - 新增 `article_review_log` 表
  - 全部仅保留索引，不包含 `FOREIGN KEY` / `REFERENCES`

## 说明
- 历史迁移 `V7`、`V9` 中仍有外键定义文本（历史不可变更，避免 Flyway checksum 风险）。
- 实际数据库结构会在执行 `V10` 后移除物理外键，满足当前规则。

## 验证结果
- 后端测试：`cd back && mvn -q test` 通过。
- 后端实际启动联调：`python scripts/dev/test_batch31_user_security_flow.py` 通过（`success=true`），说明新迁移可正常执行。
- `schema.sql` 扫描无物理外键关键字。

## 后续建议
- 后续所有新迁移禁止新增 `FOREIGN KEY` 语句。
- 如需彻底“源码层无外键定义”且清理历史迁移文本，建议走一次 Flyway 基线重整（单独窗口执行）。
