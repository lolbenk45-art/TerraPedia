# Batch29 执行规划（2026-03-23）

## 1. 批次目标

- 先修复“运行事实不一致”，再做文档收口，避免继续传播错误结论。
- 锁定本地开发统一口径：Redis `6380/root`、后端 `8888`、前台 `5174`、管理端 `3001`。
- 输出可复用的执行与验收清单，供后续 Batch30 直接接续。

## 2. 当前基线（执行前确认）

- 历史日志存在冲突：
  - `reports/2026-03-18-batch28/admin-dev.log` 显示管理端实际监听 `3000`，与文档中的 `3001` 不一致。
  - `reports/2026-03-18-batch28/backend-dev.log` 显示 Redisson 连接 `127.0.0.1:6379` 并出现密码错误，和本地 Redis 约定 `6380/root` 冲突。
- 当前仓库存在大量构建产物与依赖缓存变更（尤其 `node_modules`、`.nuxt`、`.output`），评审噪音高。

## 3. 本批实施项

### P0（必须完成）

1. 启动脚本参数收敛  
   - 统一 `pnpm run dev` 参数透传写法，修复管理端端口参数可能失效的问题。
2. 后端 Redis 连接口径收敛  
   - 在本地后端启动脚本显式注入 `TERRAPEDIA_REDIS_*`，确保 dev 启动走 `6380/root`。
   - 在后端配置中同步 `spring.data.redis` 与 `spring.redis`，避免不同组件读取不同前缀导致偏差。
3. 文档同步  
   - 更新 `README` 与本地运行说明中的关键命令和环境变量说明。
4. 噪音控制  
   - 扩展 `.gitignore`，阻断新增构建产物/缓存继续进入工作区。

### P1（本批形成方案）

1. 历史已追踪构建产物清理方案  
   - 仅产出清理建议与执行边界，不在本批做大规模删除。

## 4. 执行命令模板

> 以下为 Batch29 验收建议命令，执行时将日志落盘到新目录 `reports/2026-03-23-batch29/`。

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\dev\stop-local-stack.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\dev\start-local-stack.ps1
```

```powershell
# 后端构建
Set-Location .\back
$env:MAVEN_OPTS="-Dmaven.repo.local=G:\ClaudeCode\terraPedia\back\.m2\repository"
mvn -DskipTests package
```

```powershell
# 前台构建
Set-Location .\front
pnpm run build
```

```powershell
# 管理端构建
Set-Location .\data-query-app
$env:NODE_OPTIONS="--max-old-space-size=4096"
pnpm run build
```

## 5. 验收标准（Reviewer Checklist）

- 启动一致性
  - [ ] `http://localhost:5174` 可访问。
  - [ ] `http://localhost:3001` 可访问（不再漂移到 `3000`）。
  - [ ] `http://localhost:8888/api/swagger-ui.html` 可访问。
- Redis 与缓存一致性
  - [ ] 后端运行时不再连接 `6379`。
  - [ ] Redis 认证 `root` 正常，`GET/SET` 正常。
  - [ ] 访问统计/详情接口后出现 `stats:overview`、`item:detail::<id>` 缓存键。
- 文档一致性
  - [ ] `README` 与脚本行为一致（端口、命令、Redis 参数一致）。
  - [ ] 阶段文档不再保留“Redis 未验证/端口未确定”等过时结论。

## 6. 风险与回滚

- 风险 1：本地依赖状态异常（`node_modules` 不完整）导致 dev/build 失败。  
  回滚：保持脚本与文档改动，单独记录“环境修复批次”，不混入业务改动。
- 风险 2：不同组件读取 Redis 配置前缀不一致。  
  回滚：保留双前缀配置并在脚本强制注入环境变量，优先保证可运行。
- 风险 3：仓库历史追踪产物过多，短期无法一次清理。  
  回滚：仅新增忽略规则并输出清理计划，避免大范围改动阻塞主线收口。

## 7. 本批产物

- 脚本与配置收敛改动（启动参数、Redis 配置兼容、忽略规则）。
- 文档更新（README 与本地执行口径）。
- 本规划文档（本文件），作为 Batch29 审核基准。
