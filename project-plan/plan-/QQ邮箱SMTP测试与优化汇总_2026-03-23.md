# QQ邮箱 SMTP 测试与优化汇总（2026-03-23）

## 1. 本轮目标
- 使用你在 `.env` 中提供的 QQ 邮箱配置进行真实发信测试。
- 按本地私有环境变量执行 SMTP 测试，发件方、收件方与授权码均保存在未提交到 Git 的本地配置中。
- 回归检查后端 / 前台 / 后台管理端，确认是否仍有未通过项并做优化说明。

## 2. 环境与配置读取
- 生效配置文件：本地未提交的环境变量文件
- 使用键：
  - `test`（发件邮箱）
  - `test2`（收件邮箱）
  - 本地私有 SMTP 授权码（不记录具体键名）
- 安全处理：测试输出仅显示脱敏邮箱，不回显授权码。

## 3. SMTP 实测结果
- 执行脚本：`terraPedia/scripts/dev/test_qq_smtp.py`
- 执行命令：`python G:\ClaudeCode\terraPedia\scripts\dev\test_qq_smtp.py`
- 结果：`MAIL_SEND_SUCCESS`
- 实际通道：`465/SSL`
- 脱敏日志：`from=15***@QQ.COM to=32***@QQ.COM`

结论：QQ SMTP 真实发信链路可用，本地发件地址到本地收件地址发送成功。

## 4. 三端回归结果
### 4.1 后端（back）
- 命令：`mvn -q test`
- 结果：通过（Exit Code 0）

### 4.2 前台（front）
- 命令：`pnpm run check` + `pnpm run build`
- 结果：通过（Exit Code 0）

### 4.3 后台管理端（data-query-app）
- 命令：`pnpm run check` + `pnpm run build`
- 结果：通过（Exit Code 0）
- 备注：存在历史非阻塞警告（`vue-router/volar/sfc-route-blocks` 路径导出提示），不影响类型检查和构建产物生成。

## 5. 未通过项优化结论
- 本轮未发现新的阻塞失败项。
- 之前“未通过”的主要项已清空，当前均可测试通过。
- 保留项：后台管理端 Volar 插件提示属于兼容性警告，非功能失败。

## 6. 建议的后续优化（可选）
1. 固化 SMTP 自动化校验：将 `test_qq_smtp.py` 纳入联调脚本，发布前可一键验证邮件能力。
2. 处理后台管理端警告：统一 `vue-router` / `vue-tsc` / Volar 相关版本，消除 `sfc-route-blocks` 提示噪音。
3. 增加邮件失败告警：在业务邮件发送逻辑中补充失败重试与审计日志，便于线上排障。

## 7. 本轮产物
- SMTP 测试脚本：`G:\ClaudeCode\terraPedia\scripts\dev\test_qq_smtp.py`
- 汇总文档：`G:\ClaudeCode\terraPedia\project-plan\QQ邮箱SMTP测试与优化汇总_2026-03-23.md`
