# Batch42 三端迁移实施方案（2026-03-28）

## 1. 目标

将 `G:\ClaudeCode\terraPedia-db-redesign` 的 P0 能力迁移到 `G:\ClaudeCode\terraPedia`，并在同一仓库联动三端开发：

1. 前台用户端：`front`
2. 后端服务：`back`
3. 管理端：`data-query-app`

本批次先完成 P0 最小闭环：**后端聚合读接口 + 前台接入 + 可回退 + 全流程日志与汇总机制**。

## 2. 强约束

1. 严格控制上下文窗口：只读取当前改造必需文件，避免无关扫描。
2. 数据库约束：**禁止物理外键（FOREIGN KEY / REFERENCES）**，仅允许逻辑外键（`*_id + 索引 + 应用层校验`）。
3. 写链路保持模块化：不把后台管理 CRUD 聚合成大写接口。
4. 读链路可聚合：前台优先消费聚合只读接口。
5. 改造过程必须记录日志，完结后产出汇总 md，再清理临时日志。

## 3. 多角色讨论结论（摘要）

### 3.1 后端

1. 新增 `GET /public/items/{id}/aggregate`，复用现有只读服务拼装 `item/images/sources/recipes`。
2. 不改现有 `POST/PUT/DELETE` 写接口。
3. 逻辑外键下存在孤儿数据风险，聚合接口需容忍局部空数据。

### 3.2 前台

1. 在 `front/src/api/index.ts` 新增适配层，支持聚合优先 + 旧接口回退。
2. 先改详情页（`ItemDetailView.vue`），随后再扩首页/列表。
3. 增加特性开关 `VITE_ENABLE_P0_AGGREGATE` 用于灰度和一键回滚。

### 3.3 测试与运维

1. 必测：后端编译、前台构建、核心接口冒烟、三端端口联通。
2. 日志目录统一到单次运行目录：`reports/YYYY-MM-DD-p0-<topic>-RNN/`。
3. 完结汇总必须包含：变更清单、测试矩阵、风险遗留、日志清理结果。

## 4. P0 执行清单

1. 新增后端聚合 DTO、Service、Controller。
2. 前台类型与 API 适配层支持聚合读取与回退。
3. 详情页接入聚合数据（至少展示聚合关联摘要）。
4. 运行 `mvn -DskipTests compile` 与前台 `pnpm run check`/`pnpm run build`。
5. 写入过程日志，生成完结汇总文档，清理临时日志。

## 5. 本批次交付物

1. 代码改造（`back` + `front`）
2. 过程日志目录（运行时）
3. 完结汇总文档（md）
4. 临时日志清理记录

