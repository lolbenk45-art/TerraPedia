# 三端联动调试报告（2026-03-23）

## 结论

- 三端联动核心链路已通过本轮调试。
- 本轮共执行 16 项检查，**16/16 通过**。

## 调试前修复

- 修复了启动脚本中 `pnpm run dev` 参数传递问题：
  - 错误传参会让 Nuxt 实际执行为 `nuxt dev "--" ...`，导致管理端端口与路由行为异常。
  - 已改为正常参数传递：
    - `front`: `pnpm run dev --host localhost --port 5174`
    - `data-query-app`: `pnpm run dev --host localhost --port 3001`
- 修复文件：`scripts/dev/start-local-stack.ps1`

## 当前联动状态

- Redis：`127.0.0.1:6380`（可连接）
- 后端：`http://localhost:8888/api`（可用）
- 前台：`http://localhost:5174`（可用）
- 管理端：`http://localhost:3001`（可用）

## 用例与结果

### A. 页面可达性（3/3）

1. `GET http://localhost:5174/` -> 200 ✅
2. `GET http://localhost:3001/` -> 200 ✅
3. `GET http://localhost:8888/api/swagger-ui.html` -> 200 ✅

### B. 后端直连接口（6/6）

1. `GET /api/statistics/overview` -> `success=true` ✅
2. `GET /api/items?page=1&limit=5` -> `success=true` ✅
3. `GET /api/items/suggestions?keyword=sword&limit=5` -> `success=true` ✅
4. `GET /api/items/6122` -> `success=true` ✅
5. `POST /api/auth/login`（admin / <local-admin-password>）-> 获取 token ✅
6. `GET /api/auth/me`、`GET /api/statistics/admin/overview`（Bearer token）-> `success=true` ✅

### C. 代理链路（2/2）

1. 前台代理：`GET http://localhost:5174/api/statistics/overview` -> JSON 且 `success=true` ✅
2. 管理端代理：`GET http://localhost:3001/api/statistics/overview` -> JSON 且 `success=true` ✅

### D. 管理端鉴权与管理接口（4/4）

1. 管理端代理登录：`POST http://localhost:3001/api/auth/login` -> token ✅
2. 管理端代理 `GET /api/auth/me` -> `success=true` ✅
3. 管理端代理 `GET /api/admin/categories/tree` -> `success=true` ✅
4. 管理端代理 `GET /api/items?page=1&limit=3` -> `success=true` ✅

## 风险与后续

- 本轮通过的是“联动冒烟 + 核心功能链路”。
- 若要发布级结论，建议再补：
  - 关键页面交互回归（前台筛选/详情、管理端 CRUD 流程）
  - 一轮端到端自动化（含浏览器操作）
  - 异常场景（鉴权失效、后端重启后缓存一致性）
