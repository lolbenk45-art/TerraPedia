# TerraPedia 严格测试与修复汇总（2026-03-24）

## 1. 执行目标

- 对当前项目进行“严格全量测试”（三端构建/类型检查 + 后端单测 + 核心联调脚本 + SMTP 真链路）。
- 基于测试结果执行修复，并复测直至通过。

## 2. 测试矩阵与结果

| 模块 | 命令 | 结果 | 备注 |
| --- | --- | --- | --- |
| back | `mvn -q test` | 通过 | 仅有 JDK/ByteBuddy warning，不阻塞 |
| front | `pnpm run check && pnpm run build` | 通过 | `vue-tsc` + `vite build` + PWA 产物正常 |
| data-query-app | `pnpm run check && pnpm run build` | 通过 | 有历史工具链 warning，不阻塞 |
| 邮件链路 | `python scripts/dev/test_qq_smtp.py` | 通过 | `MAIL_SEND_SUCCESS`, channel=`465/SSL` |
| 用户安全联调 | `python scripts/dev/test_batch31_user_security_flow.py` | 通过 | `success=true`, `failedCount=0` |
| 文章审核联调 | `python scripts/dev/test_batch36_article_review_flow.py` | 通过 | `success=true`, `failedCount=0` |

## 3. 关键业务链路核验

### 3.1 用户安全链路（Batch31）

- 管理员登录、用户列表、重置密码：通过
- 用户登录/资料更新/改密/登出/重新登录：通过
- 邮箱验证码重置密码：通过
- 错误密码删号拦截：通过
- 注册验证码冷却防刷：通过

结果要点：

- `failedCount=0`
- 后端日志：`back/target/batch31_boot_run.log`

### 3.2 文章审核链路（Batch36）

- 草稿创建 -> 提交审核 -> 驳回 -> 修改 -> 再提交 -> 审核通过：通过
- 发布 -> 公网读取 -> 下线 -> 下线后拦截：通过
- 审核日志动作完整性校验：通过
- 旧状态兼容（直接 `PUBLISHED` 自动 `APPROVED`）：通过

结果要点：

- `failedCount=0`
- 后端日志：`back/target/batch36_article_review_flow.log`

## 4. 本轮修复动作

- 结论：**无阻塞失败项，无需代码修复。**
- 本轮未发生“测试失败 -> 修复代码 -> 再测”的链路，因为所有严格测试首轮即通过。

## 5. 风险与后续优化（非阻塞）

- `data-query-app` 的 `nuxt typecheck` 仍有 `vue-router/volar/sfc-route-blocks` 导出路径 warning。
- `data-query-app build` 仍有 sourcemap / Node deprecation warning。

说明：以上 warning 不影响当前“测试通过”结论，但建议单开批次治理以降低 CI 噪声。

## 6. 最终结论

- 严格测试结论：`PASS`
- 核心功能结论：`PASS`
- 修复结论：`无阻塞缺陷需修复`
