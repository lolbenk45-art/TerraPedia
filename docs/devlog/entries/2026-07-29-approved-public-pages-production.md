# 已确认公共页面正式接入

## Status

`active`

## Goal

将已确认的物品详情、NPC 详情与文章页高保真稿接入 `front-nuxt` 正式路由，保持现有公开接口和三主题令牌契约。

## Scope

- 物品：`pages/items/[id].vue`，以 `item-complete-page-terra-blade-approved-v2.html` 为定稿来源。
- NPC：`pages/npcs/[id].vue`，以已确认 NPC 样本为定稿来源；按接口能力渲染居住、到访、掉落和商店模块。
- 文章：`pages/articles/index.vue`，以 `articles-story-led-v22.html` 的“精选首屏 + 高密度文章库”结构为定稿来源。
- 实施计划：`docs/superpowers/plans/2026-07-29-approved-public-pages-production.md`。
- 保真重建计划：`docs/superpowers/plans/2026-07-29-public-pages-fidelity-rebuild.md`；该计划替代旧计划的“在共享 detail-layout 上叠加版式”实施方向。

## Boundaries

- 不修改 API、数据库、爬虫、`reports/` 或未确认的 `docs/design/terrapedia-*-detail-hifi-v1.html`。
- 不把当前旅商接口的 35 项可用商店数据称为完整 Wiki 货池。
- 不处理合成树数量污染、商人节日图像错配或后端/数据库数据修复；这些保持独立立项。

## Validation plan

- 纯展示派生先通过 Node 单测红绿验证。
- 运行 detail/public-page/visual-system 定向契约与 `pnpm run check`。
- 在 1440px 和 390px 实测物品、城镇 NPC、旅商 NPC 与文章列表。

## Result

- 已接入物品、NPC 与文章正式页面展示层：物品和 NPC 复用现有实时详情字段及共享 detail-layout；文章以实时已发布列表的首条作为精选，余项进入高密度资料库。
- NPC 模块按能力决定是否展示；旅商的商店区明确为当前接口可用数据，不声明为完整 Wiki 货池。
- 新增纯展示 helper 及红绿单测；新页面域样式只使用既有三主题语义令牌和冻结响应式断点。See git for code-level diff details.
- 本轮按确认稿进一步收敛：实体页手机端保留缩略图与事实并列；商人/旅商以商店作为首个工作模块；旅商身份优先于错误的城镇标记；文章精选 fold 改为标题、编号和元数据优先，封面仅作辅助识别。See git for code-level diff details.
- 物品详情的关系数据在 SSR 中已恢复：在第一个请求前捕获公开 API 客户端，避免异步边界后的 Nuxt 上下文丢失使可选关系被吞为空值；泰拉刃现显示真实合成树和制作用途。See git for code-level diff details.

## Validation

- `cd front-nuxt && node --test tests/unit/detailPagePresentation.test.mjs`：3/3 通过，覆盖异步边界后仍可使用已捕获 API 客户端、商店优先与旅商优先于城镇标记。
- `cd front-nuxt && pnpm exec nuxt typecheck`：通过；环境已有重复 auto-import 与 Node 弃用警告不影响退出码。
- `cd front-nuxt && pnpm run check`：通过，包含公共页面、详情布局、视觉令牌、断点与 `nuxt typecheck` 全量契约；Chromium DBus/GPU 与 Node 弃用提示不影响退出码。
- 运行态：`/items/757`、`/npcs/17` 和 `/articles` 在 1440×1000 与 390×844 均为 `overflowX: 0`，无 console/request errors；泰拉刃 SSR 已显示真实合成树与用途。

## Residual risk

- 合成树恢复后其数量、版本命名和递归深度仍完全以既有 API 为准；本任务不修正数据质量。
- 已明确排除的合成树数量污染、商人节日图像错配、旅商完整 Wiki 货池仍待独立任务。

## Follow-up

- Owner: data/API follow-up only for data质量问题；当前 UI 的关系装配已恢复。

## Review

- Reviewer: Codex read-only implementation review.
- Scope: approved item/NPC/article production integration and its presentation contracts.
- Findings resolved: NPC 的城镇入住与旅商到访模块现在按能力实际渲染；文章库不再宣称未经排序契约保证的“最新”；移除了禁止的装饰圆环；high-fidelity 与 production devlog 状态已对齐。
- Re-review: focused presentation test、detail/public-page/visual/breakpoint contracts和完整 `pnpm run check` 均通过。

## Fidelity rebuild direction

- 用户指出运行态与定稿仍有明显结构差距。已冻结新的逐区验收计划：以运行时截图对照定稿，而不是以 CSS 标记或通用详情卡片通过为完成条件；物品先重建配方层级，NPC 重建条件分段商店，文章重建 v22 完整 fold 与 archive shell。See plan for implementation details.
- 计划 review 发现旧版会与既有 detail/public-page/loading/preview/article-layering 合同冲突，并遗漏共享布局、断点白名单、数据降级和截图工具边界。保真计划已修订为“合同先红后绿、每 slice 跑完整 `pnpm run check`、保留共享兼容壳、新组件拆入 `components/detail/`、串行执行”的版本；实施前先执行 Task 0 基线与 SHA 锚定。
- 第二轮 review 已把存量半成品、SSR 关系加载修复、合同迁移权限、gitignored 设计稿、截图落盘和双计划归属收紧为明确边界：先做存量裁定表和用户授权的回滚锚点，再按“Articles → NPC → Item”执行；合同只能精确迁移或增加，不能放宽匹配；全量与补充守门在每个 slice 收尾执行。See fidelity rebuild plan for the executable checklist.

## Task 0 baseline

- WIP rollback anchor authorized and created: `2303e0f3` (`chore(ux): checkpoint public page redesign WIP`). It contains only this task's route, contract, test, SSR-fix, plan, and devlog paths; unapproved detail HTML, old entry, `.shot.mjs`, and `reports/` remain unstaged and user-owned.
- Approved local baseline fingerprints: item v2 `.superpowers/brainstorm/339185-1785149609/content/item-complete-page-terra-blade-approved-v2.html` = `0ea553685475d6371945c747383373d4350427921206c5e6c60a45cc5040ca33`, 79,207 bytes, mtime `2026-07-27 22:43:14 +0800`; merchant v1 `.superpowers/brainstorm/339185-1785149609/content/npc-complete-page-merchant-v1.html` = `b9c8bebae652c9fdf52bde09c5513564f8147722384556a08e9a3c505ca0d9a6`, 73,648 bytes, mtime `2026-07-27 23:45:38 +0800`; articles v22 `.superpowers/brainstorm/3542442-1785229764/content/articles-story-led-v22.html` = `1078d92c2b5dbc4b25c9331df864258f9536e80090136b4c376201bbfe85c6f3`, 37,825 bytes, mtime `2026-07-29 07:21:17 +0800`.
- Gate baseline: `pnpm run check` exit 0; `check:visual-blocked-source` exit 0; `check:loading-skeleton` exit 1 with exactly eight pre-existing missing armor-set markers and none on items/NPC/articles. Runtime-only gate baseline remains pending the dedicated `localhost:5176` server.
- Live article baseline: `/api/articles?page=1&limit=1` returns pagination total 24, so the 12-record fold/archive acceptance is data-reachable. `items/757` is 泰拉刃 and `npcs/17` is 商人.
- Existing-work disposition: page routes, `detail-pages-redesign.css`, `index.css`, and two contract scripts = rebuild-on-top; `detailPagePresentation.ts` and its test = migrate-then-delete; `usePublicApi.ts` and `usePublicItemDetail.ts` = freeze-read-only; two unapproved detail HTML files, `entries/2026-07-27-detail-pages-redesign-hifi.md`, closed `entries/2026-07-29-articles-hifi.md`, `.shot.mjs`, and `reports/` = user-owned untouched, except `.shot.mjs` will be deleted after its existing local purpose is recorded.
- Contract disposition: the currently widened item/NPC layout regexes are recorded as pre-existing debt. The owning slice must restore exact class assertions or obtain an explicit user exception; no slice may treat their widened form as its ratchet baseline.
- Named-region inventory: item = specimen strip/identity-and-stage hero/core-facts rail/合成链/采集清单/可用于制作/其他资料/related facts rail; all consume the existing item detail bundle and compact empty state when absent. NPC = specimen strip/portrait-condition-stat hero/商店 condition bands/幸福度与居住/其他资料/facts rail; all consume the existing NPC aggregate and traveler uses only current live stock. Articles = mast/feature story/reading stack/archive tools+dense rows/popular rail/topic grid; feature/list/rows use published-page fields, while unavailable topic/popular aggregates use the plan's explicit compact labels.
- Runtime baseline restored under the tracked local-stack script. This worktree has isolation slot `3`; invoking it with `TERRAPEDIA_FRONT_PORT=5173` yields the plan-required front `http://127.0.0.1:5176`, backend `http://127.0.0.1:18191`, and admin `13004`. `/articles`, `/items/757`, `/npcs/17`, and `/npcs/368` all return `200`; no data refresh was run.
- Same-server runtime-gate baseline: `check:light-theme` exits `1` on pre-existing cross-route light-theme contrast findings (including `/articles` controls and unavailable historical slugs); `check:typography-spacing` exits `1` waiting for `/search ready`; `check:crafting-wiki-structure` exits `1` on the independent `/crafting` graph ordering, icon-size, and expanded-child assertions. These routes/contracts are not changed by this plan; Task 4 compares the three commands against this baseline rather than repairing them here.
- Named-region/data/degradation inventory, extracted from the three SHA-pinned references: Item `specimen strip` is sample navigation and is omitted in production; `identity-and-stage hero` consumes `PublicItemDetail` name/image/rarity/game-period/description and stat rows (keep loading/not-found state); `core facts rail` uses item id/stats/coverage/real related links (omit empty cards); `合成链` uses `PublicItemDetailBundle.recipeTree` through `buildCraftingRecipeModel` (compact `资料整理中` state); `采集清单` flattens that model (omit if absent); `可用于制作` uses `recipeUsages` (compact empty); `其他资料` uses sources/price/equipment/loot facts (omit unavailable capability); the related rail never invents a completeness score. NPC `specimen strip` is omitted; `portrait-condition-stat hero` uses `PublicNpcAggregate.npc`, wiki assets, spawn/identity/stats (keep loading/not-found); `商店 condition bands` uses `shopEntries` item/price/conditions (omit with no live stock; traveler stays current-stock scoped); `幸福度与居住` uses actual residence/preference relations only; `其他资料` and `facts rail` use only loot/money/traceable facts. Articles `mast` uses pagination/current-page authors (remove fake topic count); `feature story` and `reading stack` use current-page `UserArticle` title/summary/cover/author/date/view/favorite; `archive tools+dense rows` uses the remainder with no padding; `popular rail` is current-page ranking labelled `本页阅读数据`; no topic aggregate exists, so its compact state is `资料整理中` without counts.
- Article capacity ruling: 24 published rows make the twelve-row target reachable. With 6–11 rows, render one feature plus up to five reading-stack entries and place only the remainder in the archive rail; with fewer than six rows or any keyword filter, use archive-only; zero remainder means no archive rail. Pagination stays deep-linked; changing to 12/page deliberately accepts page-boundary drift.
- Reuse mapping: `buildCraftingRecipeModel` owns variant ordering, active recipe selection, stable keys, image fallbacks, quantities, stations, and alternatives; `craftingRecipeCompact.ts` owns compact tree normalization; `recipeHierarchyGraphRenderer.ts` owns rendered graph semantics. Item work only adapts these views. NPC work extracts the existing `shopConditionsLabel` / `shopConditionSummary` / `shopGroupKey` implementation from `pages/npcs/[id].vue`, retaining `safeNpcDisplayText` and stable ids. The article SSR key already includes page and keyword; Task 1 changes its limit and projection only after red tests cover query/page behavior.
- `utils/publicCopy.ts` now owns `PUBLIC_COPY_UNDER_CONSTRUCTION`, `PUBLIC_COPY_PAGE_READING_DATA`, and `PUBLIC_COPY_CURRENT_AVAILABLE_SHOP_DATA`; `tests/unit/publicCopy.test.mjs` was red for missing exports then green. Route consumers move to these constants in their owning slices so literal-location contracts can migrate exactly rather than be widened.
- `front-nuxt/.shot.mjs` was recorded before deletion: a 952-byte untracked, machine-path-bound one-off Playwright screenshot script (mtime `2026-07-28 18:53:13 +0800`) that captured one URL, overflow, and console/request failures. The tracked `audit-shoot.mjs` now provides the same capability with route/viewport overrides, so this plan removes the obsolete helper rather than retaining permanent dirty state.
- Ignored screenshot evidence lives at `front-nuxt/tmp/shots/2026-07-29-public-pages/`. `audit-shoot` at `1440×1000` and `390×844` returned `200`, no horizontal overflow, and no captured console/request failures for all approved probes: `articles--desktop.png` `ae961ab9f0bd06543d70a7b46b463d01ba538a44e810846c426cdace3b7191d0`; `articles--desktop-full.png` `bf3d9ca6c09fb053cb63e30b184b54a470f66f9cd6fc071b16e500332bb7448b`; `articles--approved-mobile.png` `a430824a35fb047fa05df118c9c6a856537f3bddb0ccbdd35a6bc4a949364a56`; `articles--approved-mobile-full.png` `f22a67acd118036c4353feddb5f69ff3acaef56bbde47b6e8f427950c451b727`; `item-terra-blade--desktop.png` `abc1f36a5ecdbd6da65b00af8e67e62201194e798bcc862a1ee9c3a4db7c82db`; `item-terra-blade--desktop-full.png` `d64e7c388372c88880130075803d3cb5298a743559fc95185336a3d905a8ed46`; `item-terra-blade--approved-mobile.png` `b2622f9f3b423b074129e5a6ceb99200e45bf1507e5a8cabc3e6f879a39fbdff`; `item-terra-blade--approved-mobile-full.png` `82b915e91749c5cdfb6201f05d9ffd4183cc62b5942030d08f5191f9825f7a0c`; `npc-merchant--desktop.png` `1ef537d25d95062e6293a95bf54cebe9eb7dbb5ee60d59350bfcf170f2111c7e`; `npc-merchant--desktop-full.png` `a7992aa7ad8a3a2c4409317801caea0d1d1bb3f0da217a469ade45b968cabb00`; `npc-merchant--approved-mobile.png` `7dbc3f74685343be86c61a2e5d2b68e3fa52b9f274a843d809a3a8926a509cf4`; `npc-merchant--approved-mobile-full.png` `3f2245bb7f44fcec40c8a8b96e72e85c3f487d7a321b3bb0949a51b071ac8445`; `npc-traveling-merchant--desktop.png` `8274587e9bf25d0fd6c77d1f841767cc8c45b6cedcd1cb55d6e8f92777455027`; `npc-traveling-merchant--desktop-full.png` `9c66560eaa530ceb91933f20f48da488a37405da075f02100bb99df9ed11f208`; `npc-traveling-merchant--approved-mobile.png` `07e24576db0a3acacb13bd524da349497633949d481789489b0fd38b9a3c118f`; `npc-traveling-merchant--approved-mobile-full.png` `09c1bd4fac70e5d0617a035934630368833bee76950eaa9cc588fbb316004870`.

## Articles contract ratchet

- Expected-red whitelist before the Article move: `check-public-pages.mjs` currently has one exact page-file assertion each for `article-mast`, `article-featured-story`, `article-featured-story__index`, `article-library-shell`, and `article-archive-row`. Destination assertions are added first (three to `components/article/ArticleFeatureMeta.vue`, two to `components/article/ArticleArchiveRail.vue`); until those components exist, the only permitted red is five `missing approved article region marker` failures with reason `missing implementation`. After the move, old page-location assertions are removed one-for-one; semantic assertion count remains five. The shared main-shell, balanced loading-grid, inline loading skeleton, and `articleLoadingSlotCount` assertions stay in `pages/articles/index.vue` with count one each.
- Relocation-phase expected red: once the five named regions leave `pages/articles/index.vue`, the only permitted additional red is its five former marker messages, each with reason `migration`; the next edit retargets those five assertions to the already-green component destinations without changing their count or text.
- The relocation probe also exposed two inherited page-only assertions (`<h1` exactly once and `<TerraBreadcrumb`), both now in `ArticleFeatureMeta.vue`. They are added to the same destination assertion set and the page's semantic scan is retargeted to that component; the observed three messages are classified `migration`, not a loss of heading or navigation semantics. No other red was observed.
- The Articles slice full-gate run exposed one omitted contract owner: `check:user-module` still required the relocated article link/title/summary, preview cover/fallback, and `loading="lazy"` literals in `pages/articles/index.vue`. The deterministic red is recorded before changing the contract. Root cause: the script has two page-only article-list contracts and no explicit route-component migration branch, unlike `check-public-pages`. Resolution is limited to exact destination assertions in `ArticleFeatureMeta.vue` and `ArticleArchiveRail.vue`, while the data fetch/pagination requirements remain page-local; assertion count must increase and the forbidden literal checks remain intact.

## Articles slice

- The v22 route now requests 12 live rows and projects unfiltered page one into exactly one featured story, five reading-stack entries, and six archive rows. Keyword queries and fewer than six rows render archive-only; the 6–11 range leaves only its true remainder in the archive. Topic aggregation remains the explicit `资料整理中` compact state, and current-page ranking is labelled `本页阅读数据`. See git for code-level diff details.
- Ratchet result: `check-public-pages` retains the five approved named-region assertions and adds exact destinations plus the relocated heading/breadcrumb semantics. The unplanned `check:user-module` migration moves nine former page-only UI requirements into twelve exact component requirements: article route assertion count rises from 27 to 30; fetch, pagination, error, loading, preview resolver, author helper, and pagination controls remain page-local. No regex was widened and forbidden-placeholder checks remain in place.
- Validation: `node --test tests/unit/articleArchive.test.mjs` 5/5; `pnpm run check:public-pages`; `pnpm run check:front-layout-layering`; `pnpm run check:user-module`; and full `pnpm run check` all exit 0. `check:visual-blocked-source` exits 0. `check:loading-skeleton` remains exit 1 with exactly the Task 0 baseline's eight failures in `pages/armor-sets/[id].vue`, with no new item/NPC/article marker.
- Runtime evidence under `http://localhost:5176`: `audit-shoot` recorded status 200, no captured console/request failure, and no horizontal overflow for `/articles` at 1440×1000, existing 375×812, and approved 390×844. Screenshot SHA-256: `articles--desktop.png` `691d68125b42c29459a4d330c73f4f1bf674dac6d1853dd67d3f8a4c4fb6310d`; `articles--desktop-full.png` `8d3a438929248c9642ee5c61d3abe3a7ae468f2c3b28e7ba89b38afbfe53ce74`; `articles--mobile-existing.png` `3c911ac13ac4fdf64167eae0afb17adaf1b8668af8d6b190a1fc49c181bc491c`; `articles--mobile-existing-full.png` `ffde2b5003a10c727d585d735c774f7d83dd43e2661b9dd1cf254140e070fa46`; `articles--mobile-approved.png` `7292b19c4488d342acd0c33114efc57c66e1a71510f7fe560465e93c57cdbebe`; `articles--mobile-approved-full.png` `8d422f1bc55f0b85ca5481fe04fbdb494e6fa68ddc166b75662d31bb4101a132`.
- The three server-dependent cross-route gates remain deferred to Task 4 by plan: their known same-server baseline failures are unrelated to this route. Next serialized owner is the NPC slice.

## NPC contract ratchet

- The first NPC utility-extraction probe intentionally runs `check:public-pages` red. Its exact six-message whitelist is: missing `特殊条件` from the aggregate-data marker list, and missing `shopGroupKey(entry)`, `常驻出售`, `阶段出售`, `地点出售`, and `解锁出售` from the shop-band marker list. Reason: migration of the pure, player-safe grouping semantics into `utils/npcShopBands.ts`; no template or fallback marker has moved yet. The next contract edit adds those six exact utility destinations before any component relocation.
- Component-relocation probe: `check:detail-layout` reports its page-only compact-grid count falling from five to three; `check:public-pages` reports the page-only shop summary, price-image, group-loop, and visible/remainder markers. Those six contract reds are migration-only and must be retargeted one-for-one to `NpcShopBands.vue` or `detail-pages-redesign.css`. The same probe also reports generic player-copy scanner matches for `Internal` and `fallback`: both originate from a parent-template inline key and prop binding, not rendered data. Their corrective action is limited to a page-script `shopEntryKey` binding and neutral prop naming; it must not weaken the scanner.

## NPC slice

- `utils/npcShopBands.ts` now owns the safe condition label/summary, ordered band projection, selected-band filtering, and archive-module decision. `DetailNpcShopBands.vue` renders the capability-first condition bands; merchant keeps residence support and traveler shows `当前可用商店资料`, never a complete stock-pool claim. `detailPagePresentation.ts` and its superseded test were removed after both consumers migrated. See git for code-level diff details.
- Ratchet result: six utility markers, compact relation-grid counting, shop toolbar, visible/remainder lists, price-image safety, and component CSS all have exact new destinations. The two template scanner reds were fixed by moving the stable key to script and using a neutral component prop; scanner rules were not changed. A stale dev server initially did not index the new directory-prefixed component; the authoritative Nuxt name is `DetailNpcShopBands`, and a local-stack restart confirmed it resolves without hydration mismatch.
- Validation: `node --test tests/unit/npcShopBands.test.mjs` 4/4; `pnpm exec nuxt typecheck`; detail/public-page/preview/breakpoint contracts; and full `pnpm run check` exit 0. `check:visual-blocked-source` exits 0. `check:loading-skeleton` remains exactly the eight known armor-set failures. Fresh audit is 200/no overflow/no captured console or request failure for merchant and traveler at 1440×1000 and 390×844. Screenshot SHA-256: merchant desktop `0f85ce488f33d42e1ef4292ced2953168043697b89aabfe4168aa49ad86e4058`, desktop-full `c46110da2070311a68daa5cd856516b33a817631a71aa219da27f9a86958eebf`, mobile `ef35081be7321b2ee91692e68b9df6936d69432fb4fb7e1c9dd0fd0747708a9b`, mobile-full `e597b57ce5a6aeed652bb2856e3e407404cd600f13302c236c1b2b13976e7cb6`; traveler desktop `0439217581428c2b98ec8e3bb7eb7ce1e3c57d680b528644a16664313fcf8546`, desktop-full `eb776107a7a929d89809fa2d19f0c39856e11cfc41136c442ec4a5169ae1f714`, mobile `e73f66d03ca3dfe174c2bfcd815e5ee49f6b30387adf3ae8f71829b11c16e114`, mobile-full `c2cb97705496ea6c0956dd5e99910e6070fba1578859d55251dd53610c5c25bd`.

## Item contract ratchet

- The Item slice has started from the Task 0 WIP anchor. The `publicApiFetcher` regression remains green before item recomposition (`2/2`), and the new `itemRecipeHierarchy` projection was red for the missing module then green (`3/3`). It accepts only the existing `buildCraftingRecipeModel` output, retaining that helper's variant order, quantities, stations, alternatives, fallback assets, and cycle protections.
- Expected-red whitelist for the first hierarchy relocation: `check:detail-layout` has exactly one failure for the old page-file `item-recipe-summary-module` class list; reason: the approved hierarchy class is added to the compatible module wrapper. `check:public-pages` has exactly one failure for page-local `制作路线`; reason: that reader-facing region migrates to `components/detail/ItemRecipeHierarchy.vue`. `check:preview-images` stays green. `check:loading-skeleton` remains the eight Task 0 baseline failures in `pages/armor-sets/[id].vue` only; it is neither a new Item failure nor in scope to repair.
- Green-step disposition: restore the item detail-layout assertions to exact final class lists (no optional-class regex), relocate the one `制作路线` assertion to the new component, and add exact assertions for `buildCraftingRecipeModel` → `buildItemRecipeHierarchy`, L3/L2/L1/OUT bands, quantities, stations, alternatives, and compact empty state. No wildcard regex or assertion-count reduction is allowed.

## Item slice

- `/items/757` now uses `DetailItemRecipeHierarchy` as the first content region: the existing crafting model is projected as L3 → L2 → L1 → output bands, with live quantities, alternate materials, station chips, a compact no-data state, and an unchanged expandable `RecipeSummaryCard` compatibility summary. The right rail now begins with the real stat rows as `核心属性`, followed by the live coverage facts; it no longer substitutes the approved fact rail with a single generic overview card. See git for code-level diff details.
- Ratchet result: the two expected reds were eliminated by exact relocation. `check-detail-layout-contract.mjs` now pins the final item shell, grid, hierarchy module, and coverage-rail class lists; it does not retain an optional-class regex for the Item route. `check-public-pages.mjs` keeps the `制作路线` promise at the hierarchy component and adds exact page/component/utility markers for shared-model adaptation, four stage bands, quantities, alternatives, stations, and explicit empty state. The three stage constants are asserted in the projection utility rather than falsely requiring the consumer component to duplicate them.
- Validation: `node --test tests/unit/publicApiFetcher.test.mjs` 2/2 before the slice; `node --test tests/unit/itemRecipeHierarchy.test.mjs` 3/3; targeted detail/public-page/preview/breakpoint/type checks passed; full `pnpm run check` passed. `check:visual-blocked-source` passed. `check:loading-skeleton` remains exactly the Task 0 baseline's eight failures in `pages/armor-sets/[id].vue`, with no Item/NPC/article violation.
- Runtime evidence under `http://localhost:5176`: `audit-shoot` at 1440×1000 and 390×844 returned `200`, `hasHScroll: false`, and no console/request failure. Current screenshot SHA-256: `item-terra-blade--desktop.png` `31ad0c42db94cf97e83d0d6f3020d33a4517d7da618f75050b7c56d4bc65d829`; `item-terra-blade--desktop-full.png` `d78cbc1584bd9b9b15b51e052eb596330014032f76443114733bcc3c6c920846`; `item-terra-blade--mobile.png` `cf6fd24626532a93b50bb89c533a6ed84a06ef79135a42ac8c6665ff541d00c8`; `item-terra-blade--mobile-full.png` `7d3daad9a33fcc3703147b0e6784b3936466334037d894e99cf4c0eca28de4e8`.

## Item direct-transplant reset

- User acceptance supersedes the preceding Item visual result: the prior hierarchy projection and safety work remain reusable, but the loaded body is rebuilt from the approved DOM instead of receiving another styling pass on the legacy detail skeleton. This reset does not reopen the completed Articles or NPC slices.
- Confirmed body boundary: production global navigation and `TerraBreadcrumb` remain; mock top bar, sample strip, mock breadcrumb, and design note stay out. The existing production container width remains, while the approved hero proportions, two-column body, primary/secondary card hierarchy, sticky tool rail, chain bands, tally, usage, and compact facts become the live structure.
- Confirmed compatibility boundary: preserve favorite/history/SEO/loading/error/image fallback, SSR relation capture, price-coin images, source safety filtering, recipe-usage expansion, and all existing data modules. No API/database/crawler/report work and no sample-specific hard-coded content. Missing core fields use compact `资料整理中`; absent optional modules are hidden.
- Live adaptation probes are fixed at `/items/757` (泰拉刃), `/items/22` (铁锭), and `/items/95` (燧发枪). Read-only API check confirms relation counts: 757 has 0 sources / 1 usage; 22 has 27 sources / 17 usages; 95 has 3 sources / 1 usage. These three routes cover crafting-first, crafting-plus-source, and shop-source shapes without mock data.
- Owned write paths for this reset: `pages/items/[id].vue`, `components/detail/ItemRecipeHierarchy.vue`, `utils/itemRecipeHierarchy.ts` only if the approved band/tally projection needs additional fields, `tests/unit/itemRecipeHierarchy.test.mjs`, `assets/css/domains/detail-pages-redesign.css`, the Item sections of `check-detail-layout-contract.mjs` and `check-public-pages.mjs`, this entry, and the active fidelity plan. `useDetailLayout.ts`, `usePublicApi.ts`, `usePublicItemDetail.ts`, `RecipeSummaryCard.vue`, unapproved detail HTML, unrelated devlogs, and `reports/` remain untouched.
- Expected-red whitelist for the direct-structure contract addition: exactly eleven page markers (`item-approved-body`, `item-approved-hero`, `item-approved-plinth`, `item-approved-ident`, `item-approved-metrics`, `item-approved-layout`, `item-approved-column`, `item-approved-card`, `item-approved-rail`, `item-approved-anchors`, `item-approved-related`) and six hierarchy-component markers (`chain`, `band`, `band-body`, `nodes`, `tally`, `tally-grid`) are initially missing. Their reason is `missing implementation`. Existing safety and data assertions must stay green; any additional red stops implementation.
- Structure-ratchet baseline before this addition: zero exact direct-transplant markers and seventeen proposed destinations. The existing seven Item detail-layout markers plus public data/safety assertions remain in force until their exact destination is implemented; later relocation may change file ownership but cannot broaden a regex or reduce the total semantic assertion count.
- Observed red matches the whitelist exactly: `check:detail-layout` reports only the eleven page markers and `check:public-pages` reports only the six hierarchy-component markers. No inherited data, safety, preview-image, SSR, or interaction assertion failed at this phase.
- Fresh 1440×1000 geometry comparison after the direct transplant found three production-only cascade collisions, not missing approved DOM: global `.hero` leaves the Item hero at the homepage `790px` minimum instead of the approved `282px` intrinsic height; the retained `tp-detail-module` compatibility class turns approved cards into a gapped grid; global `.node-meta` adds an 18px margin, 14px padding, and divider to every crafting subtitle. The next detail-layout red whitelist is exactly three new CSS destination assertions under `.item-approved-body` for hero intrinsic height, inert card compatibility layout, and node metadata reset. Reason: `missing implementation`; assertion count increases by three and no existing assertion moves or widens.
- That three-assertion probe went red exactly as listed and is green after scoped resets. Fresh geometry is hero `307px` and first crafting node `48px` (previously `790px` / `81px`). The remaining approved-body cascade probe adds four exact assertions: suppress the homepage hero `::before`, restore the plinth/preview intrinsic sprite sizing at desktop and mobile, keep the real favorite target at the shared touch size, and neutralize the legacy coin-pill/icon sizing inside approved price cells. Expected red is exactly four `missing implementation` messages; no data or interaction assertion moves.
- The four-assertion probe also went red exactly and returned green. Computed-style inspection then showed two higher-specificity remnants: homepage `grid-template-rows: minmax(0, 1fr) auto` still creates an empty hero row, and `public-layout.css` keeps a more-specific `!important` full-frame sprite rule. The final cascade whitelist is exactly two new assertions for a one-row approved hero and the 108px approved desktop sprite destination; both are `missing implementation`, with no existing marker weakened.
- The next Item projection red is bounded to new unit expectations for role-based stages: direct inputs remain L1, deepest craftable descendants become L2, their real leaf inputs become L3, every normalized `childRecipes` option participates in an explicit one-to-one fork, and procurement retains deduplicated leaves from every live branch without item-id/name special cases. The existing depth-only helper is expected to fail those assertions; `publicApiFetcher` and all page safety contracts must remain green.
- The next Item interaction ratchet adds one exact `check-public-pages.mjs` destination requirement for a single usage row to select `NuxtLink` only when `usage.href` exists and otherwise render a non-link element. Its only expected red is the current `:to="usage.href || route.path"` self-link fallback; no favorite, expansion, SSR, preview-image, or source assertion may move or widen.
- Observed red matches that whitelist: the focused hierarchy suite has exactly three new behavior failures (role stages, explicit normalized child-recipe fork, and all-branch leaf procurement), while its variant/empty cases and `publicApiFetcher` 2/2 tripwire remain green. `check:public-pages` has exactly the one inert-or-real usage destination failure; no unrelated route or safety message appeared.
- The role projection and inert-usage green step now passes the combined focused suite 7/7, `check:public-pages`, and Nuxt typecheck. Fresh production/reference geometry shows the hero is already within 4px (`286px` versus `282px`); remaining primary-body inflation is localized to an outer legacy compatibility wrapper (17px inset), station copy inside node metadata (L2/L1 wrapping), and a one-row usage summary that is only operationally needed when six-row overflow exists.
- The next bounded red adds: one utility expectation that projected node metadata is derived only from normalized material titles/quantities, not appended station copy; exact detail-layout destinations for the compatibility classes on the approved primary card plus a forbidden nested wrapper; and one public-page marker requiring `recipe-usage-summary` only under `hasRecipeUsageOverflow`. Expected failures are those destinations only. The compatibility classes and `RecipeSummaryCard` remain present, and the six-row expand/collapse behavior is unchanged.
- That red/green cycle matched exactly: the metadata test is green, public pages are green, and detail-layout produced only the old-wrapper migration red before its exact binding moved to the primary card; typecheck remains green. Fresh geometry is primary x/width `32/1058`, L1 `111px` versus approved `108px`, and one-row usage `167px` versus `161px`. The remaining L2 wrap is explained by implicit `x1` pills and repeated per-ingredient quantities, not missing data or CSS width.
- The next focused red adds exact component markers that L2 suppresses only implicit `x1` quantity pills while L1/L3/OUT retain quantities, plus a utility assertion that a frontier recipe with three or more ingredients uses their normalized names without repeating quantities. No material, route, station, or branch is removed; this is a compact-display projection only.
- That compact projection is green (hierarchy 6/6, public-pages, typecheck). Runtime now places the fork, grass blade, volcano, and Excalibur on the first L2 row; only Chlorophyte Bar wraps because the exact live names exceed the 900px node track by about 52px. The next detail-layout red is one exact CSS destination limiting only direct L2 intermediate nodes to `144px`; nested fork nodes and every other stage remain untouched.
- The `144px` hypothesis was falsified by runtime geometry: the direct-node row still totals `911px` inside an actual `899px` content track, so Chlorophyte Bar remains on a second flex row and L2 grows to `201px`. The corrected single-variable hypothesis is `136px` for the two bounded direct nodes (`895px` total); the exact CSS contract changes red before the value changes, then geometry must prove a single row. No additional selector or content change is authorized in this correction.
- The corrected `136px` value is green and proven: all five direct L2 slots occupy one row, L2 is `129px` versus approved `118px`, and desktop has no overflow. Mobile comparison then exposed a separate pre-existing `max-width: 640px` divergence from the approved CSS: it changes band tracks to one column, turns nodes/forks into forced grids, removes the flow offset, sets every node to full width, and collapses the tally to one column. The next detail-layout red consists exactly of those five forbidden mobile overrides plus the tally override; implementation removes only those declarations so the approved base two-track/flex layout and the existing 900px two-column tally inherit at 390px.
- The mobile chain/tally reset is green and preserves the approved left-track/flex composition at 390px with no horizontal overflow. Variant geometry then isolated a separate legacy scoped-style collision in `/items/22`: at 1440px the live source table computes to two `506px` columns instead of the approved three-column grid, and `detail-relation-meta` forces each rate into a second row, inflating every source card from the intended `92px` minimum to about `119px`. The next exact detail-layout red whitelist has three `missing implementation` messages: an Item-specific three-column source-table destination with enough specificity to beat the scoped legacy rule, a third-track rate destination, and a mobile prohibition against the two-column/rate-below-copy override. This cycle changes CSS only; all 27 live source records, copy, links, images, values, and ordering remain unchanged.
- The three source-density contract reds returned green. Runtime now computes three `336px` desktop columns and `92px` cards; `/items/22` falls from `4715px` to `3726px` desktop and from `8271px` to `7404px` mobile with zero horizontal overflow and no hidden record. The preservation probe then found a separate interaction regression in both `/items/757` and `/items/95`: the string dynamic component renders literal `<nuxtlink to="...">` elements with no `href` and `tabIndex=-1`. The established working repository pattern is `ArmorSetCard.vue`, which imports `NuxtLink` from `#components` and passes the component object to `:is`. The next `check:public-pages` red whitelist is exactly one single-usage destination failure; implementation changes only that dynamic shell and keeps the inert `span` fallback.

## Item final acceptance evidence

- The single-usage destination red returned green. Runtime now renders `/items/757` usage as focusable `<a href="/items/4956">` and `/items/95` as `<a href="/items/127">`; the no-destination branch remains an inert `span`. `/items/22` still previews six usages, expands to all 17, and returns to six on collapse. The favorite control remains a real enabled `button`, reports `aria-pressed`, and measures `198x44px` in the desktop probe.
- Three-theme data-shape matrix: all 18 combinations of `757 / 22 / 95`, `dark / morning-paper / warm-slate`, and `1440x1000 / 390x844` returned `200`, the requested theme key, zero console/request failures, zero failed images, and zero horizontal overflow. The first all-in-one run correctly exposed the backend's fixed public-read limit (`120` requests per `60s`): each Item SSR consumes nine public endpoints, so the final theme batch crossed the limit and degraded to Item-not-found. No product code was changed for this environment boundary; the invalid screenshots were discarded and the matrix was rerun in sub-120-request windows with fresh contexts.
- Full-page matrix SHA-256, dark: Terra Blade desktop `261b91acf74d667ee9307512aa8d96f725611e82a57d9d45fc705b3555c6cce2`, mobile `74c06720a0b24014a3c3fe6a94e6a846e4a8052c9251da712f02b5b366ed9122`; Iron Bar desktop `f88767e29a5da3f856857627995ead699a3ff91a1deac0b9fc809b7d6aa67fe0`, mobile `3317d4cfb57039e96481a902f8e7fdaf887b70a6f66a16114c5a3a06776e7813`; Flintlock Pistol desktop `e81a37e0ea56800ddd57dfb7bdb36f168d68780761e85f5386d8e079eb223c32`, mobile `7f7c106eca5f3ab3a8791ad88f4fed0b41224bf3155e431d5fe0cd7cff46466b`.
- Full-page matrix SHA-256, morning-paper: Terra Blade desktop `4bda7d1aac7a55f8d11ee7123167e1b24bb3e9a47c805f5512c103eea968b562`, mobile `efafaf03c021ac8094deea532c6703aa214606da51bccc3eb7c81aaa2cf1afb6`; Iron Bar desktop `bd45612df106e278d9e884990e2e692d5725efa42894db7167eef9d81e48f18b`, mobile `f5c1a99268931600854ef39e86a04ff5bf696c7db79990974494c6e4154da25a`; Flintlock Pistol desktop `76fb8b0f1bb92b5922feac1a19bef75bccc804ec6649693d31b13542f1b90637`, mobile `6cef823d534ecd307ed9405a27c29f9ec79a8aca420984c3ed4e86dc62eab3bd`.
- Full-page matrix SHA-256, warm-slate: Terra Blade desktop `d9e55432be7f710402248195378f0ab23ddacc0300b3e694a0b09975c99fc4ea`, mobile `4e1b07f36bff22c69b5134467275b5335873026e479d147ff90527293a9969b4`; Iron Bar desktop `bd956e5fc42aecac448b1aa048f16c9137f569a3a34de555c51f236761a7706b`, mobile `067aecebfb6a7224e7802adac5f7759f305315c918770a41ca2160b6937fdfe7`; Flintlock Pistol desktop `608d2468536d8f9dad0b630fee66f7afa250fee50fb903704c6447d6bc77847f`, mobile `c89b5193b83747dcec5434be446b515eeba035a2aaa037a51322f88ddb8e276a`.
- Fresh-stack cold audit under `http://localhost:5176` is also `200` with no overflow or captured console/request failure for all three routes and both probes. Full-page SHA-256: Terra Blade desktop `d73d9c42167a9234fe94ae12ab0c6d269135f413ef651ba1908cc553ac7c9a86`, mobile `fc5b0e119566428cf39e74679a0b07f9d0552f89f979d31c2a9855687004b481`; Iron Bar desktop `f88767e29a5da3f856857627995ead699a3ff91a1deac0b9fc809b7d6aa67fe0`, mobile `6317cf6eac3f62bd37696ec1aaef4ccf618c5f2aa559cede25d04a6f425d65a8`; Flintlock Pistol desktop `12c27777e5427ace434816c357936931f6c4bf3e9a9b93e2de7a742dcafbb176`, mobile `3c03d1685ef9e26250c76ae47eb7d4646f3ae79d9af5f5d42c569358fb0fad8e`.
- Final validation: focused Item/API tests `8/8`; full `pnpm run check` exit `0`; `check:visual-blocked-source` exit `0`; `check:loading-skeleton` remains exactly the eight Task 0 armor-set failures and has no target-route failure; `git diff --check` exit `0`. After a tracked local-stack cold restart, the three runtime gates reproduce only their Task 0 baselines: light-theme's unrelated homepage/article/legacy-slug contrast findings, typography-spacing's `/search ready` timeout, and crafting-wiki-structure's independent `/crafting` findings.
- Residual visual difference is data-bound and intentional under the user's preservation rule: approved Iron Bar groups representative evidence into six cards, while production keeps all 27 existing live source records; Flintlock keeps all three live shop evidence rows instead of merging them into one fact. CSS now uses the approved compact geometry, but changing counts, grouping semantics, or adding a new source expander remains out of scope. Technical acceptance is complete; the entry stays `active`, uncommitted, pending user visual acceptance.

## Item hero material polish

- User visual review accepts the rebuilt Item framework but rejects the remaining surface-detail gap, naming `hero item-approved-hero` as the clearest mismatch. This round is styling-only: no information architecture, data projection, API, interaction, count, or route behavior changes.
- Computed-style comparison at 1440x1000 localizes the mismatch. The production hero has the approved dimensions (`286px` versus reference `282px`) but replaces the reference's final green 125-degree surface with one mixed surface color; plinth and rarity markers use global gold instead of the live rarity semantic; metrics and prices share one surface instead of raised/sunken roles; the page-scoped favorite rule overrides the approved primary button into a pill outline.
- A single-variable runtime injection of only the reference hero surface confirms the diagnosis: structure and geometry remain stable while the perceived material moves toward the approved frame. A second temporary injection of rarity, metric/price, and favorite treatments confirms those are independent cascade/material differences rather than missing DOM. Evidence is ignored under `front-nuxt/tmp/shots/2026-07-29-public-pages/item-polish/`.
- Contract ratchet for this round adds, before implementation, exactly ten expected failures: one page marker for `:data-item-rarity="itemRarity"`; one exact live-label mapping for the three approved sample rarities; one semantic hero/rareness token set; one hero consumer for rarity tint, green surface, and compact shadow; one rarity-aware plinth frame; one rarity-aware label; one exact primary favorite override that keeps the 44px target; one raised metric surface; one sunken price surface; and one light-theme flattening override. Every failure reason is `missing implementation`; no existing assertion moves, widens, or disappears.
- Approved rarity adaptation is label-based rather than item-ID-based. A source recheck found four rarity values across the six approved Item samples: `浅红色`, `蓝色`, `绿色`, and the Anklet of the Wind's `橙色` (`/items/212`, approved `#ffad5d`). The first three are already green; the next single expected red extends the same exact mapping contract to `橙色` before its CSS rule is added. All other live labels retain the existing accent fallback until a separately approved full rarity-palette task. Light themes mix the same data signal into their existing flat surface tokens instead of importing the dark glow.
- The first hero green step is now contract-green and computed at 1440x1000. Production/reference hero geometry is `1376x286` / `1400x282` under the accepted container difference; both render the same five background layers, `44px` grid, rarity radial field, 125-degree green surface, compact shadow, raised metric and sunken price roles. Remaining material drift is below the hero structure: production still applies the global bright gold border and `0 24px 70px` surface shadow to primary/rail cards, while the approved dense body uses separate primary, secondary, sunken, and object levels.
- The next material-depth ratchet adds exactly ten `missing implementation` destinations in `check-detail-layout-contract.mjs`: dark semantic tokens plus light-theme flattening; primary card; secondary card; rail card; sunken band/fact surfaces; raised node surface; stat-list well; rarity-colored value; and the selected anchor inset marker while retaining the shared 44px target. Assertion count for this polish subsection rises from ten to twenty; no existing assertion moves, widens, or disappears. This cycle owns only `assets/css/domains/detail-pages-redesign.css`, the Item portion of the detail-layout contract, and this active entry.
- The material-depth green step passes all twenty polish assertions. The hero now uses the approved five-layer composition, 125-degree green surface, live rarity field/frame/label, raised metrics, sunken prices, and the three-theme primary favorite treatment; primary, secondary, rail, well, sunken, and object surfaces below it now have separate depth roles. The orange rarity mapping for `/items/212` is also contract-covered. See git for code-level diff details.
- Fresh verification after the final CSS is green: `pnpm run check` exit `0`; focused Item/API tests `8/8`; `check:visual-blocked-source` exit `0`; `git diff --check` exit `0`. `check:loading-skeleton` remains exactly the eight recorded `pages/armor-sets/[id].vue` baseline failures and has no Item/NPC/Articles finding. A focused dark probe reports `/items/757` as `200`, zero console/request errors, zero failed images, no horizontal overflow at `1440x1000` and `390x844`; its favorite is an enabled `198x44px` button with `aria-pressed` and a visible `3px` focus outline.
- The remaining light-data matrix covers `/items/22` and `/items/95` under `morning-paper` and `warm-slate` at both probe sizes: all eight cases are `200`, use the requested theme, and have zero console/request errors, failed images, or horizontal overflow. Iron Bar preserves 27 source rows and six usage-preview rows in every case; Flintlock preserves three merchant rows. Screenshot SHA-256: Terra Blade dark desktop `12da55dcef0f71a2f44cf5bbf0461be8fef3b7678627e42b7f5368bd0c3f62c1`, mobile `f17309fb7a675b16bf423970260e5e9003c14047bfcbc965a129a05c6e017829`; Iron Bar morning desktop/mobile `08cb3ea073a594e97ed1406cd7b41ee92136f56627a58f1a473dacdba1f3c6ea` / `607d9444f82c7e1070d018664d530c0ef01d04c49a8368ad1cd9040cad9e056e`, warm-slate desktop/mobile `5542c5d859b8088b239252c8957e22e334744a111a84a5cdbe8a6d72ed2b7ff6` / `dd984b9e9db53a4ee73a954c73196686f13f939e151ac2da3d8cf6945939b3be`; Flintlock morning desktop/mobile `7d22e56584e3bc635c54f6a4c30cae7da7b4fdbc92188fd729148b34e7fa9d4b` / `3f02721e80df2f65b896014139a15f382e513d45a6b0eed31d76917b150a9ecc`, warm-slate desktop/mobile `9c8fd1e9f6f35dc87bccdc83323ea72140ee5777d8203898f5570b30989f4fc9` / `2401b4a52bfc91eba51ec0711e5e530b2527cd897c18fe0d813630bc1471325f`. Evidence remains ignored under `front-nuxt/tmp/shots/2026-07-29-public-pages/item-polish/`; the entry stays `active` pending user visual acceptance.

## Item coverage rail restoration

- User review found that the approved `资料完整度` rail card was omitted during the direct body transplant. Root cause: the ten-row legacy `itemCoverageRows` derivation survived, but the approved rail markup and ring styles were not migrated into `item-approved-body`; the compatibility wrapper therefore remained without visible coverage content.
- Approved correction keeps the reference card between page anchors and related entries, but does not copy sample percentages. Eight fixed live capabilities own the denominator: foundation/classification, price, crafting, usage, image, description/tooltip, effects/equipment, and source records. Available count and rounded percentage derive only from those predicates; the four reference summary lines expose complete, partial, and unavailable states without changing API or data semantics.
- Contract red whitelist adds exactly six `missing implementation` findings before production edits: approved rail order; live ring/tail/group binding; exact eight live capability predicates; dynamic count/percentage formula; compact coverage layout; and token-driven ring/list states. Assertion count only increases; no existing Item, data-safety, fallback, interaction, or material assertion moves or widens.
- The corrected red run matches that whitelist exactly: four `check:detail-layout` destinations and two Item-only `check:public-pages` data assertions fail. An initial contract-placement mistake briefly applied the two data assertions to `pages/index.vue`; it was corrected before any production edit, and the repeated red has no out-of-scope route or retained-contract failure.
- The minimal green restores the approved card between anchors and related entries. It replaces the unused ten-row legacy overview with exactly eight live predicates, derives `available / 8` and the rounded ring percentage, and renders the approved four summary lines with complete, partial, and unavailable states. No item ID, sample percentage, API field, or fallback copy determines the score. See git for code-level diff details.
- Runtime values prove data adaptation: Terra Blade is `5 / 8` and `63%` in all three themes at `1440x1000` and `390x844`; Iron Bar is `7 / 8` and `88%`; Flintlock Pistol is `5 / 8` and `63%` from its live merchant-source shape. Every probe is `200`, places coverage before related entries, has four labelled summary rows, zero console/request failures, zero failed images, and no horizontal overflow. The card is a stable `300x146px` desktop tool and `358x146px` at the approved mobile probe.
- Coverage screenshot SHA-256: Terra Blade dark desktop/mobile `2f9a7efd353190d0f8c45400620ccf1fdc1ef00d7af8adfcd7a0262ef0a7e49a` / `8bbf75ff1d4001aa693af328bd556143a85a095b0d6dc00cd8ac47e9f759d100`; morning-paper desktop/mobile `b5372f7a8af636397a449b723f0db462e71cfdf48847b5196f461bd17091960d` / `4d5c6365a0e2497c2485430b4d93f65380118831a2c41604e7c4445f4ccd76bc`; warm-slate desktop/mobile `46dcb36d6ce0b392c06c5c6b190d2e7a2fac0b374e93c49bd41196599cb013b4` / `84f46e2a4de77b37b858e0f05d7a1c56903c4f4eda626ce6b2057e2802270e21`; Iron Bar dark desktop `322b02daafd2af26e6644f0ebab1693f6243a6c0ffdfef866b2f29049819761a`; Flintlock dark desktop `2c87dc29e4203c913fe3b65af5f87fa74584d1156a2a4faf5461f59afa782f2b`. Evidence remains ignored under `front-nuxt/tmp/shots/2026-07-29-public-pages/item-polish/coverage-rail/`.
- Fresh final gates after the restoration: `pnpm run check` exit `0`; focused Item/API tests `8/8`; `check:visual-blocked-source` exit `0`; `git diff --check` exit `0`. `check:loading-skeleton` remains exactly the eight recorded armor-set baseline failures with no Item/NPC/Articles finding. The entry remains `active` and uncommitted for user visual acceptance.

## Item dark-ground and hero-data refinement

- User review identifies two remaining approved-Hero gaps: the dark route lacks the reference page-ground depth, and the identity/metric presentation omits visible live details. Computed-style evidence locates the background mismatch at the full-page `.screen.detail-screen`, not `.tp-detail-shell`: the approved `.shell` is transparent and width-only, while its `body` supplies a fixed 125-degree forest field plus the 40px grid. The production route currently supplies a flat `--index-bg` plus a 420px dark veil. The correction therefore adds an Item-only route archetype class and must not place paint on the shared layout shell or global `body`.
- Live data inspection for `/api/public/items/757`, `/22`, `/95`, and `/212` confirms the public DTO already supplies `internalName`, English `name`, localized `nameZh`, classification, stackability, damage, defense, knockback, use time, dimensions, and prices. No API or data work is authorized. The current page loses the English value because it reads only absent `nameEn`, and it never binds `internalName`; direct values remain authoritative. The `/20` knockback scale and `60 / useTime` rate are explicitly presentation derivations, shown with a scale suffix and approximate marker rather than represented as API fields.
- Contract red whitelist for this cycle is exactly eight `missing implementation` findings. `check:detail-layout` owns four: the Item-only full-screen class, the dark token/grid ground, fixed background attachment, and metric scale/meter styling. `check:public-pages` owns four: exact safe internal-code mapping/binding, English-name fallback from the distinct live `name`, knockback/use-time presentation derivation, and stackability copy. The previous blanket internal-name prohibition is replaced by one exact allowed projection plus a residual-content prohibition, so it cannot authorize internal names as title/category fallbacks. No existing safety, fallback, interaction, recipe, source, theme, or coverage assertion may move, widen, or disappear.
- The contract-first run returns exactly those eight findings, split `4 + 4`, with no failure outside the whitelist. The internal-name rule now removes only the exact approved safe projection before checking residual page text, preserving the prohibition everywhere else. Production implementation may proceed on `pages/items/[id].vue` and `assets/css/domains/detail-pages-redesign.css`; no shared layout, theme source, API, or DTO file is opened for writes.
- Green-step self-review adds one bounded data-degradation red: the knockback `/20` presentation suffix must render only when the live knockback value exists, otherwise a missing value would misleadingly read `— / 20`. This changes no current sample output and adds no API or fallback data.
- The eight-item green step and the bounded degradation correction are contract-green. The route now exposes `detail-screen item-detail-approved-screen`; only `[data-theme="dark"]` replaces that full-screen ground, using the existing page, moss, accent, and 40px grid tokens with fixed attachment. The shared `.tp-detail-shell`, global `body`, `hifi-preview.css`, and both light-theme ground rules remain unchanged. Computed dark ground matches the reference's four layers, `auto / 40px / 40px / auto` sizing, and four fixed attachments; its gradient stops resolve to approximately `rgb(10,16,11) / rgb(24,35,21) / rgb(22,29,17)` versus reference `rgb(9,16,11) / rgb(23,36,20) / rgb(23,29,18)`.
- Hero identity and metrics now consume the live DTO without sample IDs: Terra Blade renders `TerraBlade`, `Terra Blade`, `不可堆叠`, `85`, `7 / 20`, the 35% semantic meter, `18`, `≈ 3.3 次 / 秒`, and non-armor defense copy. Iron Bar adapts to `IronBar`, `Iron Bar`, `最大堆叠 9999`, and its recipe/source/usage counts; Flintlock Pistol adapts to `FlintlockPistol`, `Flintlock Pistol`, `不可堆叠`, `1 / 20`, and `≈ 3.8 次 / 秒`. Missing knockback suppresses both the meter and scale suffix.
- Runtime matrix for Terra Blade covers `dark / morning-paper / warm-slate` at `1440x1000 / 390x844`: all six return `200`, apply the requested theme, show the exact identity/metric values, and have zero console errors, request failures, or horizontal overflow. Dark desktop/mobile screenshot SHA-256 is `111c798fa8684945d9da595f07d51294546add06ba86e4efb3dfce2ea846d3c6` / `0bce5dc0472ce11c0596152e16eedc59aa6efcba906b85f407de85593ca971ef`; morning-paper is `43e36ba587f0076f54cd8b6e4028748eb789ee16d10d39beacf068c93b30e052` / `915759d850e762f69fe578309d4b848eda18fe68f003d22e4de08ff60b35b26c`; warm-slate is `7d8ba2e03882dba5b620ce47a9abd8eda59c00bdc620583d32ae566a4096f4` / `e6af8bd9f482d10d12bc070e1a54449f5a059c00bdc620583d32ae566a4096f4`. Reference desktop SHA-256 is `8e4f17559c14bd470482e96c7eec23bc2380f90f6036106db3ad534f1a9debe9`; evidence remains ignored under `front-nuxt/tmp/shots/2026-07-29-public-pages/item-polish/dark-ground-hero/`.
- Fresh final validation after the degradation fix: `pnpm run check` exit `0`; focused Item/API tests `8/8`; `check:visual-blocked-source` exit `0`; `git diff --check` exit `0`. `check:loading-skeleton` remains exactly the eight recorded armor-set failures. Runtime `check:typography-spacing` still times out at the recorded unrelated `/search ready` baseline; `check:light-theme` preserves its recorded homepage/article/legacy-slug findings and reports no `/items/757` regression. The entry remains `active`, uncommitted, and served at `http://localhost:5176/items/757` for user visual acceptance.

## Item checkpoint and continuation

- User authorized a focused checkpoint commit for the current Item result and
  requested immediate continuation on the remaining approved NPC and Articles
  pages. This is not task closeout: the entry stays `active`, and no push is
  authorized.
- The continuation order is NPC first, then Articles. Each slice keeps the
  production navigation, breadcrumb, container proportion, live-data and
  interaction contracts, and three-theme behavior; API, database, crawler,
  unapproved design HTML, old devlogs, and `reports/` remain out of scope.
- The checkpoint includes the already completed shared projection, contract,
  audit harness, Item, NPC, and Articles foundation paths. The next fidelity
  edits must build on that checkpoint and record their own exact expected-red
  contract lists before changing production markup or CSS.

## NPC approved-body continuation

- The accepted Item checkpoint is commit `8d459fe1` (`feat(front): reproduce approved public page designs`). NPC fidelity continues on top of that commit; no push or task closeout is authorized.
- Live capability matrix for the four approved probes: `/npcs/17` Merchant has 31 shop rows, 7 preferences, no loot/buffs, and 3 Wiki asset kinds; `/npcs/22` Guide has 1 trusted loot row, 7 preferences, no shop/buffs, and 3 asset kinds; `/npcs/229` Pirate has 8 shop rows, 9 preferences, no loot/buffs, and 3 asset kinds; `/npcs/368` Traveling Merchant has 122 current shop rows, 1 trusted loot row, no preferences/buffs, and 3 asset kinds. Every live shop condition array is empty, so the approved condition/rarity/time groups are not copied: the existing safe projection truthfully degrades them to `常驻出售`, and traveler stock remains `当前可用商店资料`.
- Final body mapping is capability-driven rather than sample-ID-driven. The common approved Hero consumes live portrait/assets, localized and English identity, category, behavior notes, four decisive stats, and capability counts. The main column selects arrival first for a temporary merchant, shop first for a shop NPC, and role behavior first when no shop exists; supported shop, loot, preference, buff, and related-item regions retain their current links, fallbacks, filters, and expanders. The rail consumes live base stats, rendered module anchors, an eight-predicate completeness projection, and real NPC preference links only. Missing capabilities are omitted or marked as currently unrecorded; no design-sample timeline, shop condition, recommendation, or related NPC is fabricated.
- TDD expected red before utility implementation: two new `npcShopBands.test.mjs` behaviors fail only because `buildNpcCoverage` is absent. They require an eight-capability denominator with a rounded live percentage and four paired summary rows whose states are `complete`, `partial`, or `missing`; zero-count shop/loot/preference/buff inputs cannot be treated as complete.
- Contract expected-red whitelist before production markup/CSS: `check:detail-layout` may report only the exact missing destinations `npc-detail-approved-screen`, `npc-approved-body`, `npc-approved-hero`, `npc-approved-layout`, `npc-approved-card`, `npc-approved-rail`, `npc-approved-anchors`, `npc-approved-coverage`, `npc-approved-related`, the restored exact shell/grid/rail class lists, and four scoped CSS destinations for dark ground, final-cascade 280/1fr/286 Hero, 1fr/300px body, and 640px mobile recomposition. The approved file first declares a 198px portrait track in its shared palette layer, then its later `npc-item-family-v1` block overrides the actual desktop track to 280px; the contract pins that final computed result. `check:public-pages` may report only the missing live completeness binding/formula, exact approved region markers, the approved four-column `npc-shop-grid`, and the 15px coin-image destination. The last two replace the prior exact `auto-fill minmax(172px, 1fr)` and 24px style assertions one-for-one; this is a stricter approved-geometry migration, not a wildcard or assertion removal. Preview fallback, sanitized copy, shop grouping/filter/expansion, item links, structured coin labels, buff grouping, loading/error, and all non-NPC contracts must stay green. Any red outside this list stops the slice.
- Observed reds match that whitelist exactly. The focused utility suite has only the two expected missing-`buildNpcCoverage` failures while its prior 4/4 behaviors remain green. `check:detail-layout` reports exactly 14 approved body/class/CSS destinations, `check:public-pages` reports exactly 11 completeness/shop-geometry destinations, and `check:preview-images` remains green. Production implementation may proceed without changing the whitelist.
- Green-step narrow verification exposed five exact follow-up migrations rather than new product behavior. Nuxt typecheck reports four `boolean | undefined` reads because the fixed eight-capability list is inferred as an unchecked array; the already-green `buildNpcCoverage` behavior tests remain the tripwire while the implementation moves those paired reads to named booleans. The mobile rail declaration exists in a combined selector but the exact destination contract requires its own scoped rule. `check:public-pages` also still points its public-screen and busy-state literals at the pre-approved body classes, so those two assertions move exactly to `entity-screen npc-detail-approved-screen` and the final `npc-approved-shell` main marker. The remaining page findings are player-copy safety (`接口` / `聚合`) and the exact asset-loop destination. No regex is widened, no assertion is removed, and no API, count, grouping, fallback, or interaction behavior changes.
- Desktop and mobile side-by-side review of all four approved NPC samples found one remaining data-independent order drift: the approved Hero metric grid is `生命值 / 防御 / 伤害 / 击退抗性`, while the shared rail rows correctly retain `生命值 / 伤害 / 防御 / 击退抗性`. One new exact `check:public-pages` destination asserts a Hero-only ordered label projection before implementation; expected-red reason is `missing implementation`. The rail projection and every live value remain unchanged.
- The first 24-case theme/viewport probe found no status, theme, console, request, image, overflow, or Hero-order failure, but measured the existing shop remainder `<summary>` at `16px` high on Merchant and Traveling Merchant. One new scoped `check:detail-layout` assertion requires `44px` minimum height under `.npc-approved-body`; expected-red reason is `missing implementation`. The native disclosure element, marker, keyboard semantics, and expansion behavior stay intact.
- The disclosure contract-red run also surfaced one old-location assertion: `check:detail-layout` still required the pre-ordering `npcStatRows.value.filter` implementation. The semantic destination has moved to the exact `npcHeroStatLabels` four-item list plus `flatMap`; this assertion is retargeted one-for-one with no wildcard widening or count reduction. After that exact migration, only the expected 44px disclosure red may remain.
- Keyboard runtime verification then proved the NPC focus declaration was not visible: `:focus-visible` matched, but computed outline was `3px none` because its `--tp-color-focus` token does not exist anywhere in the frontend. Item already uses the owned three-theme `--button-focus-ring`. One new exact detail-layout assertion requires that existing semantic token on the NPC-scoped focus rule; expected-red reason is `missing implementation`.

## NPC final acceptance evidence

- The four approved capability profiles now share the approved `280px / fluid / 286px` Hero, `1fr / 300px` body, compact four-column shop rows, named cards, page anchors, live eight-capability completeness rail, and real related-NPC preference links. Hero metrics use the approved `生命值 / 防御 / 伤害 / 击退抗性` order without changing the rail order. Existing shop filters, eight-row preview/expansion, loot, preferences, buff relations, item links, image fallbacks, loading/error paths, and safe copy remain intact. See git for code-level diff details.
- The final `17 / 22 / 229 / 368` × `dark / morning-paper / warm-slate` × `1440x1000 / 390x844` matrix is 24/24 HTTP `200`, applies the requested theme, renders the four Hero metrics in approved order, and has zero console errors, failed images, or horizontal overflow. Every shop segment is `44px`; Merchant and Traveling Merchant remainder disclosures are now `44px` instead of `16px`, retain native Enter expansion, and expose a visible `3px solid` focus ring from each theme's `--button-focus-ring` token. One navigation request was aborted only while the first matrix overlapped Nuxt typecheck; an isolated post-gate Merchant dark desktop rerun is clean, so no persistent request failure remains.
- Screenshot SHA-256, Merchant dark desktop/mobile `7333249616d926e0abf9b9cda41942ce46cb3b985be59acfe8aee0fe85bab74c` / `62dbd8da06ce2ca52bd78a4aa2d21dc1bcfaca366d21e51c64ec209f45cde4ff`, morning-paper `d4e3757b2da574b9e52e89ceaa82be619efa217d5802ea1e27b32b19accd9a4e` / `7b4c6f1b14093acad9a7c4de8a0e3705bb9baf2d4d5d2b6c44d15ece567714f4`, warm-slate `a51f27c45e5b74dc01323d1fb5093b130ca1f5fb70ff9edeeed00869d6dc6ec8` / `4c4697e0439278e6ce285052caf22fbb7b4c72bdbe80883288258eb6fd68c09d`.
- Screenshot SHA-256, Guide dark desktop/mobile `250297f831fa77e35e30940365c23367bf9c4a05f9a507932961b79f561e5051` / `af82db7051f50c29a8c82d95ebe7f15275d2c0aaaa9e4c28c8e5f6947f8673ef`, morning-paper `ec8267db468f147b1058bf24c51ea247415450e6032c641375b4572f7313a654` / `ca59574cc6837b4104ec2d7bd4d53d210131e48dca6d1bfa0ec1387b34affc3d`, warm-slate `3de3b22f85082fc5c239b909cba4f8587513241127dc093dbac1508a73980640` / `e81c3037afb9380862191520186318c80ae7cf87a8079c87eb83f8493e3540d5`.
- Screenshot SHA-256, Pirate dark desktop/mobile `30a6ed14bfa9056de9f8dd68f38d210e739a92464eb34172fb39023b1c2526b7` / `7a3aa250040a93983115ed616be6ebee27cd1e2370c9f75e4d2bdcf559cf93f0`, morning-paper `294dda3f30c36fbbb368afbc13f2b490913c89bbc191f426b0c1bd97fdcd106e` / `1a936cabc0c48e70822e8a22a790dc381d32ec5d22a8789c767ef9b83a50fc28`, warm-slate `ba701bb81f879414f153651927ca738dd5b184cb12625b5766bd03f7f57615ab` / `c61a520e109a9a55299c598acbb528eae58bf3fd1cc15a7e3ee9cc77b14ffcf8`.
- Screenshot SHA-256, Traveling Merchant dark desktop/mobile `29fe42a6c03e23590ae4f161b4d9fe580e21920ae3eea0a969e638f183d5328f` / `2619125b239a2aeaa39412a37d80c7504d4bfafc6d49976478d7a78f7bdd4231`, morning-paper `8a72dcf4a4a488c3e17196d1467286fe3bd353c3f7f35428c0c96cc8d79b1620` / `8ea7ccdfb2f87b399534780173d6da5000935e2aa6531ad7137d4f7423fd78c6`, warm-slate `b7502e42de0c35676779ac5aefdc228cd7be361ed267b450eb1611a6bf515b6e` / `d58f5b911666f80c2d74f083df483a71fe59b3c429960e60d89651dddc45bb06`.
- Fresh NPC-slice gates: focused utility tests `6/6`; `pnpm run check` exit `0`; `check:visual-blocked-source` exit `0`; `git diff --check` exit `0`. `check:loading-skeleton` remains exactly the eight recorded `pages/armor-sets/[id].vue` baseline failures with no Item/NPC/Articles finding. Approved condition bands, Guide feature cards, and Traveling Merchant timeline remain data-bound differences: the live DTO does not provide those structured facts, so production shows current verified records and explicit omissions instead of copying sample claims.

## Articles v22 approved-body continuation

- NPC fidelity is checkpointed at `3d9045fa` (`feat(front): reproduce approved NPC detail pages`). The remaining production slice is `/articles` against the SHA-pinned v22 reference; navigation, shared breadcrumb ownership, outer container width, query/pagination behavior, and real links remain production-owned.
- Read-only payload verification of `/api/articles?page=1&limit=12` returns 24 published records and twelve rows on page one. Each list row includes `contentHtml`, title, slug, summary, cover, published time, author identity/avatar, view, like, comment, and favorite counts. This authorizes an explicitly approximate reading duration derived from visible article characters without a new request or API change. There is still no topic/tag aggregate, global popular ranking, author-wide publication count, or global view total.
- v22 truthfulness mapping: the lead header, two compact continuation links, author/metric band, cover note, CTA, five-row reading stack, archive toolbar geometry, dense rows, popular rail, and topic rail are reproduced. `同主题` becomes `接着阅读`; the stack says `当前公开排序`; mast statistics are `已发布 / 本页作者 / 本页浏览 / 本页收藏`; popular remains `本页阅读数据`; topic aggregation remains `资料整理中`. No topic/sort control is rendered without behavior.
- TDD red whitelist: `articleArchive.test.mjs` adds exactly two failures while `buildArticleArchive` remains 5/5 green. Both failures are caused by the missing `estimateArticleReadingMinutes` export: visible article text must produce a 300-Chinese-character-per-minute approximation, while tags, script/style bodies, and empty copy cannot inflate the estimate and retain a one-minute floor.
- Contract red whitelist before markup/CSS: `check:public-pages` may report only one exact route-screen migration, eleven missing approved component destinations (`article-approved-stage`, lead head/related/byline/stats/foot/CTA, reading-stack metadata, approved content/tools/row action), and two missing truthful data projections (reading-time binding plus four real mast statistics). `check:front-layout-layering` may report only eight missing CSS destinations: dark fixed page ground, final 1.12fr/452px stage, three-row lead frame, 30px/fluid/64px stack rows, fluid/278px archive grid, 44px CTA/focus treatment, light-theme flattening, and 640px recomposition. Existing shell, loading skeleton, article data/link/image, pagination, safe-copy, and forbidden-fixture contracts must stay green; any red outside this list stops implementation.

## Articles v22 final technical acceptance

- The production body now reproduces the approved mast, three-phase featured lead, two continuation links, five-row reading stack, author/stat band, cover note, real CTA, archive status bar, six dense archive rows, current-page reading rail, and truthful topic-data degradation. The implementation keeps the production navigation, breadcrumb ownership, outer width, query/pagination behavior, and live article links. See git for code-level diff details.
- Final runtime matrix: `/articles` under `dark`, `morning-paper`, and `warm-slate` at `1440x1000` and `390x844` is 6/6 HTTP `200`, applies the requested theme, renders `1` feature, `5` reading rows, `6` archive rows, and `2` continuation rows, and has zero console errors, request failures, HTTP resource errors, broken images, pending lazy images, or horizontal overflow. Every image was individually scrolled into view before the final `complete`/`naturalWidth` check.
- The live CTA resolves to `/articles/guide-true-nights-edge-demo` with HTTP `200`. The primary CTA and archive-row action are both `44px` high in every probe; keyboard focus is `3px solid` in all three themes. No fake topic count, global ranking, sort control, or fixture row was added.
- Final screenshot SHA-256 under ignored `front-nuxt/tmp/shots/2026-07-30-articles-fidelity/final-matrix/`: dark desktop/mobile `9fde52fb7dcf6a22b82fa92953f3291056eaac272610fcc6d8bcd5309cd11107` / `d39ba0a4894a11501d93ab40267dfb7bf28ac5264f639f65227c20c89413ffd7`; morning-paper `826074997211d6ae59dbd48e4705e2dd9c2dc7d3f86cde73bb6cf4838463ea47` / `10a160984ad9484fda1af73b6a998582b62dd69d0b8c64d7f8f3b07e7a1ddf55`; warm-slate `a283a379d24af94763c3941603d35914bd39cd21e8fad0abe6808b0e558f3d8f` / `3df955bd383f89547e8a5e4de3eefd08273c984cb5b64d271fec57d00853ae57`.
- Fresh gates after the final visual adjustment: `pnpm run check` exit `0`; `articleArchive.test.mjs` `7/7`; `check:public-pages`, `check:front-layout-layering`, `check:visual-blocked-source`, and `git diff --check` exit `0`. `check:loading-skeleton` remains exactly the eight recorded `pages/armor-sets/[id].vue` baseline failures with no Item/NPC/Articles finding.
- Server-dependent baseline comparison is unchanged outside this route: `check:light-theme` still reports the recorded homepage, historical string routes, category, login, and font findings but no production `/articles` finding; `check:typography-spacing` still times out at unrelated `/search ready`; `check:crafting-wiki-structure` still reports the recorded `/crafting` graph/icon/expansion findings. These are not repaired in this slice.
- Path-only review confirms the Article-owned changes are limited to its page, two article components, domain CSS, article projection/test, two exact contracts, this plan, and the active devlog/current-state records. The pre-existing modified design HTML, historical devlog, untracked closed Article entry, and `reports/` remain user-owned and untouched. The task stays `active` and Article remains uncommitted until user visual acceptance.

## Article archive-row refinement

- User selected the balanced archive-row option after runtime measurement showed a `121px` desktop row with only a `78x64` cover. The approved refinement uses `104x84` on desktop and `88x72` on mobile without increasing the desktop row beyond its existing natural density. Images use contained rendering so small entity art is not cropped.
- The approved information hierarchy removes the player-irrelevant raw article ID. Every row keeps the real published date, safe author label, approximate reading duration, and view count; likes, comments, and favorites render only when their sanitized live value is positive. The article collection, API request, page size, ordering, garbled historical record, and data-quality policy do not change in this UI-only refinement.
- Initial contract-red whitelist is six `missing implementation` findings before production edits. `check:public-pages` owns three: the `公开手札` identity replacing raw `文章 #id`; positive-only like/comment/favorite bindings in `ArticleArchiveRail.vue`; and non-negative page adapters plus prop bindings for `likeCount` / `commentCount`. `check:front-layout-layering` owns three: exact desktop `104px / fluid / action` track with `104x84` cover; contained image rendering; and the `88px / fluid`, `88x72`, copy-column action mobile composition. The observed red is exactly `3 + 3`, with no failure outside the whitelist.
- Design-system self-review adds one bounded seventh `missing implementation` red in `check:front-layout-layering`: the current archive metadata is explicitly reduced to `10px`, below the approved public UI `12px` production metadata floor. The new exact assertion owns only `.article-approved-content .article-archive-row__meta`; it does not change typography elsewhere. Existing article links, fallback, lazy loading, summary, pagination, truthful topic/popular labels, 44px targets, focus, theme, loading, and safety assertions may not move, widen, or disappear.
- Mobile runtime investigation found one migration defect after metadata moved inside `.article-archive-row__copy`: the inherited `max-width: 640px` rule still assigns `.article-archive-row__meta` to outer-grid coordinates `column 2 / row 2`. Those coordinates now create a `157px + 91px` implicit grid inside the copy, splitting kicker/title and summary/metadata and inflating the first measured row to `238.9px`. One additional exact `check:front-layout-layering` assertion is added before the fix; its only expected red requires the approved mobile copy at outer column 2 / row 1 and resets nested metadata to normal auto flow. Reason: `migration`; assertion count increases by one and the existing 88x72/action contract remains.
- The new mobile-flow assertion failed alone as expected. A browser-only style injection then confirmed the root-cause hypothesis before production CSS changed: the copy became one `252px` column, all four text layers returned to document order, and the same first row fell from `238.9px` to `200.2px` while preserving its `88x72` cover and `44px` action. The green implementation adds only the two approved-scope coordinates proven by that experiment.
- Final refinement result: desktop rows use `104px / fluid / action` tracks with `104x84` contained art and measure `123.8px` for the first live row. At `390x844`, all six rows keep `88px / 252px` tracks, `88x72` art, one `252px` copy column, auto-flow metadata, and a `252x44px` action; live row heights are `182.3-200.2px` with no split text columns. The contract now slices the v22-owned `max-width: 640px` block before asserting mobile composition, so earlier NPC or legacy Article media rules cannot satisfy it accidentally.
- The final `dark / morning-paper / warm-slate` x `1440x1000 / 390x844` matrix is 6/6 HTTP `200`, renders six archive rows, applies the requested theme, keeps metadata at `12px` and focus at `3px solid`, and has zero console errors, request failures, HTTP resource errors, failed images, or horizontal overflow. Keyword probe `真永夜` renders the real `LOLBEN / 约 3 分钟 / 177 浏览 / 3 评论 / 1 收藏` values and omits the zero like count.
- Screenshot SHA-256 under ignored `front-nuxt/tmp/shots/2026-07-30-articles-fidelity/archive-row-refinement/final-matrix/`: dark desktop/mobile `a25737f9fe7ebc31edda0a62e865ab53fa53d78b4a1d8919ba2298ca55dc4085` / `85ebc044d5bf04994d14f7645d6f7053e1fdfd54f67fc77c981fb295df26f505`; morning-paper `fe80c5110c902308272b1bb24ed76970033873e22d50ddd22eae1cef51ad2863` / `64c5506cbd6290ee03f92a7d8f37bfbd8723d59e43e55be31ab1ff98c44efc2c`; warm-slate `cbfc278c0f32da8cee0d657482c57cec9c0e4a8aee41a6b24b990d5fda14dd5e` / `935d1036adc235b63d585150c44a9d172c77c766a0367c0c79793912061f1947`. Focused dark mobile row evidence is `cab20a33d6a3a827ff7d5e1273f31f1a1ea0af95cb61ec73cce2984ca70703fc`.
- Fresh gates: full `pnpm run check` exits `0`; `check:public-pages`, scoped `check:front-layout-layering`, Nuxt typecheck, `check:visual-blocked-source`, Article tests `7/7`, and `git diff --check` pass. `check:loading-skeleton` remains exactly the eight Task 0 `pages/armor-sets/[id].vue` failures with no Item/NPC/Articles finding. The refinement stays uncommitted and the entry remains `active` for user visual acceptance.

## Article popular-reading cover rail

- User approved visual option B from `.superpowers/brainstorm/1605451-1785373344/content/article-popular-cover-layout.html`. Current desktop geometry is measured at `1070px` archive rows, `28px` gap, and `278px` rail. The approved target is `1028px / 28px / 320px`, giving each popular entry a `56x48` contained live cover or the existing fallback while preserving readable archive rows.
- Data and interaction boundary: reuse the component's existing `coverUrl` and `coverFallback` props; retain the same four current-page popular entries, rank, title, view/favorite values, article destination, safe image chain, and one linked entry. No API, ranking, page projection, pagination, sort, or mobile ordering changes. Mobile remains a single-column archive followed by the reading rail.
- Contract red whitelist before implementation: one exact public component destination for linked cover/fallback/title/metrics; three exact layout destinations for the `320px` desktop rail, `56px / fluid` popular link with `56x48` contained art, and retained approved mobile single-column composition. Any other red stops implementation.
- The observed red matched the migration boundary: `check:public-pages` reported only the one missing popular-entry destination; `check:front-layout-layering` reported only the missing `320px` rail and `56x48` popular-cover geometry. The existing isolated mobile single-column assertion stayed green, so it did not produce a third failure. No assertion was removed or widened.
- The production component now keeps rank outside one full-entry article link and renders three live covers plus one existing fallback for the four current-page popular records. Desktop runtime geometry is exactly `1028px / 28px / 320px`; all popular art frames are `56x48`, link heights are `48-58.21875px`, and mobile remains archive-first then rail. See git for code-level diff details.
- Final `dark / morning-paper / warm-slate` x `1440x1000 / 390x844` runtime matrix is 6/6 HTTP `200`, applies the requested theme, renders four valid popular links and six archive rows, keeps `3px solid` focus, and has zero console errors, request failures, HTTP resource errors, failed images, or horizontal overflow.
- Screenshot SHA-256 under ignored `front-nuxt/tmp/shots/2026-07-30-articles-fidelity/popular-cover-rail/final-matrix/`: dark desktop/mobile `724d03e7eb7f9b920ae8ef832fda9db3db717e84eab2a5995e84a6e1b47c865c` / `4580ba4b18ed3dd7ec51cf439ed523b7a106cee72eec704a36b3a737f52a8404`; morning-paper `5c3c3fb08a80faeacb12732f16854d9802a888f32e4ea09087bc1d6d4beb5cee` / `5f5281330ead984570abf7bf0e337600137b5e4a5602ed05e2dd59205b304ac3`; warm-slate `5ec78a37722ecc36555ebe38631ba3f7cbeb1c8946a2c3fb54da7072059b9804` / `dad974da8e0b91efb3e817227c15b375aa341aeb3c12ef03cde4920546928965`.
- Fresh gates: `check:public-pages`, `check:front-layout-layering`, Article tests `7/7`, Nuxt typecheck, full `pnpm run check`, `check:visual-blocked-source`, and `git diff --check` pass. `check:loading-skeleton` remains exactly the eight recorded `pages/armor-sets/[id].vue` baseline failures with no Article finding. Article work remains uncommitted for user visual acceptance.

## Article search and complete-archive correction

- User acceptance found a functional regression: `浏览全部` anchors to the content shell, but the normal archive projection excludes the six records reused by the featured fold, and the route has no operable control for its existing `?keyword=` API query. The approved correction keeps the fold editorial and makes the archive a complete current-page list.
- Scope is limited to the Article projection, route query control, archive heading markup/styles, exact Article contracts/tests, this plan, and active devlog records. Search submits through the existing route/API chain and resets to page one; page size, backend pagination, ordering, ranking, API, data, and article links remain unchanged.
- Planned red matched exactly: Article tests reported three projection failures; `check:public-pages` reported one missing operable-search destination; `check:front-layout-layering` reported two missing desktop/mobile search layouts. No unrelated contract failed.
- The featured fold still selects one lead and five reading entries, while `archive` now contains all current-page API rows. The current local first page therefore renders twelve archive rows rather than only the six rows not reused by the fold. The archive search owns a labelled 44px input/button pair; submitting `真永夜` produces `/articles?keyword=真永夜`, one real result, and no featured fold; clearing it restores `/articles` and twelve rows.
- Final `dark / morning-paper / warm-slate` x `1440x1000 / 390x844` runtime matrix is 6/6 HTTP `200`, applies the requested theme, renders twelve archive rows, preserves four popular links and mobile archive-first ordering, and has zero console errors, request failures, HTTP resource errors, failed images, or horizontal overflow. Search controls are `44px` high with `3px solid` focus.
- Screenshot SHA-256 under ignored `front-nuxt/tmp/shots/2026-07-30-articles-fidelity/popular-cover-rail/final-matrix/`: dark desktop/mobile `ef12cecb081f563d8d013aa59ac8ebe134fa1515d90e43f0197547008e9edd7a` / `d37cb29599400f5382c7b2c1a15dfaa6a29c67df1cf301a36748b79b5ecaccd6`; morning-paper `0cf9944a362102927d2b38922e3386a35d700e5b7c6bfec390a91b77ebbb8d63` / `b67a55ca7c6c4ded2d1402c45e322324e47dceac98f6b33b37eda6800e5e5a8e`; warm-slate `6fdf155a74206cd15877ddf91636d472fd4db2642c8f4bab14b349d4c8fe19aa` / `044ec37221a6e4f1eeb7088cfb4d358a5091b4790acbc475cc4903dabf233740`.
- Fresh validation: full `pnpm run check`, Nuxt typecheck, `check:public-pages`, `check:front-layout-layering`, `check:visual-blocked-source`, Article tests `7/7`, and `git diff --check` pass. `check:loading-skeleton` remains exactly the eight recorded armor-set baseline failures with no Article finding. Standard local stack is live at front `15177`, backend `18191`, and admin `13004`; Article work remains uncommitted for user visual acceptance.

## Article discovery/archive split decision

- User visual review rejected further replacement of the production Article fold. `/articles` keeps the current `article-approved-stage`, five-entry reading stack, six-row latest layout, and current-page popular rail. The existing search control moves into `article-mast article-approved-mast` so it is visible without scrolling; submitting it navigates to `/articles/archive?keyword=...` and cannot remove the discovery fold.
- A dedicated `/articles/archive` owns complete search and pagination at the existing twelve-record API limit. Its approved presentation is the full-width four-column compact record-card grid from the accepted A option, with no popular/topic sidebar, `74x74` desktop and `88x72` mobile contained covers, truthful live metadata, and the production Article theme/background rather than the standalone mockup palette.
- Legacy `/articles?keyword=` and `/articles?page=N` deep links preserve their query while moving to the archive route. Bare `/articles` remains first-page discovery and no longer owns pagination. No API, backend, database, crawler, ordering, topic aggregate, or global popularity work is authorized.
- The reviewed design authority is `docs/superpowers/specs/2026-07-30-article-discovery-archive-split-design.md`. Existing Article implementation remains uncommitted; the next step is a contract-first implementation plan, followed by production work and renewed three-theme desktop/mobile visual acceptance.
- Plan-auditor review repaired six execution gaps before planning: discovery now owns only post-fold positions 7–12; false-complete discovery copy is forbidden; archive breakpoints are fixed at `1180 / 900 / 640`; mast/archive search IDs are distinct; the ignored option-A reference is pinned by path, size, and SHA-256; and all four route/layout/loading/image contracts must register the new static route explicitly. The review also requires query redirects before data fetch, twelve archive skeleton slots, static-route-vs-slug runtime proof, serialized ownership, and no push/merge without a later request. No Critical or Important design defect remains.

## Article discovery/archive split execution baseline

- The audited implementation plan is committed at `601970e4`, with the plan-only execution-baseline repair at `9304689b`; no product file changed in either commit. Execution remains serialized in this worktree under `docs/superpowers/plans/2026-07-31-article-discovery-archive-split.md`.
- Task 0 reconfirmed the pinned option-A reference at SHA-256 `fbe2833b8a2701cfb81d24c7772a453854e6c91a727debc5eac4a3ebc1b54edd` and `10860` bytes. All Article-owned file fingerprints matched the plan. The ignored rollback patch is `front-nuxt/tmp/rollback/article-discovery-archive-split.patch`, `64966` bytes, SHA-256 `d5f02ef9eeec7262b12637569e408147df927c02a30225dd067abd02afc97dfa`.
- Contract proxy baseline is public pages `352`, user module `56`, layout layering `39`, loading skeleton `21`, and preview fallback `13`. Article unit tests are `7/7`; public/user/layout/preview/visual-blocked gates and full `pnpm run check` exit `0`. Loading skeleton remains exactly the eight recorded `pages/armor-sets/[id].vue` failures with no Article finding; armor remains out of scope.
- The standard local stack was restored without a data refresh: front `15177`, backend `18191`, admin `13004`, database `terria_v1_local`. `/articles` returns `200`; `/api/articles?page=1&limit=12` returns 12 rows, total 24, page 1, limit 12, and two pages. Product implementation has not started yet. See git for code-level diff details.
- Task 1 projection TDD matched the whitelist: exactly four new `discoveryLatest` assertions failed because the property was absent while archive order and reading-time tests stayed green. The minimal projection now passes `7/7` for positions 7–12, 7–8, exact-six empty latest, and fewer-than-six archive-only degradation; the full API-page `archive` remains unchanged. See git for code-level diff details.

## Article discovery/archive split implementation handoff

- Tasks 2–6 are implemented and remain uncommitted. `/articles` now fetches only unfiltered page one at limit 12, keeps the approved feature/reading stage, moves search into the mast, renders only `discoveryLatest` positions 7–12, and no longer owns pagination. `/articles/archive` is a static route with live keyword/page API state, twelve stable skeleton slots, full-width four/three/two/one-column cards, contained image fallback, search/clear/retry, and `CommonPaginationDock`. See git for code-level diff details.
- Runtime SSR testing found that the original anonymous page-meta middleware changed the hydrated browser URL but returned HTTP `200` without `Location`. The plan and exact public contract were repaired before the production fix; `middleware/article-discovery-archive-compat.ts` is now a named Nuxt route middleware attached before discovery data setup. Encoded legacy URLs return `302` to the equivalent archive URL and preserve keyword/page, while canonical discovery/archive routes returned `200` during the live run.
- Contract red/green migration is resolved: projection tests are `7/7`; public-page, user-module, layout-layering, preview-image, breakpoint, visual-blocked-source, Nuxt typecheck, and full `pnpm run check` passed before the final named-middleware repair. After that repair, fresh projection `7/7`, public-page contract, Nuxt typecheck, and `git diff --check` pass. Contract proxy counts are `364 / 56 / 53 / 23 / 15`, all at or above the Task 0 baseline `352 / 56 / 39 / 21 / 13`. Loading-skeleton remains exactly the eight known armor-set findings and has no Article finding.
- The live `dark / morning-paper / warm-slate` × `1440x1000 / 390x844` matrix covered both `/articles` and `/articles/archive`: all 12 probes were HTTP `200`, applied the requested theme, reported no console/request failure or horizontal overflow, and measured 12 archive cards when live data permitted. Evidence is ignored under `front-nuxt/tmp/shots/2026-07-31-article-archive-split/`. Viewport screenshot hashes: dark discovery desktop/mobile `bbce6d90ab0351dd45c05d8802eac7dd3d0b4afc880a1a5bceab7a296e4f5378` / `14ef3426423b3ee3e2252f1b4b93770c8b64e7ecd264fd6ec846ec7dfba2e83a`, dark archive `f1e0c065c8b4efb83caa801703c22a7271f8df4b01b7c1ecfa4e0655a7e17bdd` / `dc84c0762a160566d11dfc35e056d4805d68bca0a43bff96b65689b82a6d388c`; morning discovery `8fdd71817f016860c7f1caabc37c704cfa9b6827e69225bdf119b624688b722e` / `829d15fad7629308ca2581f79f9bc9dc22129cf86a431b2306f9876ef7746500`, archive `5bb91b37d551147afe2c56141216ce2f3e4076bb9caff7d35c86ad97907b5c5b` / `6d01735e5adbb42b233d07666b05b8357b66bc19b22f6d253c4dc68d762dee86`; warm-slate discovery `d627f0c67bebadc179915e3a0348e380a08b0f470a899a63f0400f8fc228a9ab` / `915edb67129ea4ebfbeb25656129dae38fd449844cdc9bf7c056d6a7a8d5bcda`, archive `061dba5f6b5264457160c332d6dcbe473a8766b09adb2027ff0b10fdbc54a09f` / `50fe26cb980cd5c2d41a446a102241d58305dd2ae366eba4a61a69f0d4fd92a1`.
- The final Playwright flow passed feature/reading/latest counts `1 / 5 / 6`, no fold/latest link intersection, mast search trim and browser back, archive search/clear/page-two/back, fresh-page legacy redirects without a discovery request, static-route ownership, 12 cards, complete images, zero runtime errors, no overflow, visible `3px` focus, and 44px targets. Two earlier probe failures were test-timing/disabled-control defects and were repaired in the plan without weakening assertions. The final temporary probe was deleted, but its post-repair hash was not captured.
- Handoff stop: the three cross-route runtime gates were first invoked without their existing `TERRAPEDIA_FRONT_NUXT_URL` override and therefore targeted unused port `5176`; the corrected run was interrupted for handoff before results completed. The standard stack is now down on `15177 / 18191 / 13004`. Next owner must restart it, run Task 7.5 with the explicit base URL, repeat the full static gate after the named-middleware repair, perform the design-system visual review, and then give the user live links. Residual visual review should pay particular attention to fallback-cover typography and kicker/date spacing in compact archive cards. Status remains `active`; no implementation commit, push, merge, stash, or cleanup was performed.

## Article discovery/archive split Task 7 closure

- Stack restart required one authorized infrastructure repair outside the product diff. Redis `16380` refused to load because the previous non-graceful stop left a torn 70-byte tail on `appendonly-16380.aof.2.incr.aof`, so the backend could not reach `18191`. Read-only `redis-check-aof` proved the BASE AOF valid at 276 keys and the incr file valid up to `32870231` of `32870301` bytes. With explicit user authorization the directory was copied to `~/.local/share/terrapedia/redis/aof-backup-16380-20260731-184304` (verified byte-identical by SHA-256) and `redis-check-aof --fix` truncated exactly those 70 bytes. The restarted stack reports `verify-local-stack: all requested checks passed` on front `15177`, backend `18191`, and admin `13004`. No repository path and no product behaviour changed.
- Task 7.2 direct routing is proven: `/articles` and `/articles/archive` return `200`; `/articles?keyword=...`, `/articles?page=2&keyword=...`, and `/articles?page=2` each return a real SSR `302` whose `Location` preserves the encoded keyword and page. The archive SSR body carries one `article-archive-page-shell`, one `article-archive-card-grid`, twelve cards, and zero `article-detail-layout`, so the static route owns the path rather than `pages/articles/[slug].vue`.
- Task 7.5 reproduces the recorded same-server baseline with no new target-route finding. `check:light-theme` exits `1` on the homepage, six unavailable historical slugs that render the shared route-not-open placeholder, `/categories/weapons`, login/register, and the font-family record; it reports no production `/articles` or `/articles/archive` finding. `check:typography-spacing` exits `1` on the unrelated `/search ready` timeout. `check:crafting-wiki-structure` exits `1` on the independent `/crafting` graph-order and expanded-child assertions. None are repaired here.
- The Task 7.6 visual review found two real compact-card defects that every static contract had missed, reproducible in all three themes. `.public-article-kicker` and `.public-article-cover-fallback b/em` receive their base styling from the `pages/articles/index.vue` scoped block, so the new archive route inherited none of it: the kicker computed to `display: block` with a measured `0px` gap that read as `公开手札2026/06/05`, both kicker spans rendered at `16px` — larger than the `14px` card title — and the fallback well rendered `16px` lettering that pushed itself to `80px` inside the `74px` cover.
- Repair followed the plan's runtime-failure branch: two focused assertions were added to `check-front-layout-layering-contract.mjs` and confirmed red as the only failures before any CSS changed, then `assets/css/domains/detail-pages-redesign.css` gained archive-card-scoped kicker and fallback rules built only from existing tokens. The first repair pass regressed the kicker date to `--tp-color-text-faint`, measured at `3.93` on morning-paper; it was moved to `--tp-color-text-muted` and the readable colour is now locked inside the contract regex.
- Post-repair runtime measurement across `dark / morning-paper / warm-slate`: kicker is `display: flex` with a measured `8px` span gap plus the shared `·` separator, kicker `12px` sits below the `14px` title, fallback renders a `22px` monogram over an `8px` uppercase wordmark inside a `72x72` well with no overflow, and minimum text contrast is `7.97 / 5.33 / 7.04` with the kicker date no longer the floor. Discovery still measures `1 / 5 / 6` with zero fold-latest link intersection; archive measures twelve cards at four desktop columns and one mobile column, zero nested anchors inside whole-card links, DOM order equal to visual order, no horizontal overflow, and zero console or request failures.
- Design-system review, explicit result: editorial-discovery-plus-dense-archive archetype PASS; data and real covers as visual subject PASS; dark ground/band/surface/object depth distinguishable without a black card wall PASS; both light themes flat, low saturation, readable PASS; no nested cards, new palette, fake art, fake aggregation, oversized hero, or sparse long archive PASS; card title/date/author/read-time/view legibility at desktop and mobile PASS only after the repair above, FAIL as first measured; whole-card links free of nested links with keyboard order matching visual order PASS; image wells reserve dimensions and contain their art PASS; controls labelled, focus-visible at `3px`, and at least `44px` PASS.
- Fresh full gate after the repair: `articleArchive.test.mjs` `7/7`; `check:public-pages`, `check:user-module`, `check:front-layout-layering`, `check:preview-images`, `check:breakpoints`, `check:visual-blocked-source`, and full `pnpm run check` exit `0`; `git diff --check` exits `0`; `check:loading-skeleton` remains exactly the eight recorded `pages/armor-sets/[id].vue` findings with no Article finding. Contract proxy counts are `364 / 56 / 55 / 23 / 15`; layering rose from `53` to `55` for the two new assertions and no count fell below the Task 0 baseline `352 / 56 / 39 / 21 / 13`.
- The twelve-probe matrix was recaptured after the repair and all probes are HTTP `200` with the requested applied theme, no error array, and `hasHScroll: false`. Current viewport screenshot hashes under `front-nuxt/tmp/shots/2026-07-31-article-archive-split/`: dark discovery desktop/mobile `bbce6d90ab0351dd45c05d8802eac7dd3d0b4afc880a1a5bceab7a296e4f5378` / `14ef3426423b3ee3e2252f1b4b93770c8b64e7ecd264fd6ec846ec7dfba2e83a`, dark archive `d20813b711a1b50b6d7f0fc9da8399dcce46e724492e54e043215c8d758eecdd` / `f0d82f47f5439be64e0c557a998ab2b7bb1324ee5b287c8311abb1480307b1e4`; morning discovery `8fdd71817f016860c7f1caabc37c704cfa9b6827e69225bdf119b624688b722e` / `829d15fad7629308ca2581f79f9bc9dc22129cf86a431b2306f9876ef7746500`, morning archive `833878e13438560a81076a703606a69de038ad7c283b3d5006ef514b0cd923bd` / `4bcd31cc423a04cffa095ec3a72a45568896f88496896b49ea288ea5c471123c`; warm-slate discovery `d627f0c67bebadc179915e3a0348e380a08b0f470a899a63f0400f8fc228a9ab` / `915edb67129ea4ebfbeb25656129dae38fd449844cdc9bf7c056d6a7a8d5bcda`, warm-slate archive `71e794a9920421f50320f0adda9d36879af7bef80571c0395a519dcb4346c958` / `2aa77f95bc6a2195b14e822f8f1eb72740d68308f63278217158583698d3492f`. The measurement probe was `front-nuxt/tmp/article-visual-review.mjs`, `8718` bytes, SHA-256 `560ff19cc5ac299349560e9389fb9d4689b38726edcaa5e75958082f6a255609`, deleted after its results were recorded.
- Truthful-source note: the tenth archive record renders the title `??????????????2026-03-24?` exactly as the public API returns it. This is stored article data, not a rendering or encoding defect, and no data repair is authorized by this plan. See git for code-level diff details.
- Status remains `active`. Article implementation is complete and uncommitted pending user visual acceptance; no commit, push, merge, stash, or cleanup was performed.

## Article discovery dark-depth repair (user-directed, G3 + L0)

- User acceptance of Task 8 was withheld for the dark discovery page: the fold read flat and `article-archive-layout` read as "一坨黑". Investigation found a fidelity regression rather than a taste dispute. The approved `articles-story-led-v22` reference builds the fold from two elements — a lighter full-width band and a *darker* inset stage with a gold border and deep shadow — but production collapsed both onto one `<section class="article-fold article-approved-stage">` (`components/article/ArticleFeatureMeta.vue`). The reference's `rgba(3,8,5,.78)` inset then became `color-mix(in srgb, var(--tp-color-page) 78%, transparent)`, which composites 78% of the page colour over the page colour and therefore resolves back to the ground. All four `--article-approved-*` surfaces were transparent tints of the same `--tp-color-page`, so ground/stage/stack/object were numerically identical in dark. The light themes never showed it because their override block already re-declares all four as real `--tp-color-surface*` values.
- The below-fold complaint had a second cause. The reference deliberately leaves that region on bare ground with rule lines, and an earlier reading of the design system treated a surface there as forbidden. That reading was too strict: the system reserves cards for *repeated items*, and the latest-submission rows are exactly that. The accepted `/articles/archive` card grid was the counter-example — same data density, never questioned, because each card is fully enclosed.
- Four dark treatments were specimen-rendered for the fold and three for the archive region, then dialled through four fill levels in an interactive rig with in-place switching (keyboard variant/brightness switches, the archive section anchored so it does not drift between switches — measured at zero drift across all four variants). The user selected **G3 two-column cards at fill level L0**, the darkest step, where separation comes from borders, gaps, and shadow rather than fill brightness. Specimens are ignored under `.superpowers/brainstorm/article-dark-depth-2026-07-31/content/`.
- Implementation is token-derived, with no local palette. Dark now uses `color-mix(in srgb, var(--tp-color-positive) 3%, var(--tp-color-page))` for the stage and `5%` for stack/object, plus `--article-approved-line` / `--article-approved-line-strong` at `color-mix(in srgb, var(--tp-color-accent) 30%/44%, transparent)`. Measured against the approved L0 targets the stage renders `rgb(10,13,10)` versus `rgb(9,14,10)` and stack/card render `rgb(13,17,13)` versus `rgb(11,18,13)` — a maximum deviation of two levels on any channel. The light block gained `--article-approved-line*` overrides pinned to the shared border tokens so the accepted light appearance is unchanged.
- Structure: `.article-approved-content .article-archive-rows` is a two-column card grid collapsing to one column at 640px; each card keeps the original three-track shape at `88px minmax(0, 1fr) auto` with the cover spanning both rows, copy across tracks two and three, and metadata plus the 44px action sharing a bordered footer line. Reading-rail blocks were promoted to the same object surface. `.article-approved-content` gained the fold closing rule. `ArticleArchiveRail.vue` moves `.article-archive-row__meta` out of `.article-archive-row__copy` so it can occupy that footer; nothing else in the template changed.
- Contract ratchet, migrated one-for-one before any CSS changed and confirmed red as exactly six failures first: the frozen desktop `104px minmax(0, 1fr) auto` row track became `88px minmax(0, 1fr) auto`; the frozen `104px`/`84px` cover became `88px`/`72px`; the 640px row-reflow marker group and the mobile copy/meta reflow regex were replaced by a single-column collapse assertion plus a desktop footer-placement assertion. Five assertions were added: two-column grid, card object surface, rail-block surface, token-derived dark ladder, and the fold closing rule. Proxy counts are `364 / 56 / 60 / 23 / 15`; layering rose `53 → 55 → 60` and nothing fell below the Task 0 baseline `352 / 56 / 39 / 21 / 13`.
- Validation: `articleArchive.test.mjs` `7/7`; the six focused gates and full `pnpm run check` exit `0`; `git diff --check` exits `0`; `check:loading-skeleton` remains exactly the eight recorded armor-set findings. A runtime probe asserts, in all three themes, a two-column grid of six cards, `88x72` covers, metadata rendered exactly once per card and no longer nested in copy, footer metadata and action on one line with the action right of it at `44px`, the closing rule present, no horizontal overflow, zero console/request errors, and every card text ratio at or above `4.5` (minimums `7.97 / 5.33 / 7.04`). Mobile renders one column with no overflow. Evidence is ignored under `front-nuxt/tmp/shots/2026-07-31-article-g3-l0/`; probe `tmp/verify-g3-l0.mjs`, `6746` bytes, SHA-256 `2e50d79a5dae7d3b4f31a081371a7da2dd62911923f0f49fcdaef29eeec36f4a`.
- Light-theme non-regression has byte-level proof: the `morning-paper` and `warm-slate` viewport screenshots are hash-identical to their pre-change captures (`8fdd71817f016860c7f1caabc37c704cfa9b6827e69225bdf119b624688b722e` / `829d15fad7629308ca2581f79f9bc9dc22129cf86a431b2306f9876ef7746500` and `d627f0c67bebadc179915e3a0348e380a08b0f470a899a63f0400f8fc228a9ab` / `915edb67129ea4ebfbeb25656129dae38fd449844cdc9bf7c056d6a7a8d5bcda`), so the light fold is pixel-unchanged; only the below-fold card region differs, by design. Current dark viewport hashes are `de4a3ee9887e67cd9c82fe45e8854a995a0c85c5cffbc3706a360b3415c29a33` desktop and `f32772f437adf810e70adb429ef6ae59e7188a0d639a311405ab633cfadbadec` mobile.
- Residual boundary: the reference's band is full-bleed, whereas this repair carries the closing rule at content width and drops the band element entirely, because at L0 the band fill is imperceptible against the ground and a wrapper would change the component's DOM for no visible gain. If a full-bleed band is wanted later it needs that wrapper in `ArticleFeatureMeta.vue`. See git for code-level diff details.

## Article discovery dark-depth repair (user-directed, G3 + L0)

- User visual acceptance rejected the dark discovery page: `article-approved-stage` and `article-archive-layout` read as one flat black mass. Root cause is a translation defect, not a taste dispute. The approved v22 reference builds the fold from two elements — a lighter `.fold` band plus a darker `rgba(3,8,5,.78)` inset `.stage` with a gold rule and deep shadow — but production collapsed both onto one `<section class="article-fold article-approved-stage">` and expressed all four surfaces as `color-mix(var(--tp-color-page) N%, transparent)`. A transparent tint of the page colour composited over the page colour returns the page colour, so ground, stage, reading stack, and object all resolved to the same value. Light themes were unaffected only because their override block already replaced those four variables with real `--tp-color-surface` values.
- Four design rounds were produced under `.superpowers/brainstorm/article-dark-depth-2026-07-31/content/` (git-ignored, so the pending diff is untouched): v1 band-vs-surface polarity, v2 brightened ladder, v3 dimmed ladder, and an interactive comparison rig with in-place variant switching, four fill levels, and a viewport-anchored scroll restore. The user selected **G3** (two-column archive card grid) at **L0** (darkest fill level, separation carried by borders and gaps rather than fill brightness).
- The design-system reading that first ruled out card surfaces below the fold was too strict and was corrected: `docs/design/terrapedia-public-ui-design-system-v1.md` assigns Surface the job of framing *a repeated datum*, and reserves cards for *repeated items*. Archive rows are repeated items, so giving them a real object surface is compliant; the prohibition covers turning whole sections into cards and nesting cards.
- Contract ratchet migration in `scripts/check-front-layout-layering-contract.mjs`, one-for-one: desktop row track `104px minmax(0, 1fr) auto` → `88px minmax(0, 1fr) auto` with the three-track shape preserved; cover `104px`/`84px` → `88px`/`72px`; the mobile 640px marker group (`88px minmax(0, 1fr)`, `88px`, `72px`, `grid-column: 2`, `justify-content: flex-end`) → a precise `.article-archive-rows` single-column collapse regex; the mobile copy/meta reflow regex → a desktop footer-placement regex. Five assertions were added: two-column card grid with preserved tracks, shared footer line seating metadata and the 44px action, bordered/rounded object surface, token-derived dark ladder, and the fold closing rule. Proxy count for this file rose `55 → 60`; no counted assertion was removed.
- Implementation is token-only, no local palette. `--article-approved-stage-bg` and `--article-approved-stack-bg`/`--article-approved-object-bg` now mix `3%` and `5%` of `--tp-color-positive` into `--tp-color-page`; the lead keeps its two gradient overlays and drops its opaque base so it inherits the stage; new `--article-approved-line` / `--article-approved-line-strong` derive `30%` / `44%` of `--tp-color-accent`. The light-theme override block gained matching line variables bound to `--tp-color-border` / `--tp-color-border-strong`, so both light themes render byte-identically to their accepted state. `.article-approved-content` gained the fold closing rule. `ArticleArchiveRail.vue` moved `.article-archive-row__meta` out of `.article-archive-row__copy` so the footer can seat metadata and the action on one line; markup markers required by `check-public-pages` and `check-user-module` are unchanged.
- Runtime measurement against the approved L0 targets: stage renders `rgb(10,13,10)` against target `rgb(9,14,10)`; stack and card render `rgb(13,17,13)` against target `rgb(11,18,13)`; card border alpha is exactly `.30` and the closing rule exactly `.44`. Desktop is a `508px 508px` grid of six cards with `88x72` covers, the footer places metadata left of a right-aligned `44px` action on a shared line, and mobile collapses to one column. Minimum text contrast is `7.97` dark, `5.33` morning-paper, `7.04` warm-slate; no theme has horizontal overflow or console/request failures.
- Fresh gates after the repair: Article unit tests `7/7`; `check:public-pages`, `check:user-module`, `check:front-layout-layering`, `check:preview-images`, `check:breakpoints`, `check:visual-blocked-source`, and full `pnpm run check` exit `0`; `git diff --check` exits `0`; `check:loading-skeleton` remains exactly the eight recorded armor-set findings. Proxy counts are `364 / 56 / 60 / 23 / 15`, all at or above the Task 0 baseline. The six-probe three-theme matrix is `200` with the requested applied theme and no horizontal overflow; one warm-slate mobile run reported a burst of Vite dev-server prefetch `requestfailed` entries for unrelated `_nuxt/**?macro=true` modules, and two immediate re-runs returned zero errors, so it is recorded as dev-server flake rather than a route defect. Two independent measurement probes were used and both assert-pass: `front-nuxt/tmp/article-dark-depth-verify.mjs`, `6687` bytes, SHA-256 `603d46e59b4549e15eba3dd56890eeb100d2f5b4462cf53911c02f11be3da477`, and `front-nuxt/tmp/verify-g3-l0.mjs`, `6746` bytes, SHA-256 `2e50d79a5dae7d3b4f31a081371a7da2dd62911923f0f49fcdaef29eeec36f4a`. A single `check:light-theme` run also surfaced an `/npcs` `entity-filter` focus-restore finding absent from the baseline; two re-runs restored the recorded `exactRestore=14 dynamicRestore=1 issues=0`, so it is timing flake, not a regression.
- Deliberate scope note: the v22 band element was **not** reintroduced. At L0 its fill differs from the page ground by two to four RGB steps and is imperceptible, so only its closing rule was implemented, on `.article-approved-content` at content width rather than full bleed. Reinstating a true full-bleed band would require a new wrapper element in `ArticleFeatureMeta.vue`; that remains open if the user later wants the rule to run edge to edge. See git for code-level diff details.
- Follow-up round: the user reported that the archive card metadata was unreadable because `约 2 分钟` carries no unit label. The compact rows rendered `作者 / 约 N 分钟 / N 浏览` with no noun for the duration. `check-public-pages.mjs` freezes these strings verbatim, so the assertions were migrated first and confirmed red, then the component changed: `约 N 分钟` → `阅读约 N 分钟`, `N 浏览` → `N 次浏览`, `N 点赞` → `N 次点赞`, `N 评论` → `N 条评论` (matching the established `ArticleComments.vue` unit), `N 收藏` → `N 次收藏`. The label is deliberately ordered `阅读约 …` rather than `… 分钟阅读` because `12 分钟阅读` is a banned design-fixture term in `forbiddenArticleFixtureTerms`; the chosen order never forms that substring. A runtime probe asserts every card matches `阅读约 \d+ 分钟` and `\d+ 次浏览`, contains no `分钟阅读`, and that the longer copy does not push the metadata past the action: footer measures meta right `439` against action left `451`, still on one shared line with no overflow.
- Known remaining inconsistency, intentionally not changed: the bare `约 N 分钟` still appears in `ArticleFeatureMeta.vue` (related-story line and fold reading rows) and in `ArticleArchiveCardGrid.vue` on `/articles/archive`. Only the region the user pointed at was migrated; unifying the other three is a one-line change per site plus the same contract migration, pending the user's call.
- Final round on this topic: the user then withdrew reading duration entirely, reasoning that a long estimate deters readers before they open an article. It is now removed from every discovery surface — the fold's featured stat block, the two related-story cards, the five reading-stack rows, the six discovery cards, and the twelve `/articles/archive` cards. The article detail route never displayed it. All three components dropped the `readingMinutes` prop, both pages dropped the binding and the derived adapter, and `estimateArticleReadingMinutes` plus its private helpers were deleted from `utils/articleArchive.ts` as dead code once no consumer remained. Unit tests move `7 → 6`: the two duration derivation cases are deleted and one negative case asserts the export no longer exists. Contract handling: three negative locks now fail the gate if `readingMinutes` reappears in `ArticleFeatureMeta.vue`, `ArticleArchiveRail.vue`, or `ArticleArchiveCardGrid.vue`, and `pages/articles/archive.vue` may not import `estimateArticleReadingMinutes`; the `check-user-module` marker `readingMinutes(article)` was swapped one-for-one to `viewCount(article)`, which the card already renders. A runtime probe over three themes and both routes reports `200`, six and twelve cards, and zero occurrences of `分钟`.

## Commit state

- Verified immediately before staging, all fresh: Article unit tests `6/6`; `check:public-pages`, `check:user-module`, `check:front-layout-layering`, `check:preview-images`, `check:breakpoints`, `check:visual-blocked-source`, and full `pnpm run check` (which includes Nuxt typecheck, so no orphaned prop or import survives) all exit `0`; `check:loading-skeleton` remains exactly the eight recorded `pages/armor-sets/[id].vue` findings; `git diff --check` exits `0`. Runtime: `/articles` and `/articles/archive` return `200` and `/articles?page=2` still returns a real SSR `302` to the archive.
- The three rounds are committed together because they interleave in the same files; splitting them would require hunk-level staging and would produce intermediate commits that do not build. The excluded paths remain unstaged exactly as inherited: the two design HTML files, `entries/2026-07-27-detail-pages-redesign-hifi.md`, both `docs/superpowers/plans/**` files, the untracked `entries/2026-07-29-articles-hifi.md`, and every `reports/**` artefact.
- Open follow-up, analysed but not implemented: the fold's vertical rhythm. `.article-approved-stage` is pinned to `calc(100svh - 203px)`, `.article-approved-lead` gives the whole surplus to its middle row, and `.article-lead-body` uses `align-content: center` with a fixed `18px` gap, so all spare height becomes two outer voids while the copy stays packed. Measured at `1440` width: the flow content is a constant `315px` at every viewport, while the surrounding void grows `166px → 748px` between `800` and `1440` viewport heights, i.e. from `35%` to `70%` of the middle row. The proposed fix caps the stage height, makes the body gap fluid, and gives the below-fold section a real inter-block gap; `grid-template-rows: 46px minmax(0, 1fr) auto` and the lead padding are contract-frozen and stay untouched. Awaiting the user's choice between that and simply dropping the viewport lock.
