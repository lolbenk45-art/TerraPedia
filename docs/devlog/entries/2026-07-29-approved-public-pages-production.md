# 已确认公共页面正式接入

## Status

`active`

## Goal

将已确认的物品详情、NPC 详情与文章页高保真稿接入 `front-nuxt` 正式路由，保持现有公开接口和三主题令牌契约。

## Scope

- 物品：`pages/items/[id].vue`，以 `item-complete-page-terra-blade-approved-v2.html` 为定稿来源。
- NPC：`pages/npcs/[id].vue`，以已确认 NPC 样本为定稿来源；按接口能力渲染居住、到访、掉落和商店模块。
- 文章：`pages/articles/index.vue`，以 `articles-story-led-v22.html` 的“精选首屏 + 高密度文章库”结构为定稿来源。
- 实施计划：`docs/superpowers/plans/2026-07-29-approved-public-pages-production.md`。
- 保真重建计划：`docs/superpowers/plans/2026-07-29-public-pages-fidelity-rebuild.md`；该计划替代旧计划的“在共享 detail-layout 上叠加版式”实施方向。

## Boundaries

- 不修改 API、数据库、爬虫、`reports/` 或未确认的 `docs/design/terrapedia-*-detail-hifi-v1.html`。
- 不把当前旅商接口的 35 项可用商店数据称为完整 Wiki 货池。
- 不处理合成树数量污染、商人节日图像错配或后端/数据库数据修复；这些保持独立立项。

## Validation plan

- 纯展示派生先通过 Node 单测红绿验证。
- 运行 detail/public-page/visual-system 定向契约与 `pnpm run check`。
- 在 1440px 和 390px 实测物品、城镇 NPC、旅商 NPC 与文章列表。

## Result

- 已接入物品、NPC 与文章正式页面展示层：物品和 NPC 复用现有实时详情字段及共享 detail-layout；文章以实时已发布列表的首条作为精选，余项进入高密度资料库。
- NPC 模块按能力决定是否展示；旅商的商店区明确为当前接口可用数据，不声明为完整 Wiki 货池。
- 新增纯展示 helper 及红绿单测；新页面域样式只使用既有三主题语义令牌和冻结响应式断点。See git for code-level diff details.
- 本轮按确认稿进一步收敛：实体页手机端保留缩略图与事实并列；商人/旅商以商店作为首个工作模块；旅商身份优先于错误的城镇标记；文章精选 fold 改为标题、编号和元数据优先，封面仅作辅助识别。See git for code-level diff details.
- 物品详情的关系数据在 SSR 中已恢复：在第一个请求前捕获公开 API 客户端，避免异步边界后的 Nuxt 上下文丢失使可选关系被吞为空值；泰拉刃现显示真实合成树和制作用途。See git for code-level diff details.

## Validation

- `cd front-nuxt && node --test tests/unit/detailPagePresentation.test.mjs`：3/3 通过，覆盖异步边界后仍可使用已捕获 API 客户端、商店优先与旅商优先于城镇标记。
- `cd front-nuxt && pnpm exec nuxt typecheck`：通过；环境已有重复 auto-import 与 Node 弃用警告不影响退出码。
- `cd front-nuxt && pnpm run check`：通过，包含公共页面、详情布局、视觉令牌、断点与 `nuxt typecheck` 全量契约；Chromium DBus/GPU 与 Node 弃用提示不影响退出码。
- 运行态：`/items/757`、`/npcs/17` 和 `/articles` 在 1440×1000 与 390×844 均为 `overflowX: 0`，无 console/request errors；泰拉刃 SSR 已显示真实合成树与用途。

## Residual risk

- 合成树恢复后其数量、版本命名和递归深度仍完全以既有 API 为准；本任务不修正数据质量。
- 已明确排除的合成树数量污染、商人节日图像错配、旅商完整 Wiki 货池仍待独立任务。

## Follow-up

- Owner: data/API follow-up only for data质量问题；当前 UI 的关系装配已恢复。

## Review

- Reviewer: Codex read-only implementation review.
- Scope: approved item/NPC/article production integration and its presentation contracts.
- Findings resolved: NPC 的城镇入住与旅商到访模块现在按能力实际渲染；文章库不再宣称未经排序契约保证的“最新”；移除了禁止的装饰圆环；high-fidelity 与 production devlog 状态已对齐。
- Re-review: focused presentation test、detail/public-page/visual/breakpoint contracts和完整 `pnpm run check` 均通过。

## Fidelity rebuild direction

- 用户指出运行态与定稿仍有明显结构差距。已冻结新的逐区验收计划：以运行时截图对照定稿，而不是以 CSS 标记或通用详情卡片通过为完成条件；物品先重建配方层级，NPC 重建条件分段商店，文章重建 v22 完整 fold 与 archive shell。See plan for implementation details.
- 计划 review 发现旧版会与既有 detail/public-page/loading/preview/article-layering 合同冲突，并遗漏共享布局、断点白名单、数据降级和截图工具边界。保真计划已修订为“合同先红后绿、每 slice 跑完整 `pnpm run check`、保留共享兼容壳、新组件拆入 `components/detail/`、串行执行”的版本；实施前先执行 Task 0 基线与 SHA 锚定。
- 第二轮 review 已把存量半成品、SSR 关系加载修复、合同迁移权限、gitignored 设计稿、截图落盘和双计划归属收紧为明确边界：先做存量裁定表和用户授权的回滚锚点，再按“Articles → NPC → Item”执行；合同只能精确迁移或增加，不能放宽匹配；全量与补充守门在每个 slice 收尾执行。See fidelity rebuild plan for the executable checklist.

## Task 0 baseline

- WIP rollback anchor authorized and created: `2303e0f3` (`chore(ux): checkpoint public page redesign WIP`). It contains only this task's route, contract, test, SSR-fix, plan, and devlog paths; unapproved detail HTML, old entry, `.shot.mjs`, and `reports/` remain unstaged and user-owned.
- Approved local baseline fingerprints: item v2 `.superpowers/brainstorm/339185-1785149609/content/item-complete-page-terra-blade-approved-v2.html` = `0ea553685475d6371945c747383373d4350427921206c5e6c60a45cc5040ca33`, 79,207 bytes, mtime `2026-07-27 22:43:14 +0800`; merchant v1 `.superpowers/brainstorm/339185-1785149609/content/npc-complete-page-merchant-v1.html` = `b9c8bebae652c9fdf52bde09c5513564f8147722384556a08e9a3c505ca0d9a6`, 73,648 bytes, mtime `2026-07-27 23:45:38 +0800`; articles v22 `.superpowers/brainstorm/3542442-1785229764/content/articles-story-led-v22.html` = `1078d92c2b5dbc4b25c9331df864258f9536e80090136b4c376201bbfe85c6f3`, 37,825 bytes, mtime `2026-07-29 07:21:17 +0800`.
- Gate baseline: `pnpm run check` exit 0; `check:visual-blocked-source` exit 0; `check:loading-skeleton` exit 1 with exactly eight pre-existing missing armor-set markers and none on items/NPC/articles. Runtime-only gate baseline remains pending the dedicated `localhost:5176` server.
- Live article baseline: `/api/articles?page=1&limit=1` returns pagination total 24, so the 12-record fold/archive acceptance is data-reachable. `items/757` is 泰拉刃 and `npcs/17` is 商人.
- Existing-work disposition: page routes, `detail-pages-redesign.css`, `index.css`, and two contract scripts = rebuild-on-top; `detailPagePresentation.ts` and its test = migrate-then-delete; `usePublicApi.ts` and `usePublicItemDetail.ts` = freeze-read-only; two unapproved detail HTML files, `entries/2026-07-27-detail-pages-redesign-hifi.md`, closed `entries/2026-07-29-articles-hifi.md`, `.shot.mjs`, and `reports/` = user-owned untouched, except `.shot.mjs` will be deleted after its existing local purpose is recorded.
- Contract disposition: the currently widened item/NPC layout regexes are recorded as pre-existing debt. The owning slice must restore exact class assertions or obtain an explicit user exception; no slice may treat their widened form as its ratchet baseline.
- Named-region inventory: item = specimen strip/identity-and-stage hero/core-facts rail/合成链/采集清单/可用于制作/其他资料/related facts rail; all consume the existing item detail bundle and compact empty state when absent. NPC = specimen strip/portrait-condition-stat hero/商店 condition bands/幸福度与居住/其他资料/facts rail; all consume the existing NPC aggregate and traveler uses only current live stock. Articles = mast/feature story/reading stack/archive tools+dense rows/popular rail/topic grid; feature/list/rows use published-page fields, while unavailable topic/popular aggregates use the plan's explicit compact labels.
- Runtime baseline restored under the tracked local-stack script. This worktree has isolation slot `3`; invoking it with `TERRAPEDIA_FRONT_PORT=5173` yields the plan-required front `http://127.0.0.1:5176`, backend `http://127.0.0.1:18191`, and admin `13004`. `/articles`, `/items/757`, `/npcs/17`, and `/npcs/368` all return `200`; no data refresh was run.
- Same-server runtime-gate baseline: `check:light-theme` exits `1` on pre-existing cross-route light-theme contrast findings (including `/articles` controls and unavailable historical slugs); `check:typography-spacing` exits `1` waiting for `/search ready`; `check:crafting-wiki-structure` exits `1` on the independent `/crafting` graph ordering, icon-size, and expanded-child assertions. These routes/contracts are not changed by this plan; Task 4 compares the three commands against this baseline rather than repairing them here.
- Named-region/data/degradation inventory, extracted from the three SHA-pinned references: Item `specimen strip` is sample navigation and is omitted in production; `identity-and-stage hero` consumes `PublicItemDetail` name/image/rarity/game-period/description and stat rows (keep loading/not-found state); `core facts rail` uses item id/stats/coverage/real related links (omit empty cards); `合成链` uses `PublicItemDetailBundle.recipeTree` through `buildCraftingRecipeModel` (compact `资料整理中` state); `采集清单` flattens that model (omit if absent); `可用于制作` uses `recipeUsages` (compact empty); `其他资料` uses sources/price/equipment/loot facts (omit unavailable capability); the related rail never invents a completeness score. NPC `specimen strip` is omitted; `portrait-condition-stat hero` uses `PublicNpcAggregate.npc`, wiki assets, spawn/identity/stats (keep loading/not-found); `商店 condition bands` uses `shopEntries` item/price/conditions (omit with no live stock; traveler stays current-stock scoped); `幸福度与居住` uses actual residence/preference relations only; `其他资料` and `facts rail` use only loot/money/traceable facts. Articles `mast` uses pagination/current-page authors (remove fake topic count); `feature story` and `reading stack` use current-page `UserArticle` title/summary/cover/author/date/view/favorite; `archive tools+dense rows` uses the remainder with no padding; `popular rail` is current-page ranking labelled `本页阅读数据`; no topic aggregate exists, so its compact state is `资料整理中` without counts.
- Article capacity ruling: 24 published rows make the twelve-row target reachable. With 6–11 rows, render one feature plus up to five reading-stack entries and place only the remainder in the archive rail; with fewer than six rows or any keyword filter, use archive-only; zero remainder means no archive rail. Pagination stays deep-linked; changing to 12/page deliberately accepts page-boundary drift.
- Reuse mapping: `buildCraftingRecipeModel` owns variant ordering, active recipe selection, stable keys, image fallbacks, quantities, stations, and alternatives; `craftingRecipeCompact.ts` owns compact tree normalization; `recipeHierarchyGraphRenderer.ts` owns rendered graph semantics. Item work only adapts these views. NPC work extracts the existing `shopConditionsLabel` / `shopConditionSummary` / `shopGroupKey` implementation from `pages/npcs/[id].vue`, retaining `safeNpcDisplayText` and stable ids. The article SSR key already includes page and keyword; Task 1 changes its limit and projection only after red tests cover query/page behavior.
- `utils/publicCopy.ts` now owns `PUBLIC_COPY_UNDER_CONSTRUCTION`, `PUBLIC_COPY_PAGE_READING_DATA`, and `PUBLIC_COPY_CURRENT_AVAILABLE_SHOP_DATA`; `tests/unit/publicCopy.test.mjs` was red for missing exports then green. Route consumers move to these constants in their owning slices so literal-location contracts can migrate exactly rather than be widened.
- `front-nuxt/.shot.mjs` was recorded before deletion: a 952-byte untracked, machine-path-bound one-off Playwright screenshot script (mtime `2026-07-28 18:53:13 +0800`) that captured one URL, overflow, and console/request failures. The tracked `audit-shoot.mjs` now provides the same capability with route/viewport overrides, so this plan removes the obsolete helper rather than retaining permanent dirty state.
- Ignored screenshot evidence lives at `front-nuxt/tmp/shots/2026-07-29-public-pages/`. `audit-shoot` at `1440×1000` and `390×844` returned `200`, no horizontal overflow, and no captured console/request failures for all approved probes: `articles--desktop.png` `ae961ab9f0bd06543d70a7b46b463d01ba538a44e810846c426cdace3b7191d0`; `articles--desktop-full.png` `bf3d9ca6c09fb053cb63e30b184b54a470f66f9cd6fc071b16e500332bb7448b`; `articles--approved-mobile.png` `a430824a35fb047fa05df118c9c6a856537f3bddb0ccbdd35a6bc4a949364a56`; `articles--approved-mobile-full.png` `f22a67acd118036c4353feddb5f69ff3acaef56bbde47b6e8f427950c451b727`; `item-terra-blade--desktop.png` `abc1f36a5ecdbd6da65b00af8e67e62201194e798bcc862a1ee9c3a4db7c82db`; `item-terra-blade--desktop-full.png` `d64e7c388372c88880130075803d3cb5298a743559fc95185336a3d905a8ed46`; `item-terra-blade--approved-mobile.png` `b2622f9f3b423b074129e5a6ceb99200e45bf1507e5a8cabc3e6f879a39fbdff`; `item-terra-blade--approved-mobile-full.png` `82b915e91749c5cdfb6201f05d9ffd4183cc62b5942030d08f5191f9825f7a0c`; `npc-merchant--desktop.png` `1ef537d25d95062e6293a95bf54cebe9eb7dbb5ee60d59350bfcf170f2111c7e`; `npc-merchant--desktop-full.png` `a7992aa7ad8a3a2c4409317801caea0d1d1bb3f0da217a469ade45b968cabb00`; `npc-merchant--approved-mobile.png` `7dbc3f74685343be86c61a2e5d2b68e3fa52b9f274a843d809a3a8926a509cf4`; `npc-merchant--approved-mobile-full.png` `3f2245bb7f44fcec40c8a8b96e72e85c3f487d7a321b3bb0949a51b071ac8445`; `npc-traveling-merchant--desktop.png` `8274587e9bf25d0fd6c77d1f841767cc8c45b6cedcd1cb55d6e8f92777455027`; `npc-traveling-merchant--desktop-full.png` `9c66560eaa530ceb91933f20f48da488a37405da075f02100bb99df9ed11f208`; `npc-traveling-merchant--approved-mobile.png` `07e24576db0a3acacb13bd524da349497633949d481789489b0fd38b9a3c118f`; `npc-traveling-merchant--approved-mobile-full.png` `09c1bd4fac70e5d0617a035934630368833bee76950eaa9cc588fbb316004870`.

## Articles contract ratchet

- Expected-red whitelist before the Article move: `check-public-pages.mjs` currently has one exact page-file assertion each for `article-mast`, `article-featured-story`, `article-featured-story__index`, `article-library-shell`, and `article-archive-row`. Destination assertions are added first (three to `components/article/ArticleFeatureMeta.vue`, two to `components/article/ArticleArchiveRail.vue`); until those components exist, the only permitted red is five `missing approved article region marker` failures with reason `missing implementation`. After the move, old page-location assertions are removed one-for-one; semantic assertion count remains five. The shared main-shell, balanced loading-grid, inline loading skeleton, and `articleLoadingSlotCount` assertions stay in `pages/articles/index.vue` with count one each.
- Relocation-phase expected red: once the five named regions leave `pages/articles/index.vue`, the only permitted additional red is its five former marker messages, each with reason `migration`; the next edit retargets those five assertions to the already-green component destinations without changing their count or text.
- The relocation probe also exposed two inherited page-only assertions (`<h1` exactly once and `<TerraBreadcrumb`), both now in `ArticleFeatureMeta.vue`. They are added to the same destination assertion set and the page's semantic scan is retargeted to that component; the observed three messages are classified `migration`, not a loss of heading or navigation semantics. No other red was observed.
- The Articles slice full-gate run exposed one omitted contract owner: `check:user-module` still required the relocated article link/title/summary, preview cover/fallback, and `loading="lazy"` literals in `pages/articles/index.vue`. The deterministic red is recorded before changing the contract. Root cause: the script has two page-only article-list contracts and no explicit route-component migration branch, unlike `check-public-pages`. Resolution is limited to exact destination assertions in `ArticleFeatureMeta.vue` and `ArticleArchiveRail.vue`, while the data fetch/pagination requirements remain page-local; assertion count must increase and the forbidden literal checks remain intact.

## Articles slice

- The v22 route now requests 12 live rows and projects unfiltered page one into exactly one featured story, five reading-stack entries, and six archive rows. Keyword queries and fewer than six rows render archive-only; the 6–11 range leaves only its true remainder in the archive. Topic aggregation remains the explicit `资料整理中` compact state, and current-page ranking is labelled `本页阅读数据`. See git for code-level diff details.
- Ratchet result: `check-public-pages` retains the five approved named-region assertions and adds exact destinations plus the relocated heading/breadcrumb semantics. The unplanned `check:user-module` migration moves nine former page-only UI requirements into twelve exact component requirements: article route assertion count rises from 27 to 30; fetch, pagination, error, loading, preview resolver, author helper, and pagination controls remain page-local. No regex was widened and forbidden-placeholder checks remain in place.
- Validation: `node --test tests/unit/articleArchive.test.mjs` 5/5; `pnpm run check:public-pages`; `pnpm run check:front-layout-layering`; `pnpm run check:user-module`; and full `pnpm run check` all exit 0. `check:visual-blocked-source` exits 0. `check:loading-skeleton` remains exit 1 with exactly the Task 0 baseline's eight failures in `pages/armor-sets/[id].vue`, with no new item/NPC/article marker.
- Runtime evidence under `http://localhost:5176`: `audit-shoot` recorded status 200, no captured console/request failure, and no horizontal overflow for `/articles` at 1440×1000, existing 375×812, and approved 390×844. Screenshot SHA-256: `articles--desktop.png` `691d68125b42c29459a4d330c73f4f1bf674dac6d1853dd67d3f8a4c4fb6310d`; `articles--desktop-full.png` `8d3a438929248c9642ee5c61d3abe3a7ae468f2c3b28e7ba89b38afbfe53ce74`; `articles--mobile-existing.png` `3c911ac13ac4fdf64167eae0afb17adaf1b8668af8d6b190a1fc49c181bc491c`; `articles--mobile-existing-full.png` `ffde2b5003a10c727d585d735c774f7d83dd43e2661b9dd1cf254140e070fa46`; `articles--mobile-approved.png` `7292b19c4488d342acd0c33114efc57c66e1a71510f7fe560465e93c57cdbebe`; `articles--mobile-approved-full.png` `8d422f1bc55f0b85ca5481fe04fbdb494e6fa68ddc166b75662d31bb4101a132`.
- The three server-dependent cross-route gates remain deferred to Task 4 by plan: their known same-server baseline failures are unrelated to this route. Next serialized owner is the NPC slice.

## NPC contract ratchet

- The first NPC utility-extraction probe intentionally runs `check:public-pages` red. Its exact six-message whitelist is: missing `特殊条件` from the aggregate-data marker list, and missing `shopGroupKey(entry)`, `常驻出售`, `阶段出售`, `地点出售`, and `解锁出售` from the shop-band marker list. Reason: migration of the pure, player-safe grouping semantics into `utils/npcShopBands.ts`; no template or fallback marker has moved yet. The next contract edit adds those six exact utility destinations before any component relocation.
- Component-relocation probe: `check:detail-layout` reports its page-only compact-grid count falling from five to three; `check:public-pages` reports the page-only shop summary, price-image, group-loop, and visible/remainder markers. Those six contract reds are migration-only and must be retargeted one-for-one to `NpcShopBands.vue` or `detail-pages-redesign.css`. The same probe also reports generic player-copy scanner matches for `Internal` and `fallback`: both originate from a parent-template inline key and prop binding, not rendered data. Their corrective action is limited to a page-script `shopEntryKey` binding and neutral prop naming; it must not weaken the scanner.

## NPC slice

- `utils/npcShopBands.ts` now owns the safe condition label/summary, ordered band projection, selected-band filtering, and archive-module decision. `DetailNpcShopBands.vue` renders the capability-first condition bands; merchant keeps residence support and traveler shows `当前可用商店资料`, never a complete stock-pool claim. `detailPagePresentation.ts` and its superseded test were removed after both consumers migrated. See git for code-level diff details.
- Ratchet result: six utility markers, compact relation-grid counting, shop toolbar, visible/remainder lists, price-image safety, and component CSS all have exact new destinations. The two template scanner reds were fixed by moving the stable key to script and using a neutral component prop; scanner rules were not changed. A stale dev server initially did not index the new directory-prefixed component; the authoritative Nuxt name is `DetailNpcShopBands`, and a local-stack restart confirmed it resolves without hydration mismatch.
- Validation: `node --test tests/unit/npcShopBands.test.mjs` 4/4; `pnpm exec nuxt typecheck`; detail/public-page/preview/breakpoint contracts; and full `pnpm run check` exit 0. `check:visual-blocked-source` exits 0. `check:loading-skeleton` remains exactly the eight known armor-set failures. Fresh audit is 200/no overflow/no captured console or request failure for merchant and traveler at 1440×1000 and 390×844. Screenshot SHA-256: merchant desktop `0f85ce488f33d42e1ef4292ced2953168043697b89aabfe4168aa49ad86e4058`, desktop-full `c46110da2070311a68daa5cd856516b33a817631a71aa219da27f9a86958eebf`, mobile `ef35081be7321b2ee91692e68b9df6936d69432fb4fb7e1c9dd0fd0747708a9b`, mobile-full `e597b57ce5a6aeed652bb2856e3e407404cd600f13302c236c1b2b13976e7cb6`; traveler desktop `0439217581428c2b98ec8e3bb7eb7ce1e3c57d680b528644a16664313fcf8546`, desktop-full `eb776107a7a929d89809fa2d19f0c39856e11cfc41136c442ec4a5169ae1f714`, mobile `e73f66d03ca3dfe174c2bfcd815e5ee49f6b30387adf3ae8f71829b11c16e114`, mobile-full `c2cb97705496ea6c0956dd5e99910e6070fba1578859d55251dd53610c5c25bd`.

## Item contract ratchet

- The Item slice has started from the Task 0 WIP anchor. The `publicApiFetcher` regression remains green before item recomposition (`2/2`), and the new `itemRecipeHierarchy` projection was red for the missing module then green (`3/3`). It accepts only the existing `buildCraftingRecipeModel` output, retaining that helper's variant order, quantities, stations, alternatives, fallback assets, and cycle protections.
- Expected-red whitelist for the first hierarchy relocation: `check:detail-layout` has exactly one failure for the old page-file `item-recipe-summary-module` class list; reason: the approved hierarchy class is added to the compatible module wrapper. `check:public-pages` has exactly one failure for page-local `制作路线`; reason: that reader-facing region migrates to `components/detail/ItemRecipeHierarchy.vue`. `check:preview-images` stays green. `check:loading-skeleton` remains the eight Task 0 baseline failures in `pages/armor-sets/[id].vue` only; it is neither a new Item failure nor in scope to repair.
- Green-step disposition: restore the item detail-layout assertions to exact final class lists (no optional-class regex), relocate the one `制作路线` assertion to the new component, and add exact assertions for `buildCraftingRecipeModel` → `buildItemRecipeHierarchy`, L3/L2/L1/OUT bands, quantities, stations, alternatives, and compact empty state. No wildcard regex or assertion-count reduction is allowed.

## Item slice

- `/items/757` now uses `DetailItemRecipeHierarchy` as the first content region: the existing crafting model is projected as L3 → L2 → L1 → output bands, with live quantities, alternate materials, station chips, a compact no-data state, and an unchanged expandable `RecipeSummaryCard` compatibility summary. The right rail now begins with the real stat rows as `核心属性`, followed by the live coverage facts; it no longer substitutes the approved fact rail with a single generic overview card. See git for code-level diff details.
- Ratchet result: the two expected reds were eliminated by exact relocation. `check-detail-layout-contract.mjs` now pins the final item shell, grid, hierarchy module, and coverage-rail class lists; it does not retain an optional-class regex for the Item route. `check-public-pages.mjs` keeps the `制作路线` promise at the hierarchy component and adds exact page/component/utility markers for shared-model adaptation, four stage bands, quantities, alternatives, stations, and explicit empty state. The three stage constants are asserted in the projection utility rather than falsely requiring the consumer component to duplicate them.
- Validation: `node --test tests/unit/publicApiFetcher.test.mjs` 2/2 before the slice; `node --test tests/unit/itemRecipeHierarchy.test.mjs` 3/3; targeted detail/public-page/preview/breakpoint/type checks passed; full `pnpm run check` passed. `check:visual-blocked-source` passed. `check:loading-skeleton` remains exactly the Task 0 baseline's eight failures in `pages/armor-sets/[id].vue`, with no Item/NPC/article violation.
- Runtime evidence under `http://localhost:5176`: `audit-shoot` at 1440×1000 and 390×844 returned `200`, `hasHScroll: false`, and no console/request failure. Current screenshot SHA-256: `item-terra-blade--desktop.png` `31ad0c42db94cf97e83d0d6f3020d33a4517d7da618f75050b7c56d4bc65d829`; `item-terra-blade--desktop-full.png` `d78cbc1584bd9b9b15b51e052eb596330014032f76443114733bcc3c6c920846`; `item-terra-blade--mobile.png` `cf6fd24626532a93b50bb89c533a6ed84a06ef79135a42ac8c6665ff541d00c8`; `item-terra-blade--mobile-full.png` `7d3daad9a33fcc3703147b0e6784b3936466334037d894e99cf4c0eca28de4e8`.

## Item direct-transplant reset

- User acceptance supersedes the preceding Item visual result: the prior hierarchy projection and safety work remain reusable, but the loaded body is rebuilt from the approved DOM instead of receiving another styling pass on the legacy detail skeleton. This reset does not reopen the completed Articles or NPC slices.
- Confirmed body boundary: production global navigation and `TerraBreadcrumb` remain; mock top bar, sample strip, mock breadcrumb, and design note stay out. The existing production container width remains, while the approved hero proportions, two-column body, primary/secondary card hierarchy, sticky tool rail, chain bands, tally, usage, and compact facts become the live structure.
- Confirmed compatibility boundary: preserve favorite/history/SEO/loading/error/image fallback, SSR relation capture, price-coin images, source safety filtering, recipe-usage expansion, and all existing data modules. No API/database/crawler/report work and no sample-specific hard-coded content. Missing core fields use compact `资料整理中`; absent optional modules are hidden.
- Live adaptation probes are fixed at `/items/757` (泰拉刃), `/items/22` (铁锭), and `/items/95` (燧发枪). Read-only API check confirms relation counts: 757 has 0 sources / 1 usage; 22 has 27 sources / 17 usages; 95 has 3 sources / 1 usage. These three routes cover crafting-first, crafting-plus-source, and shop-source shapes without mock data.
- Owned write paths for this reset: `pages/items/[id].vue`, `components/detail/ItemRecipeHierarchy.vue`, `utils/itemRecipeHierarchy.ts` only if the approved band/tally projection needs additional fields, `tests/unit/itemRecipeHierarchy.test.mjs`, `assets/css/domains/detail-pages-redesign.css`, the Item sections of `check-detail-layout-contract.mjs` and `check-public-pages.mjs`, this entry, and the active fidelity plan. `useDetailLayout.ts`, `usePublicApi.ts`, `usePublicItemDetail.ts`, `RecipeSummaryCard.vue`, unapproved detail HTML, unrelated devlogs, and `reports/` remain untouched.
- Expected-red whitelist for the direct-structure contract addition: exactly eleven page markers (`item-approved-body`, `item-approved-hero`, `item-approved-plinth`, `item-approved-ident`, `item-approved-metrics`, `item-approved-layout`, `item-approved-column`, `item-approved-card`, `item-approved-rail`, `item-approved-anchors`, `item-approved-related`) and six hierarchy-component markers (`chain`, `band`, `band-body`, `nodes`, `tally`, `tally-grid`) are initially missing. Their reason is `missing implementation`. Existing safety and data assertions must stay green; any additional red stops implementation.
- Structure-ratchet baseline before this addition: zero exact direct-transplant markers and seventeen proposed destinations. The existing seven Item detail-layout markers plus public data/safety assertions remain in force until their exact destination is implemented; later relocation may change file ownership but cannot broaden a regex or reduce the total semantic assertion count.
- Observed red matches the whitelist exactly: `check:detail-layout` reports only the eleven page markers and `check:public-pages` reports only the six hierarchy-component markers. No inherited data, safety, preview-image, SSR, or interaction assertion failed at this phase.
- Fresh 1440×1000 geometry comparison after the direct transplant found three production-only cascade collisions, not missing approved DOM: global `.hero` leaves the Item hero at the homepage `790px` minimum instead of the approved `282px` intrinsic height; the retained `tp-detail-module` compatibility class turns approved cards into a gapped grid; global `.node-meta` adds an 18px margin, 14px padding, and divider to every crafting subtitle. The next detail-layout red whitelist is exactly three new CSS destination assertions under `.item-approved-body` for hero intrinsic height, inert card compatibility layout, and node metadata reset. Reason: `missing implementation`; assertion count increases by three and no existing assertion moves or widens.
- That three-assertion probe went red exactly as listed and is green after scoped resets. Fresh geometry is hero `307px` and first crafting node `48px` (previously `790px` / `81px`). The remaining approved-body cascade probe adds four exact assertions: suppress the homepage hero `::before`, restore the plinth/preview intrinsic sprite sizing at desktop and mobile, keep the real favorite target at the shared touch size, and neutralize the legacy coin-pill/icon sizing inside approved price cells. Expected red is exactly four `missing implementation` messages; no data or interaction assertion moves.
- The four-assertion probe also went red exactly and returned green. Computed-style inspection then showed two higher-specificity remnants: homepage `grid-template-rows: minmax(0, 1fr) auto` still creates an empty hero row, and `public-layout.css` keeps a more-specific `!important` full-frame sprite rule. The final cascade whitelist is exactly two new assertions for a one-row approved hero and the 108px approved desktop sprite destination; both are `missing implementation`, with no existing marker weakened.
- The next Item projection red is bounded to new unit expectations for role-based stages: direct inputs remain L1, deepest craftable descendants become L2, their real leaf inputs become L3, every normalized `childRecipes` option participates in an explicit one-to-one fork, and procurement retains deduplicated leaves from every live branch without item-id/name special cases. The existing depth-only helper is expected to fail those assertions; `publicApiFetcher` and all page safety contracts must remain green.
- The next Item interaction ratchet adds one exact `check-public-pages.mjs` destination requirement for a single usage row to select `NuxtLink` only when `usage.href` exists and otherwise render a non-link element. Its only expected red is the current `:to="usage.href || route.path"` self-link fallback; no favorite, expansion, SSR, preview-image, or source assertion may move or widen.
- Observed red matches that whitelist: the focused hierarchy suite has exactly three new behavior failures (role stages, explicit normalized child-recipe fork, and all-branch leaf procurement), while its variant/empty cases and `publicApiFetcher` 2/2 tripwire remain green. `check:public-pages` has exactly the one inert-or-real usage destination failure; no unrelated route or safety message appeared.
- The role projection and inert-usage green step now passes the combined focused suite 7/7, `check:public-pages`, and Nuxt typecheck. Fresh production/reference geometry shows the hero is already within 4px (`286px` versus `282px`); remaining primary-body inflation is localized to an outer legacy compatibility wrapper (17px inset), station copy inside node metadata (L2/L1 wrapping), and a one-row usage summary that is only operationally needed when six-row overflow exists.
- The next bounded red adds: one utility expectation that projected node metadata is derived only from normalized material titles/quantities, not appended station copy; exact detail-layout destinations for the compatibility classes on the approved primary card plus a forbidden nested wrapper; and one public-page marker requiring `recipe-usage-summary` only under `hasRecipeUsageOverflow`. Expected failures are those destinations only. The compatibility classes and `RecipeSummaryCard` remain present, and the six-row expand/collapse behavior is unchanged.
- That red/green cycle matched exactly: the metadata test is green, public pages are green, and detail-layout produced only the old-wrapper migration red before its exact binding moved to the primary card; typecheck remains green. Fresh geometry is primary x/width `32/1058`, L1 `111px` versus approved `108px`, and one-row usage `167px` versus `161px`. The remaining L2 wrap is explained by implicit `x1` pills and repeated per-ingredient quantities, not missing data or CSS width.
- The next focused red adds exact component markers that L2 suppresses only implicit `x1` quantity pills while L1/L3/OUT retain quantities, plus a utility assertion that a frontier recipe with three or more ingredients uses their normalized names without repeating quantities. No material, route, station, or branch is removed; this is a compact-display projection only.
- That compact projection is green (hierarchy 6/6, public-pages, typecheck). Runtime now places the fork, grass blade, volcano, and Excalibur on the first L2 row; only Chlorophyte Bar wraps because the exact live names exceed the 900px node track by about 52px. The next detail-layout red is one exact CSS destination limiting only direct L2 intermediate nodes to `144px`; nested fork nodes and every other stage remain untouched.
- The `144px` hypothesis was falsified by runtime geometry: the direct-node row still totals `911px` inside an actual `899px` content track, so Chlorophyte Bar remains on a second flex row and L2 grows to `201px`. The corrected single-variable hypothesis is `136px` for the two bounded direct nodes (`895px` total); the exact CSS contract changes red before the value changes, then geometry must prove a single row. No additional selector or content change is authorized in this correction.
- The corrected `136px` value is green and proven: all five direct L2 slots occupy one row, L2 is `129px` versus approved `118px`, and desktop has no overflow. Mobile comparison then exposed a separate pre-existing `max-width: 640px` divergence from the approved CSS: it changes band tracks to one column, turns nodes/forks into forced grids, removes the flow offset, sets every node to full width, and collapses the tally to one column. The next detail-layout red consists exactly of those five forbidden mobile overrides plus the tally override; implementation removes only those declarations so the approved base two-track/flex layout and the existing 900px two-column tally inherit at 390px.
- The mobile chain/tally reset is green and preserves the approved left-track/flex composition at 390px with no horizontal overflow. Variant geometry then isolated a separate legacy scoped-style collision in `/items/22`: at 1440px the live source table computes to two `506px` columns instead of the approved three-column grid, and `detail-relation-meta` forces each rate into a second row, inflating every source card from the intended `92px` minimum to about `119px`. The next exact detail-layout red whitelist has three `missing implementation` messages: an Item-specific three-column source-table destination with enough specificity to beat the scoped legacy rule, a third-track rate destination, and a mobile prohibition against the two-column/rate-below-copy override. This cycle changes CSS only; all 27 live source records, copy, links, images, values, and ordering remain unchanged.
- The three source-density contract reds returned green. Runtime now computes three `336px` desktop columns and `92px` cards; `/items/22` falls from `4715px` to `3726px` desktop and from `8271px` to `7404px` mobile with zero horizontal overflow and no hidden record. The preservation probe then found a separate interaction regression in both `/items/757` and `/items/95`: the string dynamic component renders literal `<nuxtlink to="...">` elements with no `href` and `tabIndex=-1`. The established working repository pattern is `ArmorSetCard.vue`, which imports `NuxtLink` from `#components` and passes the component object to `:is`. The next `check:public-pages` red whitelist is exactly one single-usage destination failure; implementation changes only that dynamic shell and keeps the inert `span` fallback.

## Item final acceptance evidence

- The single-usage destination red returned green. Runtime now renders `/items/757` usage as focusable `<a href="/items/4956">` and `/items/95` as `<a href="/items/127">`; the no-destination branch remains an inert `span`. `/items/22` still previews six usages, expands to all 17, and returns to six on collapse. The favorite control remains a real enabled `button`, reports `aria-pressed`, and measures `198x44px` in the desktop probe.
- Three-theme data-shape matrix: all 18 combinations of `757 / 22 / 95`, `dark / morning-paper / warm-slate`, and `1440x1000 / 390x844` returned `200`, the requested theme key, zero console/request failures, zero failed images, and zero horizontal overflow. The first all-in-one run correctly exposed the backend's fixed public-read limit (`120` requests per `60s`): each Item SSR consumes nine public endpoints, so the final theme batch crossed the limit and degraded to Item-not-found. No product code was changed for this environment boundary; the invalid screenshots were discarded and the matrix was rerun in sub-120-request windows with fresh contexts.
- Full-page matrix SHA-256, dark: Terra Blade desktop `261b91acf74d667ee9307512aa8d96f725611e82a57d9d45fc705b3555c6cce2`, mobile `74c06720a0b24014a3c3fe6a94e6a846e4a8052c9251da712f02b5b366ed9122`; Iron Bar desktop `f88767e29a5da3f856857627995ead699a3ff91a1deac0b9fc809b7d6aa67fe0`, mobile `3317d4cfb57039e96481a902f8e7fdaf887b70a6f66a16114c5a3a06776e7813`; Flintlock Pistol desktop `e81a37e0ea56800ddd57dfb7bdb36f168d68780761e85f5386d8e079eb223c32`, mobile `7f7c106eca5f3ab3a8791ad88f4fed0b41224bf3155e431d5fe0cd7cff46466b`.
- Full-page matrix SHA-256, morning-paper: Terra Blade desktop `4bda7d1aac7a55f8d11ee7123167e1b24bb3e9a47c805f5512c103eea968b562`, mobile `efafaf03c021ac8094deea532c6703aa214606da51bccc3eb7c81aaa2cf1afb6`; Iron Bar desktop `bd45612df106e278d9e884990e2e692d5725efa42894db7167eef9d81e48f18b`, mobile `f5c1a99268931600854ef39e86a04ff5bf696c7db79990974494c6e4154da25a`; Flintlock Pistol desktop `76fb8b0f1bb92b5922feac1a19bef75bccc804ec6649693d31b13542f1b90637`, mobile `6cef823d534ecd307ed9405a27c29f9ec79a8aca420984c3ed4e86dc62eab3bd`.
- Full-page matrix SHA-256, warm-slate: Terra Blade desktop `d9e55432be7f710402248195378f0ab23ddacc0300b3e694a0b09975c99fc4ea`, mobile `4e1b07f36bff22c69b5134467275b5335873026e479d147ff90527293a9969b4`; Iron Bar desktop `bd956e5fc42aecac448b1aa048f16c9137f569a3a34de555c51f236761a7706b`, mobile `067aecebfb6a7224e7802adac5f7759f305315c918770a41ca2160b6937fdfe7`; Flintlock Pistol desktop `608d2468536d8f9dad0b630fee66f7afa250fee50fb903704c6447d6bc77847f`, mobile `c89b5193b83747dcec5434be446b515eeba035a2aaa037a51322f88ddb8e276a`.
- Fresh-stack cold audit under `http://localhost:5176` is also `200` with no overflow or captured console/request failure for all three routes and both probes. Full-page SHA-256: Terra Blade desktop `d73d9c42167a9234fe94ae12ab0c6d269135f413ef651ba1908cc553ac7c9a86`, mobile `fc5b0e119566428cf39e74679a0b07f9d0552f89f979d31c2a9855687004b481`; Iron Bar desktop `f88767e29a5da3f856857627995ead699a3ff91a1deac0b9fc809b7d6aa67fe0`, mobile `6317cf6eac3f62bd37696ec1aaef4ccf618c5f2aa559cede25d04a6f425d65a8`; Flintlock Pistol desktop `12c27777e5427ace434816c357936931f6c4bf3e9a9b93e2de7a742dcafbb176`, mobile `3c03d1685ef9e26250c76ae47eb7d4646f3ae79d9af5f5d42c569358fb0fad8e`.
- Final validation: focused Item/API tests `8/8`; full `pnpm run check` exit `0`; `check:visual-blocked-source` exit `0`; `check:loading-skeleton` remains exactly the eight Task 0 armor-set failures and has no target-route failure; `git diff --check` exit `0`. After a tracked local-stack cold restart, the three runtime gates reproduce only their Task 0 baselines: light-theme's unrelated homepage/article/legacy-slug contrast findings, typography-spacing's `/search ready` timeout, and crafting-wiki-structure's independent `/crafting` findings.
- Residual visual difference is data-bound and intentional under the user's preservation rule: approved Iron Bar groups representative evidence into six cards, while production keeps all 27 existing live source records; Flintlock keeps all three live shop evidence rows instead of merging them into one fact. CSS now uses the approved compact geometry, but changing counts, grouping semantics, or adding a new source expander remains out of scope. Technical acceptance is complete; the entry stays `active`, uncommitted, pending user visual acceptance.

## Item hero material polish

- User visual review accepts the rebuilt Item framework but rejects the remaining surface-detail gap, naming `hero item-approved-hero` as the clearest mismatch. This round is styling-only: no information architecture, data projection, API, interaction, count, or route behavior changes.
- Computed-style comparison at 1440x1000 localizes the mismatch. The production hero has the approved dimensions (`286px` versus reference `282px`) but replaces the reference's final green 125-degree surface with one mixed surface color; plinth and rarity markers use global gold instead of the live rarity semantic; metrics and prices share one surface instead of raised/sunken roles; the page-scoped favorite rule overrides the approved primary button into a pill outline.
- A single-variable runtime injection of only the reference hero surface confirms the diagnosis: structure and geometry remain stable while the perceived material moves toward the approved frame. A second temporary injection of rarity, metric/price, and favorite treatments confirms those are independent cascade/material differences rather than missing DOM. Evidence is ignored under `front-nuxt/tmp/shots/2026-07-29-public-pages/item-polish/`.
- Contract ratchet for this round adds, before implementation, exactly ten expected failures: one page marker for `:data-item-rarity="itemRarity"`; one exact live-label mapping for the three approved sample rarities; one semantic hero/rareness token set; one hero consumer for rarity tint, green surface, and compact shadow; one rarity-aware plinth frame; one rarity-aware label; one exact primary favorite override that keeps the 44px target; one raised metric surface; one sunken price surface; and one light-theme flattening override. Every failure reason is `missing implementation`; no existing assertion moves, widens, or disappears.
- Approved rarity adaptation is label-based rather than item-ID-based. A source recheck found four rarity values across the six approved Item samples: `浅红色`, `蓝色`, `绿色`, and the Anklet of the Wind's `橙色` (`/items/212`, approved `#ffad5d`). The first three are already green; the next single expected red extends the same exact mapping contract to `橙色` before its CSS rule is added. All other live labels retain the existing accent fallback until a separately approved full rarity-palette task. Light themes mix the same data signal into their existing flat surface tokens instead of importing the dark glow.
- The first hero green step is now contract-green and computed at 1440x1000. Production/reference hero geometry is `1376x286` / `1400x282` under the accepted container difference; both render the same five background layers, `44px` grid, rarity radial field, 125-degree green surface, compact shadow, raised metric and sunken price roles. Remaining material drift is below the hero structure: production still applies the global bright gold border and `0 24px 70px` surface shadow to primary/rail cards, while the approved dense body uses separate primary, secondary, sunken, and object levels.
- The next material-depth ratchet adds exactly ten `missing implementation` destinations in `check-detail-layout-contract.mjs`: dark semantic tokens plus light-theme flattening; primary card; secondary card; rail card; sunken band/fact surfaces; raised node surface; stat-list well; rarity-colored value; and the selected anchor inset marker while retaining the shared 44px target. Assertion count for this polish subsection rises from ten to twenty; no existing assertion moves, widens, or disappears. This cycle owns only `assets/css/domains/detail-pages-redesign.css`, the Item portion of the detail-layout contract, and this active entry.
- The material-depth green step passes all twenty polish assertions. The hero now uses the approved five-layer composition, 125-degree green surface, live rarity field/frame/label, raised metrics, sunken prices, and the three-theme primary favorite treatment; primary, secondary, rail, well, sunken, and object surfaces below it now have separate depth roles. The orange rarity mapping for `/items/212` is also contract-covered. See git for code-level diff details.
- Fresh verification after the final CSS is green: `pnpm run check` exit `0`; focused Item/API tests `8/8`; `check:visual-blocked-source` exit `0`; `git diff --check` exit `0`. `check:loading-skeleton` remains exactly the eight recorded `pages/armor-sets/[id].vue` baseline failures and has no Item/NPC/Articles finding. A focused dark probe reports `/items/757` as `200`, zero console/request errors, zero failed images, no horizontal overflow at `1440x1000` and `390x844`; its favorite is an enabled `198x44px` button with `aria-pressed` and a visible `3px` focus outline.
- The remaining light-data matrix covers `/items/22` and `/items/95` under `morning-paper` and `warm-slate` at both probe sizes: all eight cases are `200`, use the requested theme, and have zero console/request errors, failed images, or horizontal overflow. Iron Bar preserves 27 source rows and six usage-preview rows in every case; Flintlock preserves three merchant rows. Screenshot SHA-256: Terra Blade dark desktop `12da55dcef0f71a2f44cf5bbf0461be8fef3b7678627e42b7f5368bd0c3f62c1`, mobile `f17309fb7a675b16bf423970260e5e9003c14047bfcbc965a129a05c6e017829`; Iron Bar morning desktop/mobile `08cb3ea073a594e97ed1406cd7b41ee92136f56627a58f1a473dacdba1f3c6ea` / `607d9444f82c7e1070d018664d530c0ef01d04c49a8368ad1cd9040cad9e056e`, warm-slate desktop/mobile `5542c5d859b8088b239252c8957e22e334744a111a84a5cdbe8a6d72ed2b7ff6` / `dd984b9e9db53a4ee73a954c73196686f13f939e151ac2da3d8cf6945939b3be`; Flintlock morning desktop/mobile `7d22e56584e3bc635c54f6a4c30cae7da7b4fdbc92188fd729148b34e7fa9d4b` / `3f02721e80df2f65b896014139a15f382e513d45a6b0eed31d76917b150a9ecc`, warm-slate desktop/mobile `9c8fd1e9f6f35dc87bccdc83323ea72140ee5777d8203898f5570b30989f4fc9` / `2401b4a52bfc91eba51ec0711e5e530b2527cd897c18fe0d813630bc1471325f`. Evidence remains ignored under `front-nuxt/tmp/shots/2026-07-29-public-pages/item-polish/`; the entry stays `active` pending user visual acceptance.

## Item coverage rail restoration

- User review found that the approved `资料完整度` rail card was omitted during the direct body transplant. Root cause: the ten-row legacy `itemCoverageRows` derivation survived, but the approved rail markup and ring styles were not migrated into `item-approved-body`; the compatibility wrapper therefore remained without visible coverage content.
- Approved correction keeps the reference card between page anchors and related entries, but does not copy sample percentages. Eight fixed live capabilities own the denominator: foundation/classification, price, crafting, usage, image, description/tooltip, effects/equipment, and source records. Available count and rounded percentage derive only from those predicates; the four reference summary lines expose complete, partial, and unavailable states without changing API or data semantics.
- Contract red whitelist adds exactly six `missing implementation` findings before production edits: approved rail order; live ring/tail/group binding; exact eight live capability predicates; dynamic count/percentage formula; compact coverage layout; and token-driven ring/list states. Assertion count only increases; no existing Item, data-safety, fallback, interaction, or material assertion moves or widens.
- The corrected red run matches that whitelist exactly: four `check:detail-layout` destinations and two Item-only `check:public-pages` data assertions fail. An initial contract-placement mistake briefly applied the two data assertions to `pages/index.vue`; it was corrected before any production edit, and the repeated red has no out-of-scope route or retained-contract failure.
- The minimal green restores the approved card between anchors and related entries. It replaces the unused ten-row legacy overview with exactly eight live predicates, derives `available / 8` and the rounded ring percentage, and renders the approved four summary lines with complete, partial, and unavailable states. No item ID, sample percentage, API field, or fallback copy determines the score. See git for code-level diff details.
- Runtime values prove data adaptation: Terra Blade is `5 / 8` and `63%` in all three themes at `1440x1000` and `390x844`; Iron Bar is `7 / 8` and `88%`; Flintlock Pistol is `5 / 8` and `63%` from its live merchant-source shape. Every probe is `200`, places coverage before related entries, has four labelled summary rows, zero console/request failures, zero failed images, and no horizontal overflow. The card is a stable `300x146px` desktop tool and `358x146px` at the approved mobile probe.
- Coverage screenshot SHA-256: Terra Blade dark desktop/mobile `2f9a7efd353190d0f8c45400620ccf1fdc1ef00d7af8adfcd7a0262ef0a7e49a` / `8bbf75ff1d4001aa693af328bd556143a85a095b0d6dc00cd8ac47e9f759d100`; morning-paper desktop/mobile `b5372f7a8af636397a449b723f0db462e71cfdf48847b5196f461bd17091960d` / `4d5c6365a0e2497c2485430b4d93f65380118831a2c41604e7c4445f4ccd76bc`; warm-slate desktop/mobile `46dcb36d6ce0b392c06c5c6b190d2e7a2fac0b374e93c49bd41196599cb013b4` / `84f46e2a4de77b37b858e0f05d7a1c56903c4f4eda626ce6b2057e2802270e21`; Iron Bar dark desktop `322b02daafd2af26e6644f0ebab1693f6243a6c0ffdfef866b2f29049819761a`; Flintlock dark desktop `2c87dc29e4203c913fe3b65af5f87fa74584d1156a2a4faf5461f59afa782f2b`. Evidence remains ignored under `front-nuxt/tmp/shots/2026-07-29-public-pages/item-polish/coverage-rail/`.
- Fresh final gates after the restoration: `pnpm run check` exit `0`; focused Item/API tests `8/8`; `check:visual-blocked-source` exit `0`; `git diff --check` exit `0`. `check:loading-skeleton` remains exactly the eight recorded armor-set baseline failures with no Item/NPC/Articles finding. The entry remains `active` and uncommitted for user visual acceptance.

## Item dark-ground and hero-data refinement

- User review identifies two remaining approved-Hero gaps: the dark route lacks the reference page-ground depth, and the identity/metric presentation omits visible live details. Computed-style evidence locates the background mismatch at the full-page `.screen.detail-screen`, not `.tp-detail-shell`: the approved `.shell` is transparent and width-only, while its `body` supplies a fixed 125-degree forest field plus the 40px grid. The production route currently supplies a flat `--index-bg` plus a 420px dark veil. The correction therefore adds an Item-only route archetype class and must not place paint on the shared layout shell or global `body`.
- Live data inspection for `/api/public/items/757`, `/22`, `/95`, and `/212` confirms the public DTO already supplies `internalName`, English `name`, localized `nameZh`, classification, stackability, damage, defense, knockback, use time, dimensions, and prices. No API or data work is authorized. The current page loses the English value because it reads only absent `nameEn`, and it never binds `internalName`; direct values remain authoritative. The `/20` knockback scale and `60 / useTime` rate are explicitly presentation derivations, shown with a scale suffix and approximate marker rather than represented as API fields.
- Contract red whitelist for this cycle is exactly eight `missing implementation` findings. `check:detail-layout` owns four: the Item-only full-screen class, the dark token/grid ground, fixed background attachment, and metric scale/meter styling. `check:public-pages` owns four: exact safe internal-code mapping/binding, English-name fallback from the distinct live `name`, knockback/use-time presentation derivation, and stackability copy. The previous blanket internal-name prohibition is replaced by one exact allowed projection plus a residual-content prohibition, so it cannot authorize internal names as title/category fallbacks. No existing safety, fallback, interaction, recipe, source, theme, or coverage assertion may move, widen, or disappear.
- The contract-first run returns exactly those eight findings, split `4 + 4`, with no failure outside the whitelist. The internal-name rule now removes only the exact approved safe projection before checking residual page text, preserving the prohibition everywhere else. Production implementation may proceed on `pages/items/[id].vue` and `assets/css/domains/detail-pages-redesign.css`; no shared layout, theme source, API, or DTO file is opened for writes.
- Green-step self-review adds one bounded data-degradation red: the knockback `/20` presentation suffix must render only when the live knockback value exists, otherwise a missing value would misleadingly read `— / 20`. This changes no current sample output and adds no API or fallback data.
- The eight-item green step and the bounded degradation correction are contract-green. The route now exposes `detail-screen item-detail-approved-screen`; only `[data-theme="dark"]` replaces that full-screen ground, using the existing page, moss, accent, and 40px grid tokens with fixed attachment. The shared `.tp-detail-shell`, global `body`, `hifi-preview.css`, and both light-theme ground rules remain unchanged. Computed dark ground matches the reference's four layers, `auto / 40px / 40px / auto` sizing, and four fixed attachments; its gradient stops resolve to approximately `rgb(10,16,11) / rgb(24,35,21) / rgb(22,29,17)` versus reference `rgb(9,16,11) / rgb(23,36,20) / rgb(23,29,18)`.
- Hero identity and metrics now consume the live DTO without sample IDs: Terra Blade renders `TerraBlade`, `Terra Blade`, `不可堆叠`, `85`, `7 / 20`, the 35% semantic meter, `18`, `≈ 3.3 次 / 秒`, and non-armor defense copy. Iron Bar adapts to `IronBar`, `Iron Bar`, `最大堆叠 9999`, and its recipe/source/usage counts; Flintlock Pistol adapts to `FlintlockPistol`, `Flintlock Pistol`, `不可堆叠`, `1 / 20`, and `≈ 3.8 次 / 秒`. Missing knockback suppresses both the meter and scale suffix.
- Runtime matrix for Terra Blade covers `dark / morning-paper / warm-slate` at `1440x1000 / 390x844`: all six return `200`, apply the requested theme, show the exact identity/metric values, and have zero console errors, request failures, or horizontal overflow. Dark desktop/mobile screenshot SHA-256 is `111c798fa8684945d9da595f07d51294546add06ba86e4efb3dfce2ea846d3c6` / `0bce5dc0472ce11c0596152e16eedc59aa6efcba906b85f407de85593ca971ef`; morning-paper is `43e36ba587f0076f54cd8b6e4028748eb789ee16d10d39beacf068c93b30e052` / `915759d850e762f69fe578309d4b848eda18fe68f003d22e4de08ff60b35b26c`; warm-slate is `7d8ba2e03882dba5b620ce47a9abd8eda59c00bdc620583d32ae566a4096f4` / `e6af8bd9f482d10d12bc070e1a54449f5a059c00bdc620583d32ae566a4096f4`. Reference desktop SHA-256 is `8e4f17559c14bd470482e96c7eec23bc2380f90f6036106db3ad534f1a9debe9`; evidence remains ignored under `front-nuxt/tmp/shots/2026-07-29-public-pages/item-polish/dark-ground-hero/`.
- Fresh final validation after the degradation fix: `pnpm run check` exit `0`; focused Item/API tests `8/8`; `check:visual-blocked-source` exit `0`; `git diff --check` exit `0`. `check:loading-skeleton` remains exactly the eight recorded armor-set failures. Runtime `check:typography-spacing` still times out at the recorded unrelated `/search ready` baseline; `check:light-theme` preserves its recorded homepage/article/legacy-slug findings and reports no `/items/757` regression. The entry remains `active`, uncommitted, and served at `http://localhost:5176/items/757` for user visual acceptance.

## Item checkpoint and continuation

- User authorized a focused checkpoint commit for the current Item result and
  requested immediate continuation on the remaining approved NPC and Articles
  pages. This is not task closeout: the entry stays `active`, and no push is
  authorized.
- The continuation order is NPC first, then Articles. Each slice keeps the
  production navigation, breadcrumb, container proportion, live-data and
  interaction contracts, and three-theme behavior; API, database, crawler,
  unapproved design HTML, old devlogs, and `reports/` remain out of scope.
- The checkpoint includes the already completed shared projection, contract,
  audit harness, Item, NPC, and Articles foundation paths. The next fidelity
  edits must build on that checkpoint and record their own exact expected-red
  contract lists before changing production markup or CSS.
