# Daily Project Management - 2026-07-29

## Daily Summary

- Data/scripts: Wave 2 completed the exact biomes policy-promotion retry; formal readback is policy v1 `L1/ACTIVE` with zero external active transactions.
- Bug fix: the first formal NPC maint phase rolled back before writing because generic persistence included non-column `scope` and `tableName` metadata. Retry-02 then rolled back before writing because strict MySQL rejected ISO-8601 `DATETIME` values. The failing production-adapter and landing regressions now prove the three owned date fields are normalized as UTC at persistence.
- Governance: both consumed NPC phase-1 decisions remain non-reusable. Current-byte retry-03 request `sha256:d4bbf59809fb918a75396f9f92c473ff1c9a169056587712213bef99a3670427` requires the next exact Owner decision.

## Plan Alignment

- Long-term plan: `docs/superpowers/plans/2026-07-27-crawler-automated-ingestion-closure.md`.
- Milestone: Task 14 policy-promotion checkpoint completed; Task 13 NPC owner chain remains fail-closed at phase 1.
- Progress verdict: on track within the approval-gated design; no scope expansion.

## Project Health

- Stable: governed landing remains 1 base / 25 crawler facts; maint remains 0 after both rollbacks; biomes policy is auditably L1/ACTIVE.
- Weak: full gate is still 40 pass / 4 warning / 1 blocked from prior evidence; NPC T1 and all downstream owner phases are incomplete.
- Needs management attention: policy promotion is not an L1 apply authorization.

## Risk Register Updates

- Escalated: both phase-1 decisions are consumed after separate pre-row failures; the datetime fix requires a new NPC decision.
- Added: retain an explicit boundary between biomes L1 policy and its first frozen L1 apply.

## Priority Plan

- P0: obtain Owner authorization for retry-03 and execute it only through the packet runner.
- P1: after phase 1 commits, regenerate the phase-2 request bound to its result bytes.
- P2: freeze the first biomes preview bundle; do not apply it without an independent authorization.

## Evidence And Unverified Items

- Verified by command: authorization, runner, policy, NPC-owner, and maint suites 101 pass / 1 existing skip; formal DB readback for rollback, policy state, decision ledger, and external transactions.
- Not verified: full repository gate, NPC T1, any biomes L1 apply, L2, scheduler, push, or merge.
