# V1.0 批次汇总与下一步（截至 2026-03-18）

## 当前批次状态

### 已完成

- Batch 21：根 README 启动指引跑通基础链路
- Batch 22：完成一轮全链路回归，修复本地环境关键阻塞项
- Batch 23：导入与校验脚本收口，样本链路可执行
- Batch 24：稀有度表现与映射统一
- Batch 25：前台虚拟滚动列表接入
- Batch 26：后端 Redis 缓存接入
- Batch 27：物品对比功能明确延期到 V1.1 之后，不进入当前收口范围

### 新增确认结论

- 三端当前已可本地访问：
  - 前台 `5174`
  - 管理端 `3001`
  - 后端 `8888`
- Redis 当前开发配置已落地并验证：
  - 地址 `127.0.0.1:6380`
  - 密码 `root`
- Redis 缓存验证已完成，不再属于待办项：
  - `stats:overview` 已验证写入
  - `item:detail` 已验证写入
  - `GET /api/items/6122` 连续请求返回 `200`

## 已关闭事项

以下事项不再作为“下一批待验证”：

- Redis 是否可用
- Redis 是否已接入后端
- `item:detail` 缓存是否可命中
- `stats:overview` 缓存是否可命中

这些事项已经完成验证，应从后续待办中移除。

## 当前剩余问题

- 根 `README.md` 尚需保持与当前脚本、端口、Redis 配置完全一致
- `project-plan` 下部分旧文档仍保留“Redis 未验证”的过时结论
- 本地启动脚本已能用，但管理端启动路径仍建议在后续批次继续做稳定性收平
- 三端生产构建与产物验收尚未正式完成

## 下一批建议

下一批优先级重新排序如下：

### Batch 28：文档收口

目标：

- 更新根 README
- 对齐旧批次文档
- 补齐本地启动与缓存验收指令

输出：

- 可直接使用的 `README.md`
- 对齐后的 `project-plan` 阶段文档
- 一份可复用的本地执行说明

### Batch 29：三端生产构建与产物验收

目标：

- 后端 `mvn -DskipTests package`
- 前台 `pnpm run build`
- 管理端 `pnpm run build`

前置条件：

- Maven 可写本地仓库路径明确
- 管理端构建内存参数明确
- 前端构建权限问题明确

### Batch 30：发布前回归与归档

目标：

- 基于构建产物做全链路回归
- 汇总日志、产物、结论到 `reports/` 或发布目录

## 立即执行建议

1. 先完成文档收口，不继续新增功能。
2. 文档收口完成后，再做三端生产构建。
3. 构建完成后，再做基于产物的回归和归档。
