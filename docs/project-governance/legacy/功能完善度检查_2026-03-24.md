# TerraPedia 功能完善度检查（2026-03-24）

## 1. 评估目标

- 目标：检查当前项目功能是否“已完善到可用口径”，并给出缺口与建议。
- 参考计划文档：
  - `project-plan/V1.0_项目计划.md`
  - `project-plan/Batch29_执行规划_2026-03-23.md`
  - `project-plan/Batch36_全量功能测试汇总_2026-03-23.md`
  - `project-plan/三端联动调试报告_2026-03-23.md`

## 2. 本次实际核验（2026-03-24）

### 2.1 命令核验结果

- `back`: `mvn -q test` -> 通过（有 JDK/agent warning，不阻塞）
- `front`: `pnpm run check` -> 通过
- `front`: `pnpm run build` -> 通过（含 PWA 产物生成）
- `data-query-app`: `pnpm run check` -> 通过（`vue-router/volar` 已知 warning）
- `data-query-app`: `pnpm run build` -> 通过（有 sourcemap / Node deprecation warning）

### 2.2 配置与联动口径检查

- 端口口径与 Batch29 一致：Redis `6380`、后端 `8888`、前台 `5174`、管理端 `3001`。
- `scripts/dev/start-local-stack.ps1` 中前台/管理端启动参数传递正常。
- `scripts/dev/run-backend-v1-local.ps1` 显式注入 `TERRAPEDIA_REDIS_*`（`6380/root`）。
- `back/src/main/resources/application.yml` 同时配置 `spring.data.redis` 与 `spring.redis`，与 Batch29 要求一致。

## 3. 对照计划的完成度判断

> 结论按“代码现状 + 本次命令 + 历史批次报告”综合给出。

### 3.1 核心能力（P0 主链路）

- 三端启动与基础联动：已完成。
- 后端物品/分类/统计/导入 API：已完成。
- 管理端物品与分类管理：已完成。
- 用户认证与安全链路（注册、登录、改密、删号、防刷、验证码）：已完成。
- 文章链路（草稿 -> 提审 -> 审核 -> 发布 -> 下线）：已完成。
- 逻辑删除（`@TableLogic`）与 Flyway 迁移：已落地。
- Swagger 文档：已落地（`/api/swagger-ui.html`）。

### 3.2 V1.0 计划项完成情况（按当前可验证信息）

已完成或可判定完成：

- ItemMapper 字段映射修复（`description/damage/defense`）。
- 主题初始化（启动时调用 `initTheme`）。
- 详情展示增强（含稀有度与分类路径）。
- 搜索增强（关键词 + 分类 + 稀有度）与建议词。
- 列表排序（名称/稀有度）与前端参数透传。
- 管理端文章审核流页面与接口接入。
- 数据库迁移脚本体系（`db/migration/V1..V10`）。

仍有缺口/未明确闭环：

- 发布级 E2E 回归：当前不是“完整发布口径”验收，仍以构建/联调报告为主。
- `data-query-app` 工具链 warning 尚未治理（不阻塞，但影响 CI 噪声）。
- `front` 的“物品对比”功能未见落地（计划中也属于后续项）。

## 4. 综合结论

- 以“当前开发可用口径”判断：**功能已基本完善，核心业务链路可用**。
- 以“发布级口径”判断：**未完全收口**，主要差在一轮严格端到端回归与 warning 治理。

建议状态定义：

- 当前状态：`PASS_WITH_WARNINGS`（与 Batch36 口径一致）
- 完善度评级：`高（约 85%~90%）`

## 5. 建议下一步（最小闭环）

1. 固化一轮端到端回归脚本（含前台筛选/详情、管理端 CRUD、鉴权失效场景）。
2. 处理 `data-query-app` 的 `vue-router/volar` 与 sourcemap/deprecation 告警。
3. 在 `reports/` 产出本轮回归结果，给出最终“发布口径”签署结论。
