# Batch33 全清与邮箱基础能力闭环汇总（2026-03-23）

## 1. 本批次目标
- 全清：清理未使用/冗余代码（聚焦本次邮箱鉴权链路）。
- 邮箱基础操作闭环：邮箱登录、邮箱注册、邮箱验证码重置密码、已登录密码修改。

## 2. 功能完成情况
- 邮箱登录：已可用（`POST /api/user-auth/login`）。
- 邮箱注册：已可用（`POST /api/user-auth/register/code` + `POST /api/user-auth/register`）。
- 已登录修改密码：已可用（`PATCH /api/user-auth/password`）。
- 邮箱验证码重置密码：本批次新增并已联调通过：
  - `POST /api/user-auth/password/reset/code`
  - `POST /api/user-auth/password/reset`

## 3. 后端改动
- 控制器新增接口：
  - `back/src/main/java/com/terraria/skills/controller/UserAuthController.java`
- 服务接口新增能力：
  - `back/src/main/java/com/terraria/skills/service/UserAuthService.java`
- 服务实现新增逻辑：
  - `back/src/main/java/com/terraria/skills/service/impl/UserAuthServiceImpl.java`
  - 新增重置密码流程：验证码校验、密码更新、刷新令牌全量失效、审计日志。
- 验证码服务复用扩展（注册/重置双场景，避免重复代码）：
  - `back/src/main/java/com/terraria/skills/auth/RegisterVerificationService.java`
- 新增 DTO：
  - `back/src/main/java/com/terraria/skills/dto/UserPasswordResetCodeRequestDTO.java`
  - `back/src/main/java/com/terraria/skills/dto/UserPasswordResetRequestDTO.java`

## 4. 前端改动
- API 层新增：
  - `front/src/api/userAuth.ts`
  - `sendPasswordResetCode`
  - `resetUserPassword`
- Store 层新增：
  - `front/src/stores/userAuth.ts`
  - `requestPasswordResetCode`
  - `resetPassword`
- 路由新增：
  - `front/src/main.ts`
  - `/forgot-password`（仅游客可访问）
- 页面新增与登录页优化：
  - 新增 `front/src/views/UserForgotPasswordView.vue`
  - `front/src/views/UserLoginView.vue` 增加“Forgot password?”入口

## 5. 安全与防护
- 验证码服务继续使用 Redis：
  - 发送冷却、邮箱/IP 配额、验证码校验失败锁定。
- 密码策略保持：
  - 10-64 位，必须包含字母和数字。
- 重置密码后：
  - 撤销该用户全部 refresh token（Redis 维护），降低会话被盗风险。
- 审计日志：
  - 增加重置密码发送/失败/成功事件记录。

## 6. “全清”执行项（本批次）
- 删除验证码服务中未使用 getter 方法（避免无用公开 API）。
- 保持 refresh token 数据库表移除路线（V7 建表 + V8 删除）作为迁移历史，不做回滚。

## 7. 测试与联调结果
- 后端单测：
  - 命令：`cd back && mvn -q test`
  - 结果：通过。
- 前端类型检查：
  - 命令：`cd front && pnpm run check`
  - 结果：通过。
- 前端构建：
  - 命令：`cd front && pnpm run build`
  - 结果：通过。
- 数据查询端类型检查：
  - 命令：`cd data-query-app && pnpm run check`
  - 结果：通过（保留既有 `vue-router/volar/sfc-route-blocks` 非阻塞警告）。
- 数据查询端构建：
  - 命令：`cd data-query-app && pnpm run build`
  - 结果：通过。
- 三端联调脚本（含真实邮箱收码 + 重置密码链路）：
  - 命令：`python scripts/dev/test_batch31_user_security_flow.py`
  - 结果：`success=true`，`failedCount=0`。
  - 关键新增通过项：
    - `Password reset code send`
    - `Password reset by email code`
    - `User login (email-reset password)`

## 8. 结论
- 你要求的邮箱基础操作已形成闭环并通过联调：
  - 登录 / 注册 / 邮箱验证码重置密码 / 已登录改密。
- 当前可进入下一批：如邮件模板国际化、验证码场景配置拆分、风控阈值按环境区分。
