# V1.0 后续行动清单（更新于 2026-03-18）

## 已完成

- 本地 Redis 已接入当前开发链路，端口为 `6380`，密码为 `root`
- Redis 基础认证、连通、读写已验证通过
- 后端缓存链路已验证通过：
  - `stats:overview`
  - `item:detail`
- 三端本地访问已可用：
  - `http://localhost:5174`
  - `http://localhost:3001`
  - `http://localhost:8888/api`
- 本地启动与停止脚本已补齐：
  - `scripts/dev/start-local-stack.ps1`
  - `scripts/dev/stop-local-stack.ps1`

## 当前未完成项

### P0

- 根 `README.md` 文档收口
- `project-plan` 旧文档结论对齐
- 本地执行指令文档更新为当前真实端口、脚本和 Redis 配置

### P1

- `start-local-stack.ps1` 管理端启动稳定性继续收平
- 三端生产构建正式验收
- 构建日志与产物路径归档

### P2

- 启动脚本进一步抽象成更稳定的开发工具链
- 将本地缓存验证步骤补充为正式 `reports/` 验收记录

## 已移除待办

以下事项原先被列为待办，但现在已经完成，不应继续保留在后续列表中：

- Redis 缓存是否已接入
- Redis 缓存是否已验证
- `item:detail` 详情缓存是否可用
- `stats:overview` 统计缓存是否可用

## 下一执行顺序

1. 完成文档收口
2. 完成三端生产构建与构建日志归档
3. 基于构建产物做最终回归
