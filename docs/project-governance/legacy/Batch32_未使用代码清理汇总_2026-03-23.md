# Batch32 未使用代码清理汇总（2026-03-23）

## 1. 清理目标
- 将用户 `refresh token` 从数据库实现彻底切换到 Redis 后，清理项目中对应未使用代码。
- 保证运行链路不再依赖 `user_refresh_tokens` 相关 Java/Mapper 代码。

## 2. 已清理内容

### 2.1 删除未使用代码文件
- 删除：`back/src/main/java/com/terraria/skills/entity/UserRefreshToken.java`
- 删除：`back/src/main/java/com/terraria/skills/mapper/UserRefreshTokenMapper.java`
- 删除：`back/src/main/resources/mapper/UserRefreshTokenMapper.xml`

### 2.2 数据结构清理
- 更新：`back/src/main/resources/schema.sql`
  - 移除 `user_refresh_tokens` 建表语句。
- 新增迁移：`back/src/main/resources/db/migration/V8__remove_user_refresh_tokens_table.sql`
  - 执行 `DROP TABLE IF EXISTS user_refresh_tokens;`

### 2.3 Redis 实现保留
- 保留并使用：`back/src/main/java/com/terraria/skills/auth/UserRefreshTokenStoreService.java`
  - refresh token 的存储/单点吊销/全量吊销均由 Redis 维护。

## 3. 代码扫描结果
- 全局扫描 `UserRefreshToken` / `UserRefreshTokenMapper` / `user_refresh_tokens`：
  - 业务代码中已无旧实体/旧 Mapper 引用。
  - 仅保留历史迁移 `V7`（创建）与新迁移 `V8`（删除），这是 Flyway 历史需要，非未使用业务代码。

## 4. 回归验证
- 后端单测：`mvn -q test` 通过。
- 用户链路联调脚本：`scripts/dev/test_batch31_user_security_flow.py` 通过（`success=true`）。
- 前端：`pnpm run check && pnpm run build` 通过。
- 管理端：`pnpm run check && pnpm run build` 通过（保留历史非阻塞 warning）。

## 5. 说明
- 若你要求“全项目级绝对零未使用代码”，还需要引入静态分析门禁（如 Java 的 `spotbugs`/`pmd`/`sonarlint`、TS 的 `ts-prune` 等）进行持续扫描。
- 本次已完成你当前需求相关链路（refresh token 数据库实现）的彻底清理与可运行验证。
