# Batch36 全量功能测试汇总（2026-03-23）

## 1. 测试范围
本次按“全量三端 + 联调”执行，覆盖：
- 后端（Spring Boot）单元/集成测试
- 用户端前端（front）类型检查与构建
- 数据查询端（data-query-app）类型检查与构建
- 用户安全与邮箱链路联调（真实邮箱收码）
- 文章编写/审核/发布机制联调（新后端审核流）

## 2. 执行结果总览
- 结论：**PASS_WITH_WARNINGS**（核心功能通过，存在非阻塞工具链告警）
- 核心业务链路：全部通过
- 阻塞失败：0

## 3. 测试矩阵
| 模块 | 命令 | 结果 |
|---|---|---|
| back | `cd back && mvn -q test` | 通过 |
| front | `cd front && pnpm run check` | 通过 |
| front | `cd front && pnpm run build` | 通过 |
| data-query-app | `cd data-query-app && pnpm run check` | 通过（有非阻塞告警） |
| data-query-app | `cd data-query-app && pnpm run build` | 通过（有非阻塞告警） |
| 联调-用户安全 | `python scripts/dev/test_batch31_user_security_flow.py` | 通过（`success=true`） |
| 联调-文章审核流 | `python scripts/dev/test_batch36_article_review_flow.py` | 通过（`success=true`） |

## 4. 联调关键用例结果
### 4.1 用户安全与邮箱链路（batch31）
- 注册验证码发送/冷却：通过
- 注册/登录：通过
- 修改密码后登录：通过
- 邮箱验证码重置密码后登录：通过
- 用户资料更新：通过
- 错误密码删号拒绝：通过

结果文件：
- `reports/2026-03-23-batch31-user-security-flow-recheck.json`

### 4.2 文章审核流（batch36）
- 创建草稿：通过
- 提交审核：通过
- 驳回（带意见）：通过
- 驳回后编辑并再次提交：通过
- 审核通过：通过
- 发布：通过
- 公网读取已发布文章：通过
- 下线：通过
- 下线后公网访问拦截：通过
- 审核日志查询与动作完整性校验：通过
- 旧状态接口兼容直发（自动置 `APPROVED`）：通过

结果文件：
- `reports/2026-03-23-batch36-article-review-flow.json`

## 5. 非阻塞告警
1. `data-query-app` typecheck 告警：
- `vue-router/volar/sfc-route-blocks` 子路径导出告警（历史已知）
- 本次未阻断构建与联调

2. `data-query-app` build 告警：
- `module-preload-polyfill` sourcemap warning
- Node `DEP0155` deprecation warning
- 均未影响产物构建与运行验证

## 6. 风险与建议
- 当前核心业务功能可判定正常。
- 建议下一批将 `data-query-app` 的工具链告警单独治理，减少 CI 噪声并提高问题定位效率。
