# V0.1 Visible Page Review - 2026-05-25

## Routes Reviewed

| Route | Status | Issues |
| --- | --- | --- |
| `/` | HTTP `200` | Manual visual review still required. |
| `/items` | HTTP `200` | Manual visual review still required. |
| `/items/1` | HTTP `200` | Manual visual review still required. |
| `/npcs` | HTTP `200` | Manual visual review still required. |
| `/bosses` | HTTP `200` | Manual visual review still required. |
| `/projectiles` | HTTP `200` | Manual visual review still required. |
| `/armor-sets` | HTTP `200` | Manual visual review still required. |
| `/biomes` | HTTP `200` | Manual visual review still required. |
| `/crafting` | HTTP `200` | Manual visual review still required. |
| `/search` | HTTP `200` | Manual visual review still required. |

## Automated Checks

- Active front service: `http://localhost:5174`.
- Front service listener: `0.0.0.0:5174`.
- Homepage probe: HTTP `200`.
- Public page check: `(cd front-nuxt && pnpm run check:public-pages)` -> `Public page checks passed for 24 Nuxt routes.`
- Priority route probes: all 10 priority routes returned HTTP `200`.
- Tooling parse check: `front-nuxt/scripts/check-public-pages.mjs` and `front-nuxt/scripts/check-visual-regression.mjs` parse successfully.
- Report artifact check: no untracked `reports/front-nuxt` files from this automated route review.

## P0 Fixes

- None from automated route reachability checks.

## P1 Fixes

- Manual visual review remains required for missing images, layout overlap, mobile overflow, empty Chinese names, and raw technical labels.
- `front-nuxt/package.json` defaults dev to port `5176`, while the active local stack front service is on `5174`; reviewers should use the stack URL or `TERRAPEDIA_FRONT_NUXT_URL=http://localhost:5174`.

## Accepted Preview Limitations

- Manual browser review is `operator-not-reviewed` in this automated pass.
- `pnpm run check:visual` was not run in this pass to avoid creating visual report/screenshot artifacts without an operator visual-review decision.

## Validation

- Public page check: passed, `24` routes.
- Visual check: not run; manual checkpoint remains.
- Typecheck: not run in this automated route-only pass.
