# Domain A-Grade Source Snapshot Evidence

Date: 2026-05-24

## Scope

This checkpoint ran bounded source snapshot evidence for the four Group B source-readiness blockers:

- `bosses/sourceReadiness`
- `armor_sets/sourceReadiness`
- `support.shimmer/sourceReadiness`
- `support.town_npc_maintenance/sourceReadiness`

No item-page crawl, backend refresh apply, import, backfill, DB write, or production mutation was run.

## Preconditions

- Base branch: `fix/domain-a-grade-source-evidence-2026-05-24`
- Base commit: `a811d9e12e4ce5ddadc60ccaf80d8bd92f5a0e54`
- Task 1 progress contract commit was present: `ebfba10 feat(data): add domain source progress contracts`
- Active writer check at 2026-05-24T08:36:57+08:00 found no active `run-backend-data-refresh`, `run-wiki-sync`, `fetch-wiki`, `item-page`, or `crawler` writer other than the check command itself.
- `reports/backend-refresh/backend-refresh.lock.json` was absent.

Python dependency check:

- `python3 --version`: `Python 3.12.3`
- bare `python3` could not import `bs4`: `ModuleNotFoundError: No module named 'bs4'`
- `uv run --with beautifulsoup4 python3` imported `BeautifulSoup` and `Tag` successfully.
- Town NPC commands used `uv run --with beautifulsoup4 python3`; no virtualenv or local dependency folder was committed.

Town NPC seed count from `data/generated/npc-standardized-map.json`:

```json
{
  "townNpcSeedCount": 39,
  "sampleIds": ["17", "18", "19", "20", "22"]
}
```

## Live Fetch Window

- Start: 2026-05-24T08:36:57+08:00
- End: approximately 2026-05-24T08:50:46+08:00
- Operator intent: live network fetch was approved in this session.
- Stop-loss: each source lane was run one at a time; no concurrent wiki pressure was used.

One premature `--help` probe for `fetch-wiki-bosses.mjs` accidentally entered live fetch behavior before the formal smoke step. It was stopped after writing `data/generated/domain-source-bosses-progress.latest.json` with `status=running`; no gate evidence was generated by that probe. The formal Boss fetch below overwrote the progress file and completed normally.

## Commands

Town NPC smoke:

```bash
uv run --with beautifulsoup4 python3 scripts/data/fetch/fetch-wiki-town-npc-maintenance.py \
  --progress-path=data/generated/domain-source-town-npc-maintenance-progress.latest.json \
  --source=data/generated/npc-standardized-map.json \
  --output=/tmp/terrapedia-town-npc-maintenance-smoke.json \
  --snapshot-output=/tmp/terrapedia-town-npc-maintenance-smoke-report.json \
  --limit=2
```

Exit code: `0`

Result:

```json
{
  "seedCount": 2,
  "scrapedCount": 2,
  "errorCount": 0,
  "shopItems": 35
}
```

Boss source snapshot:

```bash
node scripts/data/fetch/fetch-wiki-bosses.mjs \
  --progress-path=data/generated/domain-source-bosses-progress.latest.json \
  --output-json=data/generated/wiki-bosses.latest.json \
  --report-json=reports/wiki-bosses-fetch-2026-05-24.json \
  --max-records=40
```

Exit code: `0`

Result:

```json
{
  "totalBosses": 33,
  "unresolved": []
}
```

Armor set source snapshot:

```bash
node scripts/data/fetch/fetch-wiki-armor-sets.mjs \
  --progress-path=data/generated/domain-source-armor-sets-progress.latest.json \
  --output-dir=data/generated \
  --keep-snapshot=false
```

Exit code: `0`

Result: fetched `88` wiki armor sets from `盔甲`.

Shimmer raw page source:

```bash
node scripts/data/fetch/fetch-wiki-shimmer-page.mjs \
  --progress-path=data/generated/domain-source-shimmer-progress.latest.json \
  --output=data/generated/wiki-shimmer.latest.json \
  --report-output=reports/wiki-shimmer-summary-2026-05-24.md
```

Exit code: `0`

Result:

```json
{
  "pageTitle": "微光",
  "pageId": 26209,
  "revisionTimestamp": "2026-05-04T13:58:46Z",
  "sectionCount": 10
}
```

Shimmer importable transform:

```bash
node scripts/data/transform/transform-wiki-shimmer-to-importable.mjs \
  --input=data/generated/wiki-shimmer.latest.json \
  --output=data/generated/shimmer \
  --report-output=reports/wiki-shimmer-importable-summary-2026-05-24.md \
  --use-db-lookup=false
```

Initial exit code: `1`

Root cause: `mysql2/promise` was required at module load even when `--use-db-lookup=false` was supplied.

Repair: moved `mysql2/promise` loading into `enrichLookupsFromDb` and added `scripts/data/transform/transform-wiki-shimmer-to-importable.test.mjs`.

Verification:

```bash
node --test scripts/data/transform/transform-wiki-shimmer-to-importable.test.mjs
```

Exit code: `0`

Rerun exit code: `0`

Result:

```json
{
  "contextRecords": 1,
  "itemTransforms": 279,
  "decraftRules": 248,
  "entityTransforms": 121,
  "npcTransforms": 29,
  "unresolvedTitles": 250
}
```

Town NPC full bounded source snapshot:

```bash
uv run --with beautifulsoup4 python3 scripts/data/fetch/fetch-wiki-town-npc-maintenance.py \
  --progress-path=data/generated/domain-source-town-npc-maintenance-progress.latest.json \
  --source=data/generated/npc-standardized-map.json \
  --output=data/generated/wiki-town-npc-maintenance.latest.json \
  --snapshot-output=reports/wiki-town-npc-maintenance-2026-05-24.json \
  --limit=39
```

Exit code: `0`

Result:

```json
{
  "seedCount": 39,
  "scrapedCount": 39,
  "errorCount": 0,
  "shopItems": 631
}
```

Domain reports and gate:

```bash
node scripts/data/workflow/domain-acceptance-generate-reports.mjs --repo-root="$(pwd)" --write=true
node scripts/data/workflow/domain-acceptance-a-grade-gate.mjs --repo-root="$(pwd)" --fail-on-blocked=true > /tmp/terrapedia-domain-a-grade-source-snapshots.json || test "$?" -eq 1
```

Exit codes: `0`, accepted blocked exit `1`

Generated report summary:

```json
{
  "passCount": 27,
  "warningCount": 16,
  "blockedCount": 2,
  "missingCount": 0
}
```

A-grade gate summary:

```json
{
  "overallStatus": "blocked",
  "generatedBlockedCount": 2,
  "generatedWarningCount": 16,
  "freshCount": 45,
  "staleCount": 0,
  "missingCount": 0,
  "unknownCount": 0
}
```

## Durable Evidence

Committed gate-consumed source evidence:

- `data/generated/wiki-bosses.latest.json`
- `data/generated/wiki-armor-sets.latest.json`
- `data/generated/wiki-town-npc-maintenance.latest.json`
- `data/generated/shimmer/wiki-shimmer-manifest.latest.json`
- `data/generated/shimmer/wiki-shimmer-context.importable.latest.json`
- `data/generated/shimmer/wiki-shimmer-item-transforms.importable.latest.json`
- `reports/wiki-bosses-fetch-2026-05-24.json`
- `reports/wiki-shimmer-summary-2026-05-24.md`
- `reports/wiki-shimmer-importable-summary-2026-05-24.md`
- `reports/wiki-town-npc-maintenance-2026-05-24.json`
- `reports/domain/**/2026-05-24.json`

Not committed:

- `data/generated/domain-source-*-progress.latest.json` operational progress files
- `data/generated/wiki-shimmer.latest.json` raw page snapshot
- optional shimmer transform outputs not consumed by the current gate
- `/tmp/terrapedia-town-npc-maintenance-smoke*.json`

## Result

The four source-readiness blockers from the baseline are closed or downgraded to non-blocking quality warnings:

- `bosses/sourceReadiness`: `blocked` -> `warning`
- `armor_sets/sourceReadiness`: `blocked` -> `pass`
- `support.shimmer/sourceReadiness`: `blocked` -> `pass`
- `support.town_npc_maintenance/sourceReadiness`: `blocked` -> `warning`

Remaining blocked panels after Task 2:

- `bosses/imageReadiness`: missing `reports/audit/image-source-lineage*.json`
- `projectiles/relationReadiness`: old relation baseline reports `projectiles.nameZh.gap=1006`
