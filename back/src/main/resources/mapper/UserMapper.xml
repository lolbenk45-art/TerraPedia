<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.terraria.skills.mapper.UserMapper">

    <select id="selectByEmail" resultType="com.terraria.skills.entity.User">
        SELECT
            id,
            email,
            password_hash AS passwordHash,
            display_name AS displayName,
            avatar_url AS avatarUrl,
            avatar_object_key AS avatarObjectKey,
            avatar_updated_at AS avatarUpdatedAt,
            status,
            last_login_at AS lastLoginAt,
            deleted,
            created_at AS createdAt,
            updated_at AS updatedAt
        FROM users
        WHERE email = #{email}
          AND deleted = 0
        LIMIT 1
    </select>

    <select id="selectByEmailIncludeDeleted" resultType="com.terraria.skills.entity.User">
        SELECT
            id,
            email,
            password_hash AS passwordHash,
            display_name AS displayName,
            avatar_url AS avatarUrl,
            avatar_object_key AS avatarObjectKey,
            avatar_updated_at AS avatarUpdatedAt,
            status,
            last_login_at AS lastLoginAt,
            deleted,
            created_at AS createdAt,
            updated_at AS updatedAt
        FROM users
        WHERE email = #{email}
        LIMIT 1
    </select>

    <select id="selectPublicUserById" resultType="com.terraria.skills.entity.User">
        SELECT
            id,
            display_name AS displayName,
            avatar_url AS avatarUrl,
            created_at AS createdAt
        FROM users
        WHERE id = #{id}
          AND deleted = 0
          AND status = 1
        LIMIT 1
    </select>

    <update id="reactivateDeletedUser">
        UPDATE users
        SET
            password_hash = #{passwordHash},
            display_name = #{displayName},
            avatar_url = NULL,
            avatar_object_key = NULL,
            avatar_updated_at = NULL,
            status = #{status},
            deleted = 0,
            last_login_at = #{lastLoginAt},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateAvatar">
        UPDATE users
        SET avatar_url = #{avatarUrl},
            avatar_object_key = #{avatarObjectKey},
            avatar_updated_at = #{avatarUpdatedAt},
            updated_at = NOW()
        WHERE id = #{id}
          AND deleted = 0
    </update>

    <update id="clearAvatar">
        UPDATE users
        SET avatar_url = NULL,
            avatar_object_key = NULL,
            avatar_updated_at = NULL,
            updated_at = NOW()
        WHERE id = #{id}
          AND deleted = 0
    </update>

    <select id="selectAdminUsersPage" resultType="com.terraria.skills.dto.AdminUserListItemDTO">
        SELECT
            id,
            email,
            display_name AS displayName,
            status,
            last_login_at AS lastLoginAt,
            created_at AS createdAt
        FROM users
        <where>
            deleted = 0
            <if test="email != null and email != ''">
                AND email LIKE CONCAT('%', #{email}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY id ASC
    </select>

</mapper>
